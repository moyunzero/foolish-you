/**
 * Canonical daily game API. Implementation lives in `contexts/DailyGameContext.tsx`
 * (not in this file — planning docs may still reference hooks/useDailyGame).
 */
export {
  useDailyGame,
  type DailyGameState,
  type HydrateStatus,
} from '../contexts/DailyGameContext';
