---
phase: v2.5-02-sudoku-technique-rater
plan: 03
type: tdd
wave: 3
depends_on:
  - v2.5-02-01
  - v2.5-02-02
files_modified:
  - lib/puzzles/sudoku/chains.ts
  - lib/puzzles/sudoku/rater.ts
  - __tests__/lib/puzzles/sudoku/rater.test.ts
  - __tests__/lib/puzzles/sudoku/chains.test.ts
autonomous: true
gap_closure: true
requirements:
  - TIER-02
user_setup: []
must_haves:
  truths:
    - "tryShortChain never eliminates via a weak-first open X-chain; open X-chain endpoint elim requires both endpoints to be strong-link arrivals (CR-01, RESEARCH pitfall 1, TIER-02)"
    - "XY-chain lite path under tryShortChain does not perform productive eliminations until a documented sound AIC rule lands (CR-01 / IN-02 guard)"
    - "rateSudoku returns status solved only when the working digit grid is full AND has no house conflicts; conflicted full grids return incomplete (WR-01)"
    - "Fixture tests assert both expectedTier and expectedPeak when status is solved (WR-02, ROADMAP SC-1)"
    - "Expert peak accept path still works via xy_wing fixtures; no mastery/UI/npm changes"
  artifacts:
    - path: lib/puzzles/sudoku/chains.ts
      provides: "Sound open X-chain seed/elim algebra; non-productive XY-chain lite"
      exports: ["tryShortChain", "EXPERT_CHAIN_MAX_LENGTH", "EXPERT_CHAIN_MAX_NODES"]
    - path: lib/puzzles/sudoku/rater.ts
      provides: "Conflict-validated solved termination (validate-only, no solver placement)"
      exports: ["rateSudoku", "RateSudokuResult"]
    - path: __tests__/lib/puzzles/sudoku/chains.test.ts
      provides: "Regression: illegal weak-first chain must not strip victim candidate"
    - path: __tests__/lib/puzzles/sudoku/rater.test.ts
      provides: "expectedPeak asserts + conflicted-full → incomplete"
  key_links:
    - from: "tryShortChain X-chain BFS"
      to: "strongPairs / isStrong"
      via: "seed depth-0 as not-yet-strong; first hop must be strong; elim only when depth>=2 and arrival is strong"
    - from: "rateSudoku"
      to: "house-conflict scan on board.digits"
      via: "before status solved; conflicts → incomplete; never import solver for placement"
    - from: "generateSudokuPuzzleForTier"
      to: "rateSudoku"
      via: "unchanged accept gate; benefits from sound short_chain + conflict check (no generateForTier edits required)"
---

<objective>
Close verification gaps CR-01 / WR-01 / WR-02 so Sudoku peak-tier accept (TIER-02) cannot be poisoned by unsound `short_chain` eliminations or conflicted “full” grids.

Purpose: Restore the plan must-have that ratings use only logical techniques before v2.5-05 mastery wire trusts Expert peaks.
Output: Sound (or non-productive) `tryShortChain`, conflict-checked `rateSudoku` solved path, regression + fixture peak asserts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-CONTEXT.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-RESEARCH.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-VERIFICATION.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-REVIEW.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-01-SUMMARY.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-02-SUMMARY.md
@lib/puzzles/sudoku/chains.ts
@lib/puzzles/sudoku/rater.ts
@lib/puzzles/sudoku/candidates.ts
@lib/puzzles/sudoku/validate.ts
@__tests__/lib/puzzles/sudoku/rater.test.ts
@__tests__/lib/puzzles/sudoku/fixtures/techniqueBoards.ts
@AGENTS.md
</context>

## Discretion locks

| Lock | Choice | Rationale |
|------|--------|-----------|
| CR-01 path | **Sound open X-chain fix** (not full disable of `short_chain`) | REVIEW sketch is surgical: seed depth-0 as not a completed strong arrival so the first hop must be a strong link; eliminate only when `depth >= 2` and the arrival is strong — classic both-endpoints-strong. Expert fixtures already peak on `xy_wing`; sound X-chain remains available for gen without trusting weak-first elim. |
| XY-chain lite (IN-02) | **Disable productive eliminations** in the bivalue XY-chain lite block (never return `applied: true` from that section) | Same `short_chain` label; `exitDigit === entry` branch is over-broad and untested. Prefer non-productive until a documented AIC rule lands — do not invent a full AIC engine in this gap plan. |
| Out of scope | No mastery wire, no UI, no other raters, no new npm packages, no IN-01 dead wing-skip cleanup, no frozen legal `short_chain` peak fixture (optional in VERIFICATION — skip) | Surgical gap closure only. |

## Artifacts this phase produces

| Artifact | Role after gap closure |
|----------|------------------------|
| `lib/puzzles/sudoku/chains.ts` | Open X-chain elim only on strong–weak–strong… endpoints; XY-chain lite non-productive |
| `lib/puzzles/sudoku/rater.ts` | `solved` iff full + conflict-free digits; still no solver placement |
| `__tests__/lib/puzzles/sudoku/chains.test.ts` | Illegal weak-first chain regression |
| `__tests__/lib/puzzles/sudoku/rater.test.ts` | `expectedPeak` + conflicted-full incomplete |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — CR-01 / WR-02 / WR-01 regression tests</name>
  <files>__tests__/lib/puzzles/sudoku/chains.test.ts, __tests__/lib/puzzles/sudoku/rater.test.ts</files>
  <read_first>
    - .planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-REVIEW.md (CR-01 counterexample sketch; WR-01/WR-02)
    - .planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-VERIFICATION.md (gaps.missing)
    - lib/puzzles/sudoku/chains.ts (tryShortChain X-chain BFS ~L87–132; XY-chain lite ~L146–209)
    - lib/puzzles/sudoku/candidates.ts (initTechniqueBoard, eliminateCandidate, digitBit)
    - __tests__/lib/puzzles/sudoku/rater.test.ts (fixture it.each ~L75–85)
    - __tests__/lib/puzzles/sudoku/fixtures/techniqueBoards.ts (expectedPeak fields)
  </read_first>
  <behavior>
    - chains.test: Build a TechniqueBoard (via initTechniqueBoard + manual candidate masks if needed) reproducing the REVIEW illegal pattern: weak unit-share between start A and mid B for digit D, strong link (exactly two candidates in a house) B–C, victim V sees both A and C and still has D. Call tryShortChain. Expect: either applied:false, or if applied, V still has candidate D (digit must NOT be stripped from V). Prefer asserting victim mask still contains D after the call.
    - rater fixtures: In the TIER_FIXTURES it.each, when status==='solved', also expect(result.peak).toBe(fixture.expectedPeak) (WR-02). Keep existing expectedTier assert.
    - rater WR-01: Construct a 9×9 full digit grid with a deliberate house duplicate (e.g. two 5s in row 0) that passes isValidGivens shape checks; rateSudoku must return status incomplete (not solved). Do not call solve() in the test.
  </behavior>
  <action>
    Create `__tests__/lib/puzzles/sudoku/chains.test.ts` with the illegal open-chain victim regression above (CR-01). Update `rater.test.ts` fixture it.each to assert `expectedPeak` alongside `expectedTier` (WR-02). Add one rater test for conflicted full grid → `incomplete` (WR-01). Run tests expecting RED on chains regression and WR-01 until Task 2–3 land; WR-02 may already pass if peaks match — still add the assert. Do not modify production code in this task.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/sudoku/chains.test.ts __tests__/lib/puzzles/sudoku/rater.test.ts --no-coverage 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - chains.test.ts exists and fails (or would fail) against current unsound weak-first elim by documenting victim-must-retain-candidate
    - rater fixture it.each asserts both expectedTier and expectedPeak on solved
    - rater test covers conflicted full grid expecting incomplete
    - No production file edits in this task
  </acceptance_criteria>
  <done>RED tests committed for CR-01 victim, WR-02 peak, WR-01 conflict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — sound open X-chain; disable XY-lite elim</name>
  <files>lib/puzzles/sudoku/chains.ts</files>
  <read_first>
    - lib/puzzles/sudoku/chains.ts (full file)
    - .planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-REVIEW.md (CR-01 Fix sketch)
    - __tests__/lib/puzzles/sudoku/chains.test.ts (from Task 1)
  </read_first>
  <behavior>
    - Open X-chain: depth-0 seed must NOT count as a completed strong arrival; the first expansion must take a strong link (isStrong). Endpoint elimination only when depth &gt;= 2 and the new node is a strong arrival (both endpoints strong). The illegal weak-first victim case must not strip the victim digit.
    - XY-chain lite block: must not return applied:true / must not call eliminateCandidate for productive XY-lite ends (IN-02). Budgets (EXPERT_CHAIN_MAX_*) and X-chain search remain. Returning applied:false, budgetExceeded:false at end of function when no sound X-chain fired is correct.
    - Existing Expert fixture still rates via xy_wing (no change required to fixtures).
  </behavior>
  <action>
    Per Discretion lock: fix open X-chain algebra in `tryShortChain` (seed / first-hop / elim gate) so productive elim requires strong arrivals at both chain ends. Disable productive eliminations in the bivalue XY-chain lite section (leave structure or strip elim returns — never apply under `short_chain` from that lite path). Do not change EXPERT_CHAIN_MAX_LENGTH / EXPERT_CHAIN_MAX_NODES values. Do not add npm packages. Do not touch generateForTier / mastery / UI.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/sudoku/chains.test.ts __tests__/lib/puzzles/sudoku/rater.test.ts --no-coverage 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Illegal weak-first victim retains digit candidate (chains.test green)
    - Open X-chain elim path only fires when first hop is strong and elim ends on strong arrival
    - XY-chain lite never returns applied:true
    - Existing TIER_FIXTURES still solve to expected tiers (and peaks after WR-02 assert)
    - No solver import in chains.ts
  </acceptance_criteria>
  <done>CR-01 closed: sound X-chain + non-productive XY-lite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — conflict check before solved</name>
  <files>lib/puzzles/sudoku/rater.ts</files>
  <read_first>
    - lib/puzzles/sudoku/rater.ts (termination ~L68–101)
    - lib/puzzles/sudoku/techniques.ts (isGridFull)
    - lib/puzzles/sudoku/validate.ts (getConflictCells / house peer pattern — reference only; prefer digits-only helper to avoid play-state coupling)
    - __tests__/lib/puzzles/sudoku/rater.test.ts (WR-01 conflict case)
  </read_first>
  <behavior>
    - When the rate loop exits because isGridFull is true, before returning solved: scan board.digits for duplicate nonzero values in any row, column, or 3×3 box. If any conflict → return { status: 'incomplete', peak } (peak may be non-null from steps taken).
    - Still never import ./solver or call solve for placement (validate-only).
    - Already-full trivial path (peak == null → full_house easy) must also refuse solved if conflicts exist.
    - Malformed givens path unchanged (incomplete, peak null).
  </behavior>
  <action>
    Add a small private digits house-conflict scan inside `rater.ts` (or a pure helper colocated in candidates/techniques if cleaner — prefer private in rater.ts to minimize blast radius). Wire it on both solved return sites after the loop. Conflicts → incomplete. Do not change RateSudokuResult shape. Do not edit generateForTier.ts. Confirm source-contract test (no solver / no Date.now) still passes.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/sudoku/chains.test.ts __tests__/lib/puzzles/sudoku/rater.test.ts __tests__/lib/puzzles/sudoku/generateForTier.test.ts --no-coverage 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Conflicted full grid → incomplete (WR-01 green)
    - Valid fixtures still status solved with expectedTier + expectedPeak
    - rater.ts has no solver import and no solve( call for placement
    - generateForTier suite still green (accept gate inherits safer rateSudoku)
  </acceptance_criteria>
  <done>WR-01 closed; full CR-01/WR-01/WR-02 gap set green with gen regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seeded givens → rateSudoku | Untrusted / malformed grids already rejected by shape check; technique steps mutate local board only |
| rateSudoku → generateSudokuPuzzleForTier | Gen accepts solely on solved + exact tier — unsound elim or conflicted solved poisons Expert accept (TIER-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2.5-02-03-01 | Tampering | tryShortChain open X-chain | high | mitigate | Strong–weak–strong endpoint algebra; regression test forbids illegal victim strip |
| T-v2.5-02-03-02 | Tampering | tryShortChain XY-chain lite | medium | mitigate | Disable productive XY-lite elim until documented AIC |
| T-v2.5-02-03-03 | Elevation | rateSudoku solved termination | medium | mitigate | House-conflict validate-only gate before solved; never solver placement |
| T-v2.5-02-03-04 | Denial of service | chain budgets | low | accept | Existing EXPERT_CHAIN_MAX_* unchanged; gap does not widen search |
| T-v2.5-02-SC | Tampering | npm installs | high | accept | No new packages in this plan |
</threat_model>

<verification>
- `npx jest` on chains + rater + generateForTier suites green
- Illegal weak-first victim candidate retained
- Fixtures assert expectedPeak
- Conflicted full → incomplete; no solver placement in rater
</verification>

<success_criteria>
- VERIFICATION truth #5 (logical-only techniques) restored for short_chain path
- TIER-02 peak-gate integrity no longer poisoned by unsound short_chain or conflicted solved
- CR-01, WR-01, WR-02 closed; IN-01 left deferred; no mastery/UI scope creep
</success_criteria>

<output>
Create `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-03-SUMMARY.md` when done
</output>
