---
phase: v2.5-02-sudoku-technique-rater
plan: 02
type: execute
wave: 2
depends_on:
  - v2.5-02-01
files_modified:
  - lib/puzzles/sudoku/generator.ts
  - lib/puzzles/sudoku/generateForTier.ts
  - __tests__/lib/puzzles/sudoku/generateForTier.test.ts
  - __tests__/lib/puzzles/sudoku/generator.test.ts
autonomous: true
requirements:
  - TIER-02
user_setup: []
must_haves:
  truths:
    - "generateSudokuPuzzleForTier(seed, targetTier) returns a unique-solution puzzle whose rated peak maps to exact targetTier, or after exhausted attempts softens only toward easier with softened:true + actual ratedTier (D-03, D-05, D-07, ROADMAP SC-2)"
    - "Legacy generateSudokuPuzzle(seed, dateKey?) signature and weekday-given behavior unchanged for existing callers (D-05)"
    - "Same seed + targetTier → identical puzzleHash / givens (determinism)"
    - "Expert generation completes under attempt+rate budgets without hang; incomplete/budget rates are discarded and retried or softened (D-04, ROADMAP SC-3)"
    - "No mastery hydrate / daily type-selector wiring in this phase (D-06)"
  artifacts:
    - path: lib/puzzles/sudoku/generateForTier.ts
      provides: "generateSudokuPuzzleForTier + givenGuideForTier + softenTowardEasier"
      exports: ["generateSudokuPuzzleForTier", "givenGuideForTier"]
    - path: lib/puzzles/sudoku/generator.ts
      provides: "Unchanged public generateSudokuPuzzle; shared carve helper exported or reused"
    - path: __tests__/lib/puzzles/sudoku/generateForTier.test.ts
      provides: "Determinism, unique solution, exact-tier accept, soften invariant, Expert no-hang"
  key_links:
    - from: "generateSudokuPuzzleForTier"
      to: "rateSudoku"
      via: "after unique carve: accept only status===solved && rated.tier===currentTarget"
      pattern: "rateSudoku"
    - from: "generateSudokuPuzzleForTier"
      to: "countSolutionsUpTo"
      via: "reject unless === 1 before rate accept"
    - from: "generateSudokuPuzzle"
      to: "shared carve / generateOnce"
      via: "public signature unchanged; weekday path still uses sudokuGivensForDate"
---

<objective>
Add `generateSudokuPuzzleForTier` — carve with given-count guides, unique-solution gate, exact peak-tier accept, soften-toward-easier fallback — while keeping `generateSudokuPuzzle(seed, dateKey?)` behavior intact (D-03..D-07, TIER-02 gen half).

Purpose: Prove the gen+rate loop in unit tests so v2.5-05 can wire mastery without inventing accept/reject policy later.
Output: `generateForTier.ts`, surgical `generator.ts` extract, `generateForTier.test.ts` (+ legacy generator regression green).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-CONTEXT.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-RESEARCH.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-PATTERNS.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-01-SUMMARY.md
@lib/puzzles/sudoku/generator.ts
@lib/puzzles/sudoku/rater.ts
@lib/puzzles/sudoku/solver.ts
@lib/puzzles/difficulty/tiers.ts
@lib/puzzles/rng.ts
@__tests__/lib/puzzles/sudoku/generator.test.ts
@__tests__/lib/puzzles/sudoku/rater.test.ts
</context>

## Discretion locks (Claude's Discretion → executor MUST follow)

| Topic | Lock |
|-------|------|
| Public API | `generateSudokuPuzzleForTier(seed, targetTier)` returns `{ puzzle, ratedTier, peakTechnique, softened }` — not puzzle-only |
| Legacy API | `generateSudokuPuzzle(seed, dateKey?)` signature + semantics unchanged (D-05) |
| Shared carve | Extract `generateOnce`/`carvePuzzle` for reuse inside `generator.ts` (export `generateOnce` or a named `carveUniquePuzzle(seed, targetGivens)` used by both) — surgical; do not rewrite fill RNG |
| Given guides | easy 36, medium 33, hard 29, expert 25 via `givenGuideForTier` (guides only; peak gates accept) |
| Attempts | `SUDOKU_TIER_MAX_ATTEMPTS = 40` per current target; sub-seed label `tier-${tier}-${soft}-${attempt}` via `deriveSubSeed` |
| Soften | Soften toward easier with `tierFromIndex(tierIndex(t)-1)` until Easy; set `softened: true`; never return a harder tier than originally requested (D-03). Cap total softens so tests stay bounded (`SUDOKU_TIER_MAX_SOFTENS` may be 3 = Expert→Easy path) |
| Accept rule | Exact `rated.tier === currentTarget` only; discard incomplete/budget_exhausted |
| Failure | After all softens exhausted, throw clear Error (tests may use seeds that succeed) |
| Module constants | Prefer top of `generateForTier.ts` over `constants/config.ts` unless a shared config key already exists |
| Out of scope | Do not wire mastery into type selection / hydrate; no UI; no other raters; no new npm packages |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generateSudokuPuzzleForTier loop + shared carve</name>
  <files>lib/puzzles/sudoku/generateForTier.ts, lib/puzzles/sudoku/generator.ts, __tests__/lib/puzzles/sudoku/generateForTier.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-CONTEXT.md` (D-03..D-07)
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-RESEARCH.md` (Pattern 2, generate skeleton, given guides, soften)
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-01-SUMMARY.md` (rateSudoku exports)
    - `lib/puzzles/sudoku/generator.ts`
    - `lib/puzzles/sudoku/rater.ts`
    - `lib/puzzles/difficulty/tiers.ts`
    - `lib/puzzles/rng.ts`
    - `__tests__/lib/puzzles/sudoku/generator.test.ts`
  </read_first>
  <behavior>
    - givenGuideForTier('easy'|'medium'|'hard'|'expert') → 36|33|29|25
    - Same seed+target → identical puzzleHash twice
    - Returned puzzle always countSolutionsUpTo(...,2)===1
    - When softened===false, ratedTier === requested targetTier
    - When softened===true, tierIndex(ratedTier) is strictly below tierIndex(originalTarget)
    - Never produces ratedTier harder than original request
  </behavior>
  <action>
    Per D-03/D-05/D-07: add `generateForTier.ts` implementing `givenGuideForTier` and `generateSudokuPuzzleForTier` as in RESEARCH skeleton — carve via shared helper, require unique solution, call `rateSudoku`, accept only exact tier match, else new deriveSubSeed attempt; after attempt budget soften toward easier only; return `{puzzle, ratedTier, peakTechnique, softened}`.
    Surgically expose shared carve from `generator.ts` for reuse without changing `generateSudokuPuzzle` public behavior (keep weekday `sudokuGivensForDate` path and existing `gen-${attempt}` / `gen-fallback` labels).
    Write `__tests__/lib/puzzles/sudoku/generateForTier.test.ts` first (TDD): determinism, unique solution, exact-tier when not softened, soften-never-harder invariant, and at least one Easy + one non-Easy target seed that completes. Use mulberry32/deriveSubSeed only — no non-deterministic RNG. Do not import or edit the daily type-selection module or mastery hydrate.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/sudoku/generateForTier.test.ts __tests__/lib/puzzles/sudoku/generator.test.ts --no-coverage 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `generateForTier.ts` exports `generateSudokuPuzzleForTier` and `givenGuideForTier`
    - `generateForTier.ts` contains guide values 36, 33, 29, and 25
    - `generateSudokuPuzzle(` still present in `generator.ts` with two-parameter optional dateKey shape
    - `generateForTier.test.ts` asserts unique solution via `countSolutionsUpTo`
  </acceptance_criteria>
  <done>For-tier API returns rated metadata; legacy generator still green; exact accept + soften-toward-easier implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expert budget + soften regression + legacy lock</name>
  <files>__tests__/lib/puzzles/sudoku/generateForTier.test.ts, __tests__/lib/puzzles/sudoku/generator.test.ts, lib/puzzles/sudoku/generateForTier.ts</files>
  <read_first>
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-RESEARCH.md` (D-04 Expert, pitfall 5 softened metadata)
    - `lib/puzzles/sudoku/generateForTier.ts`
    - `lib/puzzles/sudoku/rater.ts`
    - `__tests__/lib/puzzles/sudoku/generateForTier.test.ts`
    - `__tests__/lib/puzzles/sudoku/generator.test.ts`
  </read_first>
  <behavior>
    - Expert target generation finishes within Jest default timeout for a fixed seed (no hang)
    - Incomplete / budget_exhausted carves are not accepted
    - Existing generator.test.ts cases still pass unchanged
    - Soften metadata: if soft path taken, softened===true and ratedTier documented easier
  </behavior>
  <action>
    Per D-04/D-06: extend `generateForTier.test.ts` with Expert-path completion (fixed seed; assert finishes and unique), and an explicit assert that accept path requires `rateSudoku` solved+exact tier (reject incomplete). Keep attempt totals bounded via existing constants so CI stays fast — tune seeds/constants surgically if Expert rarely accepts (prefer raise attempts slightly over expanding exotic techniques).
    Re-run full legacy `generator.test.ts` and confirm weekday/given-count cases untouched. Confirm phase boundary: no mastery selector wire. If soften is hard to trigger with lucky seeds, add a unit test of `softenTowardEasier` / guide helpers and a documented comment on the integration soft path — still return `softened` from the public API.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/sudoku/generateForTier.test.ts __tests__/lib/puzzles/sudoku/generator.test.ts __tests__/lib/puzzles/sudoku/rater.test.ts --no-coverage 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - `generateForTier.test.ts` includes an Expert-target case that completes
    - `generateForTier.ts` return type / result includes `softened` field usage in tests
    - Legacy `__tests__/lib/puzzles/sudoku/generator.test.ts` still passes
    - `git diff --name-only` for this plan work does not list `lib/puzzles/dailySelector.ts` or mastery Context files
  </acceptance_criteria>
  <done>ROADMAP SC-2/SC-3 satisfied for gen loop; legacy callers safe; phase leaves selector unwired (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed + targetTier → generateForTier | Trusted app callers; still must bound CPU |
| generateForTier → rateSudoku | Internal; inherits rater budgets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2.5-02-02-01 | Denial of Service | generateForTier attempt×rate loop | high | mitigate | MAX_ATTEMPTS × softens caps; discard budget_exhausted; never unbounded Expert search |
| T-v2.5-02-02-02 | Tampering | Soften policy | medium | mitigate | Soften only toward easier; return ratedTier+softened; never harder than request (D-03) |
| T-v2.5-02-02-03 | Tampering | Legacy generator integrity | medium | mitigate | Keep public signature; preserve weekday tests; distinct sub-seed prefixes for tier path |
| T-v2.5-02-02-04 | Elevation of Privilege | N/A | low | accept | Offline puzzle gen; no auth surface |
| T-v2.5-02-SC | Tampering | npm installs | high | mitigate | No new packages (D-09) |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx jest __tests__/lib/puzzles/sudoku/generateForTier.test.ts __tests__/lib/puzzles/sudoku/generator.test.ts __tests__/lib/puzzles/sudoku/rater.test.ts --no-coverage` passes
- [ ] `npm run typecheck` passes
- [ ] Legacy `generateSudokuPuzzle` weekday/determinism tests green
- [ ] No daily type-selector / mastery hydrate edits
</verification>

<success_criteria>
- TIER-02 generate-until-accepts loop delivered with peak gate + given guides
- ROADMAP SC-2: target tier unique boards within attempt budget (or documented soften)
- ROADMAP SC-3: Expert path budgeted, no hang
- D-05/D-06 honored: legacy API intact; selector unwired
</success_criteria>

<output>
Create `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-02-SUMMARY.md` when done
</output>
