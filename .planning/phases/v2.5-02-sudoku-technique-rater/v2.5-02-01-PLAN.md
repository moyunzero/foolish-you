---
phase: v2.5-02-sudoku-technique-rater
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/puzzles/sudoku/candidates.ts
  - lib/puzzles/sudoku/techniqueIds.ts
  - lib/puzzles/sudoku/techniques.ts
  - lib/puzzles/sudoku/chains.ts
  - lib/puzzles/sudoku/rater.ts
  - __tests__/lib/puzzles/sudoku/fixtures/techniqueBoards.ts
  - __tests__/lib/puzzles/sudoku/rater.test.ts
autonomous: true
requirements:
  - TIER-01
  - TIER-02
user_setup: []
must_haves:
  truths:
    - "rateSudoku returns DifficultyTier from shared tiers.ts (no second enum) with peak technique mapped per D-02 bands (D-01, TIER-01)"
    - "Labeled fixture boards rate to their expected Easy/Medium/Hard/Expert tiers (ROADMAP SC-1, TIER-02)"
    - "Rater places digits only via logical techniques — never via backtracking solve for placement (RESEARCH pitfall 1)"
    - "Expert chain search respects step/node caps; budget trip yields budget_exhausted / incomplete, never hangs (D-04, ROADMAP SC-3)"
    - "Rating outcomes are seed/grid deterministic — no wall-clock timers in the rater path (AGENTS.md, RESEARCH)"
  artifacts:
    - path: lib/puzzles/sudoku/candidates.ts
      provides: "Bitmask pencil-mark grid init/place/eliminate"
      exports: ["initCandidates", "placeDigit", "CandidateGrid"]
    - path: lib/puzzles/sudoku/techniqueIds.ts
      provides: "SudokuTechnique union + peakToTier → DifficultyTier"
      exports: ["SudokuTechnique", "peakToTier", "TECHNIQUE_ORDER"]
    - path: lib/puzzles/sudoku/rater.ts
      provides: "rateSudoku / rateSudokuPeak SE-style restart-from-easiest loop"
      exports: ["rateSudoku", "RateSudokuResult"]
    - path: __tests__/lib/puzzles/sudoku/fixtures/techniqueBoards.ts
      provides: "≥1 labeled board per DifficultyTier with expected peak"
    - path: __tests__/lib/puzzles/sudoku/rater.test.ts
      provides: "Fixture peak→tier asserts + budget/incomplete cases"
  key_links:
    - from: "peakToTier"
      to: "DifficultyTier from lib/puzzles/difficulty/tiers.ts"
      via: "import type DifficultyTier; map singles→easy … xy_wing/short_chain→expert"
      pattern: "from ['\\\"].*difficulty/tiers"
    - from: "rateSudoku"
      to: "technique detectors + candidates"
      via: "scan TECHNIQUE_ORDER; apply first hit; track peak; restart from easiest"
    - from: "chains.ts"
      to: "EXPERT_CHAIN_MAX_LENGTH / EXPERT_CHAIN_MAX_NODES"
      via: "bounded short_chain expansions; exceed → signal budget to rater"
---

<objective>
Land the Sudoku human-technique rater: candidate bitmasks, ordered technique ladder, peak→DifficultyTier mapping, and labeled fixtures — consuming the shared tier enum (D-01..D-04, D-07..D-09, TIER-01, TIER-02 rater half).

Purpose: Make peak technique measurable so generate-for-tier (plan 02) can accept/reject boards without false Easy from backtracking.
Output: `candidates.ts`, `techniqueIds.ts`, `techniques.ts`, `chains.ts`, `rater.ts`, fixture boards + `rater.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-CONTEXT.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-RESEARCH.md
@.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-PATTERNS.md
@lib/puzzles/difficulty/tiers.ts
@lib/puzzles/sudoku/solver.ts
@lib/puzzles/sudoku/grid.ts
@lib/puzzles/sudoku/validate.ts
@__tests__/lib/puzzles/sudoku/validate.test.ts
@__tests__/lib/puzzles/difficulty/tiers.test.ts
</context>

## Discretion locks (Claude's Discretion → executor MUST follow)

| Topic | Lock |
|-------|------|
| File split | `candidates.ts` + `techniqueIds.ts` + single `techniques.ts` (singles→xy_wing) + `chains.ts` (short_chain only) + `rater.ts` — do not create `techniques/` directory |
| Technique union | Exact ids from RESEARCH: `full_house`, `naked_single`, `hidden_single`, `pointing`, `claiming`, `naked_pair`, `hidden_pair`, `naked_triple`, `hidden_triple`, `x_wing`, `swordfish`, `xy_wing`, `short_chain` |
| peakToTier | singles/full_house→easy; pointing/claiming/pairs→medium; triples/x_wing/swordfish→hard; xy_wing/short_chain→expert (D-02, A4) |
| Candidate model | `Uint16Array` length 81; bits 1..9; `cloneGrid` for digit board; never mutate caller givens |
| Rating loop | SE-style: scan ascending order; apply first productive step; `peak = max(peak, tech)`; restart from easiest until solved / stuck / step budget |
| Expert budgets | `EXPERT_CHAIN_MAX_LENGTH=6`, `EXPERT_CHAIN_MAX_NODES=2000`, `EXPERT_MAX_TECHNIQUE_STEPS=500` as named consts in `rater.ts` or `chains.ts` |
| Rate result | Discriminated union: `{status:'solved', peak, tier}` \| `{status:'incomplete', peak}` \| `{status:'budget_exhausted', peak}` — per RESEARCH |
| Uniqueness | Optional precheck OK; if used, `countSolutionsUpTo(givens,2)===1` only — do not place via `solve()` during rating |
| Packages | No new npm sudoku libs (D-09) |
| Out of scope | No generator/for-tier yet; no daily type-selector edits; no UI; no other-type raters |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Technique contracts + candidates + Easy–Hard detectors</name>
  <files>lib/puzzles/sudoku/techniqueIds.ts, lib/puzzles/sudoku/candidates.ts, lib/puzzles/sudoku/techniques.ts, __tests__/lib/puzzles/sudoku/fixtures/techniqueBoards.ts, __tests__/lib/puzzles/sudoku/rater.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-CONTEXT.md` (D-01, D-02, D-09)
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-RESEARCH.md` (Pattern 1 peakToTier, candidates sketch, pitfalls)
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-PATTERNS.md` (clone-before-mutate, house walks)
    - `lib/puzzles/difficulty/tiers.ts`
    - `lib/puzzles/sudoku/grid.ts`
    - `lib/puzzles/sudoku/validate.ts`
    - `__tests__/lib/puzzles/sudoku/validate.test.ts`
  </read_first>
  <behavior>
    - peakToTier maps each SudokuTechnique to DifficultyTier exactly per Discretion locks table
    - initCandidates from a singles-only Easy fixture yields a grid that can be emptied by singles/full_house only
    - Fixture module exports ≥1 board each for easy/medium/hard with `{givens, expectedTier, expectedPeak}` (Expert board may be stubbed until Task 2)
    - rater.test.ts RED: Easy/Medium/Hard fixtures assert expectedTier (fail until rateSudoku exists — write failing expects first)
  </behavior>
  <action>
    Per D-01/D-02/D-09: create `techniqueIds.ts` exporting `SudokuTechnique`, ordered `TECHNIQUE_ORDER` (easy→expert), and `peakToTier(peak): DifficultyTier` importing `DifficultyTier` from `../difficulty/tiers` — never fork a parallel tier union.
    Create `candidates.ts` with bitmask `CandidateGrid`, `initCandidates(givens)`, `placeDigit`, eliminate from row/col/box peers (mirror validate house math). Clone caller data; surgical only.
    Create `techniques.ts` exporting detectors that mutate a shared candidates+digit working state and return whether a step applied + which technique id. Implement through `xy_wing` (include pointing/claiming as separate ids). Fixed scan order: houses 0..26, digits 1..9, cells 0..80.
    Add `__tests__/lib/puzzles/sudoku/fixtures/techniqueBoards.ts` with hand-authored Easy/Medium boards and at least one Hard board (capture later OK if labeled). Start `rater.test.ts` with table cases for those fixtures (TDD RED). Avoid UI, selector modules, and npm solver packages.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/sudoku/rater.test.ts __tests__/lib/puzzles/difficulty/tiers.test.ts --no-coverage 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `techniqueIds.ts` contains `peakToTier` and imports from `difficulty/tiers`
    - `candidates.ts` exports `initCandidates`
    - `techniques.ts` exists and references `naked_single` and `x_wing` as technique ids
    - `techniqueBoards.ts` exports fixtures covering expectedTier values easy, medium, and hard
  </acceptance_criteria>
  <done>Contracts + candidates + Easy–Hard technique detectors exist; fixture file present; rater tests written (may still fail on missing rateSudoku until Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded chains + rateSudoku peak loop + fixture green</name>
  <files>lib/puzzles/sudoku/chains.ts, lib/puzzles/sudoku/rater.ts, __tests__/lib/puzzles/sudoku/fixtures/techniqueBoards.ts, __tests__/lib/puzzles/sudoku/rater.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-RESEARCH.md` (RateSudokuResult, Expert budgets, pitfall 2/3)
    - `lib/puzzles/sudoku/techniqueIds.ts`
    - `lib/puzzles/sudoku/candidates.ts`
    - `lib/puzzles/sudoku/techniques.ts`
    - `lib/puzzles/sudoku/solver.ts` (for what NOT to call during placement)
  </read_first>
  <behavior>
    - rateSudoku(easyFixture).status === 'solved' && .tier === 'easy'
    - rateSudoku(mediumFixture).tier === 'medium'; same for hard/expert fixtures
    - When chain node/length budget trips before solve → status budget_exhausted (or incomplete), never infinite loop
    - Same givens → identical RateSudokuResult across two calls
    - rateSudoku never imports/calls solve() for digit placement
  </behavior>
  <action>
    Per D-04: implement `chains.ts` for `short_chain` only with hard caps `EXPERT_CHAIN_MAX_LENGTH=6` and `EXPERT_CHAIN_MAX_NODES=2000`. On exceed, return a budget-exceeded signal to the rater — use step/node counters only (never wall-clock elapsed timers for accept/reject).
    Implement `rateSudoku(givens): RateSudokuResult` in `rater.ts`: init candidates; loop ≤ `EXPERT_MAX_TECHNIQUE_STEPS=500`; each iteration try techniques in `TECHNIQUE_ORDER` (including chain at Expert end); apply first success; update peak; continue until grid full → `{status:'solved', peak, tier: peakToTier(peak)}`; if no technique applies → `{status:'incomplete', peak}`; if step/node budget → `{status:'budget_exhausted', peak}`. Export `rateSudokuPeak` alias or helper returning tier on solved only if useful for plan 02.
    Freeze ≥1 Expert fixture (xy_wing or short_chain peak). Green all fixture asserts in `rater.test.ts`. Add determinism + budget cases. Do not edit `generator.ts` or wire mastery/selector in this plan.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/sudoku/rater.test.ts --no-coverage 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `rater.ts` exports `rateSudoku` and `RateSudokuResult`
    - `rater.test.ts` asserts all four expectedTier from fixtures (easy/medium/hard/expert)
    - `chains.ts` or `rater.ts` defines `EXPERT_CHAIN_MAX_NODES` (or equivalent named const)
    - Region `lib/puzzles/sudoku/rater.ts`: no wall-clock timer API calls for rating (step/node only) — `awk '/^export function rateSudoku/,/^export /' lib/puzzles/sudoku/rater.ts | grep -c 'Date\.now' | grep -q '^0$'`
    <!-- planner-discipline-allow: Date.now -->
  </acceptance_criteria>
  <done>All four tier fixtures rate correctly; Expert path budgeted; RateSudokuResult contract stable for generate-for-tier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → rateSudoku(givens) | Untrusted/malformed 9×9 grids from fixtures or future generators |
| Rater → CPU | Expert chain search can burn main-thread / Jest time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2.5-02-01-01 | Denial of Service | chains.ts / rater.ts | high | mitigate | Hard node/length/step caps; budget_exhausted discard path (D-04) |
| T-v2.5-02-01-02 | Tampering | rateSudoku input | medium | mitigate | Validate 9×9 shape + digit range 0–9 at rater entry; reject malformed |
| T-v2.5-02-01-03 | Tampering | Integrity of difficulty | medium | mitigate | Logical-only placements; fixtures lock peak→tier; no backtrack fill during rate |
| T-v2.5-02-01-04 | Information Disclosure | N/A | low | accept | Offline local puzzle lib; no network/PII |
| T-v2.5-02-SC | Tampering | npm installs | high | mitigate | No new packages this phase (D-09); legitimacy N/A |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx jest __tests__/lib/puzzles/sudoku/rater.test.ts --no-coverage` passes
- [ ] `npx tsc --noEmit` (or `npm run typecheck`) clean for new modules
- [ ] Rater imports `DifficultyTier` from `lib/puzzles/difficulty/tiers.ts` only
- [ ] No edits to daily type-selector / mastery hydrate / UI paths
</verification>

<success_criteria>
- TIER-01 satisfied for Sudoku via shared enum consumption (D-01)
- ROADMAP SC-1: fixtures classify to labeled tiers
- ROADMAP SC-3 (rater half): Expert budgets prevent hang
- Pure TS under `lib/puzzles/sudoku/`; ready for plan 02 generate-for-tier
</success_criteria>

<output>
Create `.planning/phases/v2.5-02-sudoku-technique-rater/v2.5-02-01-SUMMARY.md` when done
</output>
