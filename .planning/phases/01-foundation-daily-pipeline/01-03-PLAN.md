---
phase: 01-foundation-daily-pipeline
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - app/_layout.tsx
  - app/index.tsx
  - app/game.tsx
  - app/result.tsx
  - app/(auth)/login.tsx
  - hooks/useDailyGame.ts
  - contexts/DailyGameProvider.tsx
autonomous: false
requirements:
  - DAILY-04
  - NAV-01
  - NAV-02
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "冷启动 index 在 hydrate 完成前显示加载 UI，完成后 Redirect"
    - "status playing 进入 /game；completed 或 abandoned 进入 /result"
    - "同日重启 gameType/seed 不变；改系统日期到次日产生新记录"
    - "game 屏显示今日类型+日期+占位棋盘，不可交互"
    - "游客无需登录即可走通 index→game→result 占位流"
  artifacts:
    - "hooks/useDailyGame.ts"
    - "contexts/DailyGameProvider.tsx"
    - "app/index.tsx"
    - "app/game.tsx"
    - "app/result.tsx"
    - "app/(auth)/login.tsx"
  key_links:
    - from: "app/index.tsx"
      to: "hooks/useDailyGame.ts"
      via: "useDailyGame"
      pattern: "Redirect|status"
    - from: "hooks/useDailyGame.ts"
      to: "lib/storage/dailyStorage.ts"
      via: "loadDailySnapshot|saveDailySnapshot"
      pattern: "loadDailySnapshot"
---

<objective>
用 useDailyGame 编排每日状态，实现 index/game/result/login 路由分流与 UI-SPEC 占位界面，满足 Phase 1 成功标准。

Purpose: 完成「打开 App → 今日管道 → 结束态」可演示闭环（谜题可玩性在 Phase 2/3）。
Output: 可端到端手测的 MVP 骨架。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-foundation-daily-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-daily-pipeline/01-UI-SPEC.md
@.planning/ROADMAP.md
@lib/date/localDay.ts
@lib/puzzles/dailySelector.ts
@lib/storage/dailyStorage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DailyGameProvider + useDailyGame</name>
  <files>hooks/useDailyGame.ts, contexts/DailyGameProvider.tsx, app/_layout.tsx</files>
  <read_first>.planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md</read_first>
  <action>
    实现 `useDailyGame`（或 Context + hook）契约：

    ```ts
    type DailyStatus = 'loading' | 'ready' | 'playing' | 'completed' | 'abandoned';
  ```

    **hydrate 流程（PITFALLS #4）：**
    1. mount → `status = 'loading'`
    2. `today = getLocalDateKey()`
    3. `record = await loadDailySnapshot()`
    4. 若 `record.dateKey !== today` → `createNewDaily()`（selectDailyGame + save，status `playing`）
    5. 若 `record.dateKey === today` → 复用 record，映射 status（playing/completed/abandoned）
    6. `status` 从 loading → 目标态

    **AppState：** `background` → `active` 时重新执行 dateKey 比对；跨日则 rollover（DAILY-03）。

    暴露 actions（Phase 1 占位即可）：
    - `markCompleted()` / `markAbandoned()` → 写 storage + 更新 status（供 result 分流测试）

    `_layout.tsx` 包裹 `DailyGameProvider`；导出 `useDailyGame`。

    **路由决策（Claude's discretion，本计划锁定）：**
    - `playing` → `/game`
    - `completed` | `abandoned` → `/result`
    - `loading` → index 显示 hydrate UI，不 Redirect
  </action>
  <verify>
    <automated>grep -E "loadDailySnapshot|getLocalDateKey|AppState" /Users/moyun/code/foolish-you/hooks/useDailyGame.ts</automated>
  </verify>
  <acceptance_criteria>
    - useDailyGame.ts 含 loading 初始态与 loadDailySnapshot
    - _layout.tsx 包裹 Provider
  </acceptance_criteria>
  <done>hook 可在 index 读取 status 与 dateKey/gameType</done>
</task>

<task type="auto">
  <name>Task 2: 四屏 UI + Redirect 分流</name>
  <files>app/index.tsx, app/game.tsx, app/result.tsx, app/(auth)/login.tsx</files>
  <read_first>.planning/phases/01-foundation-daily-pipeline/01-UI-SPEC.md, DESIGN.md</read_first>
  <action>
    **index.tsx**
    - `loading`：Display「傻了么」+ caption 眉标 +「正在翻出今天的傻题…」+ ActivityIndicator
    - `playing` → `<Redirect href="/game" />`
    - `completed|abandoned` → `<Redirect href="/result" />`

    **game.tsx**（占位，01-UI-SPEC ASCII）
    - eyebrow：`今日 · {dateKey}`
    - Display：数独 / 二进制（中文标签）
    - HairlineCard 内虚线占位网格（View 边框 dashed，不可 Pressable）
    - OutlinePillButton primary「先看看题目」`disabled`
    - outline「今天不玩了」→ 调用 `markAbandoned()` 后 router 到 result（若启用）

    **result.tsx**
    - Heading「今天到此为止」+ Body「明天见。」
    - 开发用：按钮「标记完成」/「返回」可选（便于测 completed 分流）

    **(auth)/login.tsx**
    - 「登录以后再说」+ OutlinePillButton「返回今日」→ `router.replace('/')`

    文案使用 01-UI-SPEC Copywriting Contract；字重 400；间距 xl/2xl。

    创建 `app/(auth)/login.tsx` 路由组目录。
  </action>
  <verify>
    <automated>test -f /Users/moyun/code/foolish-you/app/game.tsx && test -f /Users/moyun/code/foolish-you/app/result.tsx && test -f /Users/moyun/code/foolish-you/app/\(auth\)/login.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - index 使用 Redirect 而非 imperative navigate 做初始分流
    - game 屏含 HairlineCard 与 disabled CTA
  </acceptance_criteria>
  <done>四屏可导航；分流逻辑与 hook status 一致</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Expo Go 手测 Phase 1 成功标准</name>
  <what-built>完整每日管道：hydrate、Redirect、占位 game/result、本地存储</what-built>
  <how-to-verify>
    1. `npx expo start`，Expo Go（SDK 54）打开项目
    2. 首次进入应到 /game，看到今日类型与占位棋盘
    3. 杀进程重开：同日类型与日期不变
    4. 在 result 或 game 触发 abandoned/completed，重开应到 /result
    5. 将系统日期调到次日，重开应出现新 dateKey（新类型可不同）
    6. 飞行模式重复步骤 2（离线 STOR-02）
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- ROADMAP Phase 1 成功标准 1–5 手测清单在 checkpoint 覆盖
- `npm test` 仍通过（lib 层）
</verification>

<success_criteria>
- NAV-01/02、DAILY-04、AUTH-01/02 在占位流中满足
- Phase 1 Goal 可演示
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation-daily-pipeline/01-03-SUMMARY.md`
</output>
