---
phase: 01-foundation-daily-pipeline
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - jest.config.js
  - lib/date/localDay.ts
  - lib/puzzles/types.ts
  - lib/puzzles/rng.ts
  - lib/puzzles/dailySelector.ts
  - lib/storage/dailyStorage.ts
  - constants/config.ts
  - __tests__/lib/date/localDay.test.ts
  - __tests__/lib/puzzles/rng.test.ts
  - __tests__/lib/puzzles/dailySelector.test.ts
  - __tests__/lib/storage/dailyStorage.test.ts
autonomous: true
requirements:
  - DAILY-01
  - DAILY-02
  - DAILY-03
  - DAILY-05
  - STOR-01
  - STOR-02
  - STOR-03
user_setup: []
must_haves:
  truths:
    - "getLocalDateKey() 返回设备本地 YYYY-MM-DD，不用 UTC ISO 切片"
    - "相同 dateKey 的 dailySelector 输出稳定 gameType + seed"
    - "DailySnapshot 可 JSON 序列化写入 AsyncStorage 并读回"
    - "lib/puzzles 不 import react 或 expo-router"
  artifacts:
    - "lib/date/localDay.ts"
    - "lib/puzzles/dailySelector.ts"
    - "lib/storage/dailyStorage.ts"
    - "constants/config.ts"
  key_links:
    - from: "lib/puzzles/dailySelector.ts"
      to: "lib/puzzles/rng.ts"
      via: "import"
      pattern: "mulberry32|createRng"
    - from: "lib/storage/dailyStorage.ts"
      to: "@react-native-async-storage/async-storage"
      via: "import"
      pattern: "AsyncStorage"
---

<objective>
实现每日管道纯逻辑层：本地日期键、确定性 PRNG、dailySelector（stub 谜题）、AsyncStorage 持久化，并附 Jest 单测。

Purpose: UI 层（01-03）只编排，不嵌入业务规则。
Output: 可独立测试的 `lib/` + `constants/config.ts`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-daily-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-daily-pipeline/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Jest 配置 + dateKey + PRNG</name>
  <files>package.json, jest.config.js, lib/date/localDay.ts, lib/puzzles/rng.ts, lib/puzzles/types.ts, __tests__/lib/date/localDay.test.ts, __tests__/lib/puzzles/rng.test.ts, constants/config.ts</files>
  <read_first>.planning/research/PITFALLS.md, .planning/research/ARCHITECTURE.md</read_first>
  <behavior>
    - getLocalDateKey：mock Date 为本地 2026-05-17 01:30 UTC+8 等价场景时返回 2026-05-17（非 UTC 昨日）
    - mulberry32：同 seed 连续两次调用序列前 3 项相同
    - deriveSeed(dateKey)：同 dateKey 同 APP_SALT 得相同 32-bit 整数
  </behavior>
  <action>
    添加 devDependencies：`jest`, `jest-expo`, `@types/jest`；`package.json` scripts `"test": "jest"`。
    `jest.config.js` 使用 `jest-expo` preset，`testMatch` 为 `**/__tests__/**/*.test.ts`。

    `constants/config.ts`：`APP_SALT`, `STORAGE_KEY = '@foolish-you/daily-v1'`, `STORAGE_VERSION = 1`。

    `lib/date/localDay.ts`：
    ```ts
    export function getLocalDateKey(date: Date = new Date()): string
    ```
    用 `getFullYear/getMonth/getDate` 或 `Intl.DateTimeFormat('en-CA', { timeZone: ... })`。
    **禁止** `toISOString().slice(0,10)`。

    `lib/puzzles/types.ts`：`GameType = 'sudoku' | 'binary'`；`DailyStatus`；`DailySnapshot` 含 dateKey, gameType, seed, status, puzzleStub, lastGameType?, lastPuzzleHash?。

    `lib/puzzles/rng.ts`：`hashStringToSeed(s: string): number`；`mulberry32(seed: number): () => number`；`deriveSeed(dateKey: string): number`。

    先写测试再实现。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npm test -- --testPathPattern="localDay|rng" --passWithNoTests 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - localDay.test.ts 含 UTC 边界用例
    - rng.test.ts 验证确定性
  </acceptance_criteria>
  <done>npm test 通过 date 与 rng 测试</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dailySelector stub + dailyStorage</name>
  <files>lib/puzzles/dailySelector.ts, lib/storage/dailyStorage.ts, __tests__/lib/puzzles/dailySelector.test.ts, __tests__/lib/storage/dailyStorage.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/research/ARCHITECTURE.md</read_first>
  <behavior>
    - selectDailyGame(seed, prev?): 返回 gameType ∈ {sudoku,binary}，且与 prev.lastGameType 不同（DAILY-05）
    - 同 seed 重复调用 gameType 相同
    - save/load round-trip snapshot 字段一致
    - load 无数据返回 null
  </behavior>
  <action>
    `dailySelector.ts`：
    - `selectDailyGame(params: { dateKey, seed, previous?: { gameType, puzzleHash } })` → `{ gameType, seed, puzzleStub: { kind, placeholder: true } }`
    - 用 `deriveSeed(dateKey)` 与子种子 `derive(seed,'type')` 决定类型；puzzleHash 可用 stub 常量

    `dailyStorage.ts`：
    - `loadDailySnapshot(): Promise<DailySnapshot | null>`
    - `saveDailySnapshot(snapshot: DailySnapshot): Promise<void>`
    - `clearDailySnapshot(): Promise<void>`（测试/调试用）
    - JSON.parse 失败时返回 null 并 console.warn（不崩溃）

    **lib/puzzles/** 与 **lib/storage/** 不得 import `react` / `expo-router`。

    先写测试（mock AsyncStorage：`@react-native-async-storage/async-storage/jest/async-storage-mock`）。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npm test -- --testPathPattern="dailySelector|dailyStorage" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - dailySelector.test.ts 断言 DAILY-05 不连续同类型
    - dailyStorage.test.ts 断言 round-trip
  </acceptance_criteria>
  <done>npm test 全绿（lib 相关）</done>
</task>

</tasks>

<verification>
- `npm test` 全部通过
- `grep -r "toISOString().slice" lib/` 无匹配
- `grep -r "from 'react'" lib/puzzles lib/date lib/storage` 无匹配
</verification>

<success_criteria>
- 日期/种子/存储逻辑可离线单测验证
- 满足 DAILY-01/02/03/05 与 STOR-01/02/03 的数据层能力
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation-daily-pipeline/01-02-SUMMARY.md`
</output>
