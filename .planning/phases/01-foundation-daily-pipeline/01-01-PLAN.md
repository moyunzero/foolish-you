---
phase: 01-foundation-daily-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.json
  - tsconfig.json
  - babel.config.js
  - metro.config.js
  - tailwind.config.js
  - global.css
  - app/_layout.tsx
  - app/index.tsx
  - components/ui/OutlinePillButton.tsx
  - components/ui/HairlineCard.tsx
  - constants/design.ts
autonomous: true
requirements:
  - NAV-01
user_setup: []
must_haves:
  truths:
    - "App 在 Expo Go（SDK 54）可启动，根布局为深色 canvas 背景"
    - "NativeWind className 在根布局生效"
    - "DESIGN.md 颜色 token 已映射到 tailwind.config / CSS 变量"
  artifacts:
    - "app/_layout.tsx"
    - "tailwind.config.js"
    - "global.css"
    - "components/ui/OutlinePillButton.tsx"
    - "components/ui/HairlineCard.tsx"
  key_links:
    - from: "app/_layout.tsx"
      to: "global.css"
      via: "import"
      pattern: "global.css"
    - from: "tailwind.config.js"
      to: "DESIGN.md"
      via: "theme.extend.colors"
      pattern: "canvas|hairline|accent-sunset"
---

<objective>
初始化 Expo SDK 54 + expo-router + NativeWind v4，并将 DESIGN.md 设计 token 落地为可复用的 Tailwind 主题与两个基础 UI 组件。

Purpose: 后续计划可在统一视觉系统上搭建路由与业务逻辑。
Output: 可运行的空壳 App（`index` 仅占位），设计 token 与胶囊/卡片组件就绪。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-daily-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-daily-pipeline/01-UI-SPEC.md
@DESIGN.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Expo 54 工程与 expo-router 骨架</name>
  <files>package.json, app.json, tsconfig.json, babel.config.js, metro.config.js, app/_layout.tsx, app/index.tsx</files>
  <read_first>.planning/phases/01-foundation-daily-pipeline/01-CONTEXT.md, .planning/research/STACK.md</read_first>
  <action>
    在仓库根目录用 `npx create-expo-app@latest .` 或等价方式初始化（若已有空目录则 `create-expo-app` 到当前目录）。**per D-02** 固定 Expo SDK 54：运行 `npx expo install expo@~54.0.0` 后 `npx expo install --fix`。

    安装路由与基础模块（全部用 `npx expo install`）：`expo-router`, `expo-constants`, `expo-linking`, `expo-splash-screen`, `expo-status-bar`, `react-native-safe-area-context`, `react-native-screens`, `react-native-gesture-handler`。

    配置 `package.json` 的 `"main": "expo-router/entry"`；`app.json` 设置 `scheme`、`slug`（如 `foolish-you`）、`name` 显示为「傻了么」。

    创建 `app/_layout.tsx`：`Stack` 屏、`StatusBar style="light"`、`bg-canvas` 占位。
    创建 `app/index.tsx`：临时居中 Text「傻了么」+ ActivityIndicator（完整分流在 01-03）。

    **禁止：** 使用 SDK 55 或 Dev Client 作为默认（D-01 Expo Go）。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npx expo export --platform ios --output-dir /tmp/foolish-you-export-check 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json 含 expo-router entry 与 SDK 54 范围依赖
    - app/_layout.tsx 与 app/index.tsx 存在
  </acceptance_criteria>
  <done>工程可编译；`npx expo start` 能在 Expo Go 加载 index 占位屏</done>
</task>

<task type="auto">
  <name>Task 2: NativeWind v4 + DESIGN.md token 映射</name>
  <files>tailwind.config.js, global.css, babel.config.js, metro.config.js, constants/design.ts</files>
  <read_first>DESIGN.md, .planning/phases/01-foundation-daily-pipeline/01-UI-SPEC.md</read_first>
  <action>
    按 NativeWind v4 文档安装：`nativewind`, `tailwindcss@^3.4.17`, `react-native-css-interop`（版本以官方兼容表为准）。

    在 `tailwind.config.js` 的 `theme.extend.colors` 映射 DESIGN.md：
    `canvas` #0a0a0a, `canvas-card` #191919, `canvas-soft` #1a1c20, `hairline` #212327,
    `ink` #ffffff, `body` #dadbdf, `muted` #7d8187, `accent-sunset` #ff7a17,
    `primary` #ffffff, `on-primary` #0a0a0a.

    `global.css` 写入 `@tailwind` 三行 + 上述 CSS 变量（与 01-UI-SPEC 一致）。
    `babel.config.js` 加入 `nativewind/babel` preset。
    `constants/design.ts` 导出颜色/间距常量供非 className 场景（如 ActivityIndicator）。

    根布局 `import "../global.css"`。
  </action>
  <verify>
    <automated>grep -E "canvas|hairline|accent-sunset" /Users/moyun/code/foolish-you/tailwind.config.js</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js 包含 canvas 与 hairline 色名
    - global.css 被 app/_layout.tsx 引用
  </acceptance_criteria>
  <done>index 屏背景为 #0a0a0a，文字为白色系</done>
</task>

<task type="auto">
  <name>Task 3: 字体与基础 UI 组件</name>
  <files>app/_layout.tsx, components/ui/OutlinePillButton.tsx, components/ui/HairlineCard.tsx</files>
  <read_first>.planning/phases/01-foundation-daily-pipeline/01-UI-SPEC.md</read_first>
  <action>
    安装 `@expo-google-fonts/inter`, `@expo-google-fonts/space-mono`, `expo-font`；在 `_layout.tsx` 用 `useFonts` 加载，未完成前返回 null 或同色闪屏（01-UI-SPEC）。

    实现 `OutlinePillButton`：`Pressable` + `rounded-full` + `border border-hairline` + `px-6 py-3` + `text-ink` + `min-h-[44px]`；支持 `variant="primary"` 白底黑字（`bg-primary text-on-primary`）；`disabled` 时 `opacity-50`。

    实现 `HairlineCard`：`View` + `bg-canvas-card border border-hairline rounded-lg p-6`。

  </action>
  <verify>
    <automated>test -f /Users/moyun/code/foolish-you/components/ui/OutlinePillButton.tsx && test -f /Users/moyun/code/foolish-you/components/ui/HairlineCard.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - OutlinePillButton 导出 default 且含 primary/outline 分支
    - HairlineCard 使用 canvas-card 与 hairline 边框
  </acceptance_criteria>
  <done>字体加载后 index 使用 Inter；组件可在后续屏复用</done>
</task>

</tasks>

<verification>
- `npx expo start` 启动无红屏
- index 背景 canvas、文字 ink
- grep 确认 tailwind 含 design token
</verification>

<success_criteria>
- Expo SDK 54 + Expo Go 开发路径就绪（D-01, D-02）
- DESIGN.md token 已进入 Tailwind
- 基础胶囊/卡片组件可导入
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation-daily-pipeline/01-01-SUMMARY.md`
</output>
