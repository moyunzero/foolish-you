---
phase: v2.5-06-ship-2-5-0
plan: 02
type: execute
wave: 2
depends_on:
  - v2.5-06-01
files_modified:
  - docs/TESTING.md
  - docs/qa/v2.5-ship/README.md
  - docs/qa/v2.5-ship/SHIP-02-COVERAGE.md
  - docs/qa/v2.5-ship/evidence/
  - .maestro/flows/v25/v25-smoke-sudoku.yaml
  - .maestro/flows/v25/v25-smoke-binary.yaml
  - .maestro/flows/v25/v25-smoke-nonogram.yaml
  - .maestro/flows/v25/v25-smoke-slitherlink.yaml
  - scripts/maestro-v25-ship.sh
  - __tests__/lib/daily/shipHydrateCompletePath.test.ts
  - .planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md
autonomous: false
requirements:
  - SHIP-02
user_setup: []
must_haves:
  truths:
    - "docs/qa/v2.5-ship/SHIP-02-COVERAGE.md (or equivalent) maps every SHIP-02 REQUIREMENTS bullet to concrete Jest paths; D-10 forbids a new frozen golden JSON pack (D-08, D-10)"
    - "Ship-level integration test hydrate create (mastery + avoid) → complete → appendPlayedHash is green in __tests__/lib/daily/shipHydrateCompletePath.test.ts (D-09)"
    - "docs/qa/v2.5-ship/ exists with README preconditions, Maestro commands, and evidence layout (D-11)"
    - "Four Maestro smoke flows under .maestro/flows/v25/ enter game for sudoku/binary/nonogram/slitherlink via Dev force buttons (D-11)"
    - "Evidence directory contains maestro log with MAESTRO_EXIT=0 plus at least one screenshot per type (D-11)"
    - "docs/TESTING.md has a v2.5 ship hand-QA section covering four types, in-progress reopen keep, and fresh-date create — not required Mon/Sun (D-12)"
    - "AGENTS four-command CI green and recorded in VERIFICATION (SHIP-02 / ROADMAP SC-2)"
    - "VERIFICATION.md checklist covers SHIP-01/SHIP-02 and D-01..D-14 with human signature after Maestro + hand QA (D-13)"
    - "Phase complete means repo ready — EAS production / ASC submit explicitly out of scope (D-13)"
    - "No product UI / APP_SALT / selector / DailyGameContext edits in this plan"
  artifacts:
    - path: docs/qa/v2.5-ship/README.md
      provides: "Hand QA + Maestro preconditions and evidence instructions"
    - path: docs/qa/v2.5-ship/SHIP-02-COVERAGE.md
      provides: "SHIP-02 bullet → test coverage matrix (D-08)"
    - path: __tests__/lib/daily/shipHydrateCompletePath.test.ts
      provides: "D-09 hydrate→complete→appendPlayedHash integration"
    - path: scripts/maestro-v25-ship.sh
      provides: "One-process-per-flow Maestro runner for four smokes"
    - path: .maestro/flows/v25/v25-smoke-sudoku.yaml
      provides: "Sudoku enter-game smoke"
    - path: .maestro/flows/v25/v25-smoke-binary.yaml
      provides: "Binary enter-game smoke"
    - path: .maestro/flows/v25/v25-smoke-nonogram.yaml
      provides: "Nonogram enter-game smoke"
    - path: .maestro/flows/v25/v25-smoke-slitherlink.yaml
      provides: "Slitherlink enter-game smoke"
    - path: docs/TESTING.md
      provides: "v2.5 ship manual QA section linking qa folder + VERIFICATION"
    - path: .planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md
      provides: "Sign-off matrix + CI results + human signature"
  key_links:
    - from: "SHIP-02-COVERAGE.md"
      to: "existing mastery/rater/freeze/avoid/nudge suites + shipHydrateCompletePath"
      via: "D-08 matrix rows; D-09 fills hydrate→append gap"
    - from: "scripts/maestro-v25-ship.sh"
      to: ".maestro/flows/v25/*-smoke-*.yaml"
      via: "FLOWS array + maestro test --test-output-dir evidence"
    - from: "docs/TESTING.md § v2.5 ship"
      to: "docs/qa/v2.5-ship/README.md + VERIFICATION.md"
      via: "cross-links for hand QA vs Maestro"
    - from: "buildNewDailySnapshot + persistStatus complete side effects"
      to: "shipHydrateCompletePath.test.ts"
      via: "lib-level mirror; do not mount DailyGameContext"
---

<objective>
Prove ship readiness: SHIP-02 coverage matrix, D-09 hydrate→complete integration, Maestro four-type smoke evidence, hand-QA checklist (four types / keep / fresh-date create), AGENTS CI green, and VERIFICATION human sign-off (D-08..D-13) — without EAS production or ASC submit (D-13).

Purpose: Close ROADMAP success criteria 2–3 and lock repo-ready confidence after Plan 01 version/disclosure.
Output: Coverage matrix + D-09 test + QA folder + four smokes + script + TESTING section + signed VERIFICATION.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-CONTEXT.md
@.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-RESEARCH.md
@.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-PATTERNS.md
@.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-01-SUMMARY.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-02-PLAN.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md
@docs/qa/v2.4.2-ship/README.md
@docs/TESTING.md
@scripts/maestro-v242-ship.sh
@.maestro/flows/v242/v242-smoke-sudoku.yaml
@__tests__/lib/storage/playedHashCompletePath.test.ts
@__tests__/lib/mastery/freezeMastery.test.ts
@lib/daily/dailyHydrate.ts
@AGENTS.md
</context>

## Artifacts this phase produces

| Symbol / artifact | File | Notes |
|-------------------|------|-------|
| SHIP-02 coverage matrix | `docs/qa/v2.5-ship/SHIP-02-COVERAGE.md` | Maps REQUIREMENTS bullets → suites (D-08) |
| `shipHydrateCompletePath` test | `__tests__/lib/daily/shipHydrateCompletePath.test.ts` | D-09 integration |
| QA README + evidence | `docs/qa/v2.5-ship/` | Required Maestro evidence under `evidence/YYYYMMDD/` |
| Four smoke flows | `.maestro/flows/v25/v25-smoke-*.yaml` | One enter-game smoke per type |
| Ship Maestro script | `scripts/maestro-v25-ship.sh` | Mirror v242 one-process-per-flow |
| Hand QA section | `docs/TESTING.md` | § v2.5 ship |
| Sign-off matrix | `v2.5-06-VERIFICATION.md` | CI + Maestro + hand QA + signature |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SHIP-02 matrix + D-09 integration + QA/Maestro scaffold (D-08..D-12)</name>
  <files>docs/qa/v2.5-ship/SHIP-02-COVERAGE.md, docs/qa/v2.5-ship/README.md, __tests__/lib/daily/shipHydrateCompletePath.test.ts, docs/TESTING.md, .maestro/flows/v25/v25-smoke-sudoku.yaml, .maestro/flows/v25/v25-smoke-binary.yaml, .maestro/flows/v25/v25-smoke-nonogram.yaml, .maestro/flows/v25/v25-smoke-slitherlink.yaml, scripts/maestro-v25-ship.sh, .planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-CONTEXT.md` (D-08..D-13)
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-PATTERNS.md` (SHIP-02 matrix seed, D-09 compose pattern, Maestro v25, VERIFICATION sections, TESTING § pattern)
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-RESEARCH.md` (Pattern 3–6, Pitfall 4–7)
    - `.planning/REQUIREMENTS.md` SHIP-02 bullet list
    - `docs/qa/v2.4.2-ship/README.md` (folder structure analog)
    - `docs/TESTING.md` (§ v2.4.2 insertion point)
    - `scripts/maestro-v242-ship.sh` + `.maestro/flows/v242/v242-smoke-*.yaml`
    - `__tests__/lib/storage/playedHashCompletePath.test.ts`
    - `__tests__/lib/mastery/freezeMastery.test.ts` (mocks + AsyncStorage clear)
    - `lib/daily/dailyHydrate.ts` `buildNewDailySnapshot`
    - `contexts/DailyGameContext.tsx` completed-branch mastery then appendPlayedHash order (mirror only — do not edit)
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md` (matrix template)
    - Plan 01 SUMMARY if present (confirm version/What's New landed)
  </read_first>
  <behavior>
    - D-09: after seeding mastery + avoid ring, `buildNewDailySnapshot` for a fresh dateKey with forceGameType returns `status: 'playing'` and a non-empty `puzzleHash`
    - D-09: mirroring complete side effects (`applyMasteryOutcome` + `saveMasteryState` then `appendPlayedHash`) leaves `lastOutcome === 'completed'` and ring last entry equal to that `puzzleHash`
    - D-09 does not mount React / DailyGameContext; `playedHashCompletePath.test.ts` stays unchanged
  </behavior>
  <action>
    1. Per D-08: create `docs/qa/v2.5-ship/SHIP-02-COVERAGE.md` mapping each REQUIREMENTS SHIP-02 bullet (mastery update math; rater tier fixtures for four types; selector freeze; hash-avoid; weekday nudge bounds) plus related ship-safety rows (in-progress keep via `snapshotPrep.ship02`; complete→append; hydrate→complete) to concrete Jest file paths from RESEARCH/PATTERNS seed table. Mark existing suites Covered; mark hydrate create→complete→append as Covered only via the new D-09 file. Explicitly state D-10: no new frozen ship golden JSON pack for raters — rely on existing rater/`generateForTier` tests.
    2. Per D-09 (TDD): add `__tests__/lib/daily/shipHydrateCompletePath.test.ts`. Write failing test first, then implement using PATTERNS compose: mock `constants/dev` + `runAfterInteractions` like freezeMastery; clear AsyncStorage; seed non-default mastery and a played-hash avoid entry for the forced type; call `buildNewDailySnapshot({ today, previous: null, forceGameType })`; assert playing + puzzleHash; mirror DailyGameContext complete order with `applyMasteryOutcome`/`saveMasteryState` then `appendPlayedHash`; assert mastery lastOutcome completed and ring ends with puzzleHash. Do not edit `contexts/DailyGameContext.tsx`. Keep `playedHashCompletePath.test.ts` unchanged. Optional second-create avoid assert is discretionary.
    3. Create `docs/qa/v2.5-ship/README.md` mirroring v2.4.2-ship QA README: links to CONTEXT, TESTING § v2.5 ship, SHIP-02-COVERAGE, and `v2.5-06-VERIFICATION.md`; environment table (`__DEV__` build, Metro 8081, Simulator, optional `MAESTRO_UDID`); hand-QA shortest path with D-12 rows only — four types ≥1 day, in-progress reopen keep (play cells → kill/relaunch → same board; never Dev force-type mid-check), fresh date can create after update; Maestro section marking four smokes required (D-11); evidence path `docs/qa/v2.5-ship/evidence/YYYYMMDD/` requiring log with `MAESTRO_EXIT=0` plus ≥1 screenshot per type. Do not require Mon/Sun as hand-QA gates (Pitfall 6).
    4. Copy/adapt four YAML flows under `.maestro/flows/v25/`: `v25-smoke-sudoku.yaml`, `v25-smoke-binary.yaml`, `v25-smoke-nonogram.yaml`, `v25-smoke-slitherlink.yaml` from v242 smokes. Each: `appId: com.moyunzero.foolish-you`, launchApp clearState, wait for `完成今日`, runFlow `../v22/subflows/dev-open-panel.yaml`, tapOn matching Dev force label below `DEV 调试`, wait, optional rules dismiss + hide-bar, assertVisible matching `gameTypes` title, takeScreenshot named per type.
    5. Add executable `scripts/maestro-v25-ship.sh` adapted from `maestro-v242-ship.sh`: default EVIDENCE under `docs/qa/v2.5-ship/evidence/$(date +%Y%m%d)`, FLOWS = the four v25 smoke basenames, `maestro test` path `.maestro/flows/v25/${flow}.yaml`, Metro :8081 preflight, terminate app between flows, `MAESTRO_UDID` env override, footer `MAESTRO_EXIT=0` or `1`.
    6. Append a **v2.5 ship** section to `docs/TESTING.md` (keep existing v2.4.2 / v2.5-01 sections) with D-12 hand-QA table and links to `docs/qa/v2.5-ship/README.md`, `SHIP-02-COVERAGE.md`, `./scripts/maestro-v25-ship.sh`, and phase VERIFICATION.
    7. Create `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md` with: success/decision checklist for SHIP-01, SHIP-02, and D-01..D-14; CI gate table for four AGENTS commands (empty until Task 2); invariant greps including `APP_SALT` value `foolish-you-v1`, marketing `2.5.0`, buildNumber `30` / versionCode `3`, no dual pending `2.4.2`, confirmation this phase did not edit product UI/selector; SHIP-02 matrix + D-09 row (fail if only `playedHashCompletePath` exists); separate Maestro matrix (required) vs hand-QA matrix (four types, keep, fresh-date create — not Mon/Sun required); note EAS/ASC out of phase (D-13); human signature block left unsigned until checkpoint.
    8. Do not modify `app/`, `components/`, or `locales/` product UI. Frontend-code-review skill does not apply unless those paths are touched (they must not be).
  </action>
  <acceptance_criteria>
    - `test -f docs/qa/v2.5-ship/SHIP-02-COVERAGE.md && test -f docs/qa/v2.5-ship/README.md`
    - `rg -n 'mastery|rater|freeze|avoid|nudge|shipHydrateCompletePath|D-09|D-10' docs/qa/v2.5-ship/SHIP-02-COVERAGE.md` matches
    - `test -f __tests__/lib/daily/shipHydrateCompletePath.test.ts`
    - `npx jest __tests__/lib/daily/shipHydrateCompletePath.test.ts --runInBand` exits 0
    - `test -f .maestro/flows/v25/v25-smoke-sudoku.yaml && test -f .maestro/flows/v25/v25-smoke-binary.yaml && test -f .maestro/flows/v25/v25-smoke-nonogram.yaml && test -f .maestro/flows/v25/v25-smoke-slitherlink.yaml`
    - `test -f scripts/maestro-v25-ship.sh`
    - `rg -n 'v2\.5 ship|docs/qa/v2\.5-ship|maestro-v25-ship|fresh' docs/TESTING.md` matches
    - `rg -n 'SHIP-01|SHIP-02|D-09|D-11|D-12|D-13|APP_SALT' .planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md` matches
    - Each smoke YAML contains `takeScreenshot` and a Dev force tap for its type (`rg -n '数独|二进制|数绘|数回' .maestro/flows/v25/` covers all four labels)
    - No new frozen golden JSON pack artifact under `docs/qa/v2.5-ship/` for raters (D-10)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/qa/v2.5-ship/SHIP-02-COVERAGE.md && test -f __tests__/lib/daily/shipHydrateCompletePath.test.ts && npx jest __tests__/lib/daily/shipHydrateCompletePath.test.ts --runInBand && test -f scripts/maestro-v25-ship.sh && test -f .maestro/flows/v25/v25-smoke-sudoku.yaml && rg -n 'SHIP-02|D-09' .planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md</automated>
  </verify>
  <done>
    Coverage matrix published; D-09 integration green; QA docs, four v25 smokes, runner script, and VERIFICATION scaffold exist with D-12 hand-QA shape (D-08..D-12).
  </done>
</task>

<task type="auto">
  <name>Task 2: AGENTS CI four-command + invariant greps into VERIFICATION</name>
  <files>.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md</files>
  <read_first>
    - `AGENTS.md` Verify before done section
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md` (scaffold from Task 1)
    - `constants/config.ts` (`APP_SALT` baseline)
    - Plan 01 + Plan 02 file lists / git diff to confirm no product UI / salt / selector paths changed
  </read_first>
  <action>
    1. Per SHIP-02 / ROADMAP SC-2: run `npm run typecheck && npm test && npm run test:migration && npm run lint` to completion; record each command pass/fail and timestamp in the VERIFICATION CI table.
    2. Record invariant greps in VERIFICATION: `APP_SALT` still equals `foolish-you-v1`; `app.json` marketing version is `2.5.0` with buildNumber `30` and versionCode `3`; CONFIGURATION What's New disclosure lines present (cross-check SHIP-01); no standalone pending `2.4.2` What's New heading; this phase's diff does not modify `app/`, `components/`, `locales/`, `lib/puzzles/dailySelector.ts`, `contexts/DailyGameContext.tsx`, or salt; D-09 file exists and is not satisfied solely by `playedHashCompletePath`.
    3. Do not install new npm packages. Do not run EAS production or ASC submit.
    4. Leave Maestro evidence rows and human signature unchecked — those belong to the blocking checkpoint.
  </action>
  <acceptance_criteria>
    - `npm run typecheck && npm test && npm run test:migration && npm run lint` exits 0
    - VERIFICATION CI table marks all four commands passed
    - `rg -n "APP_SALT.*=.*['\"]foolish-you-v1['\"]" constants/config.ts` matches
    - `rg -n 'typecheck|test:migration|npm run lint|shipHydrateCompletePath' .planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md` shows recorded CI results and D-09 citation
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test && npm run test:migration && npm run lint</automated>
  </verify>
  <done>
    Full AGENTS CI green and recorded; salt/version/D-09 invariants documented in VERIFICATION.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maestro evidence + hand QA sign-off (D-11..D-13)</name>
  <files>.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md, docs/qa/v2.5-ship/evidence/</files>
  <read_first>
    - `docs/qa/v2.5-ship/README.md`
    - `docs/TESTING.md` § v2.5 ship
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md` Maestro + hand-QA matrices
    - `docs/CONFIGURATION.md` 2.5.0 What's New (confirm disclosure before store paste later)
  </read_first>
  <action>
    Pause for human local sign-off. Do not change product code in this task unless defects are reported. After approval, check off Maestro and hand-QA rows and add the human signature in `v2.5-06-VERIFICATION.md`. Phase complete = repo ready only (D-13) — do not require EAS production or Submit for Review here.
  </action>
  <what-built>
    Marketing version `2.5.0`, bilingual What's New with Content Depth → Adaptive → board-drift disclosure, SHIP-02 coverage matrix, D-09 hydrate→complete integration, four Maestro smoke flows + `maestro-v25-ship.sh`, TESTING § v2.5 ship, and VERIFICATION scaffold with CI already recorded green.
  </what-built>
  <how-to-verify>
    1. Start Metro (`npx expo start` or run:ios Dev Client) so port 8081 listens; boot the target Simulator (set `MAESTRO_UDID` if not using the script default).
    2. Run `./scripts/maestro-v25-ship.sh` and confirm evidence under `docs/qa/v2.5-ship/evidence/YYYYMMDD/` includes a log ending with `MAESTRO_EXIT=0` and at least one screenshot per game type (D-11).
    3. Hand QA — four types: using Dev force, open each of 数独 / 二进制 / 数绘 / 数回 for at least one playable day and confirm the board is usable (D-12).
    4. Hand QA — in-progress keep: on a normal day, fill a few cells, background or kill the app, relaunch, confirm same board and progress. Do not use Dev force-type mid-check (it clears today's archive). Unit `snapshotPrep.ship02` already covers stale-hash keep; this is the product reopen check.
    5. Hand QA — fresh-date create: after the update narrative, open or force a date not yet played and confirm a puzzle creates successfully (D-12).
    6. Spot-check CONFIGURATION 2.5.0 What's New still has Content Depth → Adaptive → unplayed-date disclosure + in-progress keep (SHIP-01 / D-04 / D-05).
    7. Check off Maestro + hand-QA rows in `v2.5-06-VERIFICATION.md` and sign the human signature block (D-13).
  </how-to-verify>
  <resume-signal>Type "approved" after signing the VERIFICATION matrices, or list defects found</resume-signal>
  <acceptance_criteria>
    - Human reply includes approval or an explicit defect list
    - On approval: Maestro and hand-QA rows checked in `v2.5-06-VERIFICATION.md` and signature present
    - Evidence dir exists with `MAESTRO_EXIT=0` log (`rg -n 'MAESTRO_EXIT=0' docs/qa/v2.5-ship/evidence/` matches after run)
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Maestro|手测|SHIP-02|D-11|D-12|signature|签字' .planning/phases/v2.5-06-ship-2-5-0/v2.5-06-VERIFICATION.md</automated>
  </verify>
  <done>
    Human confirms Maestro four-type evidence + four-type/keep/fresh-date hand QA; VERIFICATION signed; repo ready for post-phase EAS/ASC (D-11..D-13).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Dev Client + Maestro | `__DEV__` force tools can clear today's archive; production builds must not expose Dev panel |
| QA evidence directory | Screenshots/logs only — must not archive credentials or EAS tokens |
| VERIFICATION human signature | Phase completion claim without device proof is repudiation risk |
| Jest hydrate/complete path | Test must mirror production side-effect order without weakening storage safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v25-06-06 | Tampering | `APP_SALT` / selector during ship | high | mitigate | Task 2 invariant greps; fail phase if salt/selector/UI/context paths changed |
| T-v25-06-07 | Elevation of Privilege | DevTools shipping to release | medium | mitigate | Smokes use existing `__DEV__` panel only; do not weaken Dev tools gates |
| T-v25-06-08 | Information Disclosure | `docs/qa/v2.5-ship/evidence/` | low | mitigate | Evidence = Maestro logs + screenshots only; no secrets in README |
| T-v25-06-09 | Repudiation | VERIFICATION sign-off / SHIP-02 proof | medium | mitigate | Blocking human checkpoint; coverage matrix + D-09 required; signature required (D-13) |
| T-v25-06-10 | Denial of Service | Maestro flake / wrong UDID | low | accept | One-process-per-flow script + Metro preflight + `MAESTRO_UDID` override documented |
| T-v25-06-SC | Tampering | npm installs | high | accept | No new packages; CI uses existing scripts only |
</threat_model>

<verification>
npm run typecheck && npm test && npm run test:migration && npm run lint
npx jest __tests__/lib/daily/shipHydrateCompletePath.test.ts --runInBand
Human: ./scripts/maestro-v25-ship.sh + hand QA per docs/qa/v2.5-ship/README.md
</verification>

<success_criteria>
- SHIP-02 coverage matrix published; D-09 hydrate→complete green (D-08, D-09, D-10)
- Maestro evidence MAESTRO_EXIT=0 for four types (D-11)
- Hand QA signed: four types, in-progress reopen keep, fresh-date create (D-12)
- AGENTS CI four-command green in VERIFICATION
- Phase complete without requiring EAS/ASC (D-13)
</success_criteria>

<output>
Create `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-02-SUMMARY.md` when done
</output>
