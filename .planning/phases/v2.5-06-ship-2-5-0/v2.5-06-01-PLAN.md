---
phase: v2.5-06-ship-2-5-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - package.json
  - package-lock.json
  - README.md
  - README.zh-CN.md
  - docs/CONFIGURATION.md
  - docs/GETTING-STARTED.md
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - SHIP-01
user_setup: []
must_haves:
  truths:
    - "Marketing version string is 2.5.0 on app.json expo.version, package.json version, and package-lock top/packages empty-key version (SHIP-01, D-01, D-02)"
    - "ios.buildNumber stays 30 and android.versionCode stays 3 in app.json; eas.json untouched (D-03)"
    - "Store narrative is Live 2.4.1 · shipping 2.5.0 with no standalone shipping/pending 2.4.2 as next store release (D-01, D-14)"
    - "CONFIGURATION version table and App Store status row match 2.5.0 / Live 2.4.1 · shipping 2.5.0; buildNumber prose current 30 without editing build fields (D-02, D-03)"
    - "New 2.5.0 What's New zh+en blocks follow Content Depth (2–3) → Adaptive (2–3) → standalone board-drift bullet (SHIP-01, D-04, D-07)"
    - "Board-drift bullet states unplayed/not-yet-opened dates may change after update and in-progress progress is kept (D-05)"
    - "What's New store copy omits gallery reconstruction and mastery-default rebuild nuances; omits in-app difficulty badges (D-06)"
    - "Standalone 2.4.2 pending What's New paste block retired; Content Depth may be noted as shipping with 2.5.0; do not invent 已上架 2.4.2 (D-14)"
    - "No app/, components/, locales/, APP_SALT, dailySelector, or generator edits (phase boundary)"
  artifacts:
    - path: app.json
      provides: "expo.version 2.5.0; native build fields unchanged"
    - path: package.json
      provides: "npm package version 2.5.0"
    - path: package-lock.json
      provides: "lockfile root version 2.5.0"
    - path: docs/CONFIGURATION.md
      provides: "Version table + 2.5.0 What's New zh/en paste blocks; 2.4.2 pending retired"
    - path: README.md
      provides: "Badge + live/shipping narrative for 2.5.0"
    - path: README.zh-CN.md
      provides: "Badge + 线上/待提交 narrative for 2.5.0"
  key_links:
    - from: "app.json expo.version"
      to: "package.json version + package-lock root"
      via: "same marketing string 2.5.0"
    - from: "docs/CONFIGURATION.md What's New 2.5.0"
      to: "App Store Connect paste (human, post-phase)"
      via: "fenced zh/en · bullets; D-04 order; D-05 disclosure last"
    - from: "README / CONFIGURATION store status"
      to: "honest live 2.4.1 until ASC confirms 2.5.0"
      via: "D-14 / Claude discretion — no invented live 2.4.2"
---

<objective>
Bump marketing version to `2.5.0`, unify Content Depth (ex-2.4.2) + Adaptive Mastery under one store narrative, and write bilingual App Store What's New with board-drift disclosure (SHIP-01, D-01..D-07, D-14).

Purpose: Make store-facing version and disclosure text match locked product facts before QA sign-off.
Output: Version-aligned metadata + CONFIGURATION What's New paste blocks + retired standalone 2.4.2 pending narrative.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-CONTEXT.md
@.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-RESEARCH.md
@.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-PATTERNS.md
@docs/CONFIGURATION.md
@app.json
@eas.json
</context>

## Artifacts this phase produces

| Symbol / artifact | File | Notes |
|-------------------|------|-------|
| Marketing `2.5.0` | `app.json` / `package.json` / `package-lock.json` | `expo.version` + npm version only |
| Store narrative | `README.md` / `README.zh-CN.md` / `docs/GETTING-STARTED.md` / `CLAUDE.md` / `.planning/PROJECT.md` | Live `2.4.1` · shipping `2.5.0`; retire pending `2.4.2` |
| What's New paste | `docs/CONFIGURATION.md` | Content Depth → Adaptive → drift; zh/en · bullets |

<tasks>

<task type="auto">
  <name>Task 1: Marketing version 2.5.0 + store narrative (D-01, D-02, D-03, D-14)</name>
  <files>app.json, package.json, package-lock.json, README.md, README.zh-CN.md, docs/GETTING-STARTED.md, CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-CONTEXT.md` (D-01, D-02, D-03, D-14)
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-PATTERNS.md` (marketing three-pack + README rewrite targets)
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-RESEARCH.md` (Pitfall 1 dual pending; Assumptions A4/A5)
    - `app.json` (confirm current `expo.version`, `ios.buildNumber`, `android.versionCode`)
    - `eas.json` (confirm `appVersionSource: local` + production `autoIncrement` — do not edit)
    - `README.md` / `README.zh-CN.md` badge + App Store lines + KPI/milestone tables mentioning live/shipping versions
    - `docs/GETTING-STARTED.md` end-user live/repo line
    - `CLAUDE.md` Scope marketing version clause
    - `.planning/PROJECT.md` live/shipping / v2.5 milestone wording
  </read_first>
  <action>
    1. Per D-01 / D-02 / SHIP-01 surfaces: set marketing version string to `2.5.0` in `app.json` `expo.version`, `package.json` `version`, and `package-lock.json` top-level `version` plus `packages[""].version` only. Do not rewrite transitive dependency versions.
    2. Per D-03: leave `app.json` `expo.ios.buildNumber` and `expo.android.versionCode` exactly as found (currently `"30"` and `3`). Do not edit `eas.json`.
    3. Per D-01 / D-14: rewrite store narrative so live/online is `2.4.1` and shipping/pending is `2.5.0` in `README.md`, `README.zh-CN.md`, `docs/GETTING-STARTED.md`, `CLAUDE.md` Scope, and `.planning/PROJECT.md`. Update version badges to `version-2.5.0`. Remove any standalone claim that the next store release is pending `2.4.2`. Optionally note Content Depth ships with `2.5.0`. Honest live fact: store may still show `2.4.1` until ASC confirms `2.5.0` — do not invent a live `2.4.2` claim.
    4. Rewrite stale milestone rows that still claim v2.5 is a deferred fifth puzzle type so they describe Adaptive Mastery / shipping `2.5.0` (RESEARCH A4 / PATTERNS).
    5. Do not touch `app/`, `components/`, `hooks/`, `contexts/`, `locales/`, `constants/config.ts` (`APP_SALT`), `lib/puzzles/`, or any puzzle/UI product code. Frontend-code-review skill does not apply when those paths stay untouched.
  </action>
  <acceptance_criteria>
    - `rg -n '"version": "2\.5\.0"' app.json package.json package-lock.json` matches marketing fields
    - `node -e "const a=require('./app.json'); if(a.expo.version!=='2.5.0') process.exit(1); if(a.expo.ios.buildNumber!=='30') process.exit(2); if(a.expo.android.versionCode!==3) process.exit(3)"` exits 0
    - `rg -n 'version-2\.5\.0' README.md README.zh-CN.md` matches both badges
    - `rg -n 'Live.*2\.4\.1|shipping.*2\.5\.0|线上.*2\.4\.1|待提交.*2\.5\.0' README.md README.zh-CN.md docs/GETTING-STARTED.md CLAUDE.md` matches each file at least once for the new narrative
    - After edits, narrative files must not still market pending `2.4.2` as the next release: `rg -n 'shipping \*\*2\.4\.2\*\*|待提交 \*\*2\.4\.2\*\*|shipping \`2\.4\.2\`|待提交 \`2\.4\.2\`' README.md README.zh-CN.md docs/GETTING-STARTED.md CLAUDE.md .planning/PROJECT.md` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>node -e "const a=require('./app.json'); const p=require('./package.json'); if(a.expo.version!=='2.5.0'||p.version!=='2.5.0') process.exit(1); if(a.expo.ios.buildNumber!=='30'||a.expo.android.versionCode!==3) process.exit(2)" && rg -n 'version-2\.5\.0' README.md README.zh-CN.md</automated>
  </verify>
  <done>
    Marketing surfaces say 2.5.0; native build fields untouched; Live 2.4.1 / shipping 2.5.0 narrative consistent; standalone pending 2.4.2 store story retired (D-01..D-03, D-14).
  </done>
</task>

<task type="auto">
  <name>Task 2: CONFIGURATION What's New 2.5.0 + retire 2.4.2 pending (D-04..D-07, D-14, SHIP-01)</name>
  <files>docs/CONFIGURATION.md</files>
  <read_first>
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-CONTEXT.md` (D-04..D-07, D-14)
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-RESEARCH.md` Recommended What's New skeleton + Open Question 1
    - `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-PATTERNS.md` CONFIGURATION What's New lifecycle + retirement rules
    - `docs/CONFIGURATION.md` version table, store status row, existing `2.4.2` · 待提交 and `2.4.1` · 已上架 blocks
  </read_first>
  <action>
    1. Per D-02: in `docs/CONFIGURATION.md` set the Version table marketing cell to `2.5.0` and the iOS App Store status row to Live `2.4.1` · shipping `2.5.0`. Update buildNumber prose to state current value is `30` and next EAS production autoIncrement is `31` — edit prose only; never change `app.json` build fields (D-03 / Pitfall 2).
    2. Per D-14 / RESEARCH Open Question 1: retire the standalone `` `2.4.2` What's New · 待提交 `` paste block — delete it or replace with a short note that Content Depth ships with `2.5.0`. Do not mark `2.4.2` as 已上架 (never separately submitted). Leave historical `2.4.1` / `2.4.0` 已上架 blocks intact.
    3. Per D-04, D-05, D-06, D-07 / SHIP-01: insert a new `` `2.5.0` What's New · 待提交 `` section above remaining historical blocks, with bilingual fenced paste blocks (中文 · 傻了么 / English · Brainfool). Structure locked: 2–3 Content Depth `·` bullets (week rhythm + slitherlink stretch + nonogram 120 topics), then 2–3 Adaptive `·` bullets (quiet mastery adapt + local avoid-repeat; still one game a day; no difficulty-badge claims), then exactly one standalone disclosure bullet last stating dates not yet opened/played may get a different board after update and that in-progress progress is kept. Claude drafts final wording to match historical CONFIGURATION tone (discretion); D-04..D-06 semantics are non-negotiable. Use RESEARCH/PATTERNS skeleton as the default draft.
    4. Per D-06: store copy must not discuss gallery reconstruction or mastery-default rebuild nuances.
    5. Do not add in-app release-notes UI or edit locales.
  </action>
  <acceptance_criteria>
    - `rg -n 'Version \| \`2\.5\.0\`|Live \`2\.4\.1\`|shipping \`2\.5\.0\`' docs/CONFIGURATION.md` matches
    - `rg -n '2\.5\.0\` What.s New · 待提交' docs/CONFIGURATION.md` matches
    - `rg -n '2\.4\.2\` What.s New · 待提交' docs/CONFIGURATION.md` returns no matches (pending block retired)
    - `rg -n '尚未游玩的日期盘面可能变化|进行中的进度会保留' docs/CONFIGURATION.md` matches Chinese disclosure inside the new 2.5.0 block
    - `rg -n 'dates you have not opened yet may change|in-progress progress is kept' docs/CONFIGURATION.md` matches English disclosure
    - Within the new 2.5.0 What's New fenced blocks, Adaptive bullets exist (mastery / adapt wording) and Content Depth topics (week rhythm / 120 / slitherlink stretch) appear before the drift bullet
    - `rg -n 'DEFAULT_MASTERY|gallery.*mastery|难度徽章|difficulty badge' docs/CONFIGURATION.md` returns no matches in the new 2.5.0 What's New section (spot-check manually if older sections mention unrelated terms)
  </acceptance_criteria>
  <verify>
    <automated>rg -n '2\.5\.0\` What.s New · 待提交' docs/CONFIGURATION.md && rg -n '尚未游玩的日期盘面可能变化' docs/CONFIGURATION.md && rg -n 'dates you have not opened yet may change' docs/CONFIGURATION.md && ! rg -n '2\.4\.2\` What.s New · 待提交' docs/CONFIGURATION.md</automated>
  </verify>
  <done>
    CONFIGURATION carries paste-ready 2.5.0 What's New with D-04 order and D-05 disclosure last; standalone 2.4.2 pending paste retired; D-06 store-copy constraints honored (SHIP-01, D-04..D-07, D-14).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo docs → App Store Connect | Operator pastes What's New; misleading copy becomes public store text |
| Marketing version metadata → EAS/store | Wrong `expo.version` ships wrong user-facing version; build numbers stay EAS-owned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v25-06-01 | Tampering | `APP_SALT` / `dailySelector` / generators | high | mitigate | Explicit out-of-scope; Task 1/2 forbid product/puzzle edits; Plan 02 greps salt |
| T-v25-06-02 | Tampering | `ios.buildNumber` / `android.versionCode` | medium | mitigate | D-03 acceptance: leave `"30"` / `3`; prose-only CONFIGURATION fix |
| T-v25-06-03 | Repudiation | What's New disclosure | medium | mitigate | SHIP-01 / D-05 mandatory zh+en disclosure + in-progress keep; D-04 structure |
| T-v25-06-04 | Repudiation | Dual pending store versions | medium | mitigate | D-01/D-14 single `2.5.0` narrative; retire pending `2.4.2` paste/story |
| T-v25-06-05 | Information Disclosure | Store copy oversharing | low | mitigate | D-06 forbid gallery / mastery-default rebuild nuances in What's New |
| T-v25-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |
</threat_model>

<verification>
node version checks + rg gates in task acceptance_criteria; full AGENTS CI deferred to plan 02.
</verification>

<success_criteria>
- Marketing surfaces show `2.5.0` with native build fields unchanged (D-01..D-03)
- SHIP-01 What's New zh+en: Content Depth → Adaptive → drift disclosure (D-04..D-07)
- Standalone pending `2.4.2` narrative retired; Live `2.4.1` · shipping `2.5.0` (D-14)
</success_criteria>

<output>
Create `.planning/phases/v2.5-06-ship-2-5-0/v2.5-06-01-SUMMARY.md` when done
</output>
