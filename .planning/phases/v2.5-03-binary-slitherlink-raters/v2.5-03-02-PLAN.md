---
phase: v2.5-03-binary-slitherlink-raters
plan: 02
type: execute
wave: 2
depends_on:
  - v2.5-03-01
files_modified:
  - lib/puzzles/slitherlink/techniqueIds.ts
  - lib/puzzles/slitherlink/techniques.ts
  - lib/puzzles/slitherlink/loops.ts
  - lib/puzzles/slitherlink/bifurcation.ts
  - lib/puzzles/slitherlink/rater.ts
  - lib/puzzles/slitherlink/generateForTier.ts
  - lib/puzzles/slitherlink/generator.ts
  - __tests__/lib/puzzles/slitherlink/fixtures/techniqueBoards.ts
  - __tests__/lib/puzzles/slitherlink/rater.test.ts
  - __tests__/lib/puzzles/slitherlink/bifurcation.test.ts
  - __tests__/lib/puzzles/slitherlink/generateForTier.test.ts
autonomous: true
requirements:
  - TIER-05
user_setup: []
must_haves:
  truths:
    - "rateSlitherlink maps peak SlitherlinkTechnique to shared DifficultyTier per D-24 (zero_elim/corner_three→easy; adjacent patterns + edge_count→medium; vertex_degree/local_loop→hard; bifurcation→expert) (D-01, D-06, D-22, D-24, TIER-05)"
    - "≥1 labeled Slitherlink fixture per tier rates solved with expectedTier and expectedPeak (D-04, ROADMAP SC-1)"
    - "generateSlitherlinkPuzzleForTier accepts only solved+exact tier; softens easier only; NEVER calls getSlitherlinkBuiltinPuzzle; throws after softens (D-02, D-08, D-09, D-12, D-13, D-14, ROADMAP SC-2)"
    - "Legacy easy|medium|hard DIFFICULTY_* remain generation guides; weekday slitherlinkParamsForDate path + band.test.ts stay green (D-08, D-10, D-11)"
    - "Expert bifurcation uses depth/node/step caps only; Expert-target seed completes without hang; Hard never requires bifurcation (D-05, D-18, D-21, D-24, ROADMAP SC-3)"
  artifacts:
    - path: lib/puzzles/slitherlink/techniqueIds.ts
      provides: "SlitherlinkTechnique + TECHNIQUE_ORDER + peakToTier"
      exports: ["SlitherlinkTechnique", "TECHNIQUE_ORDER", "peakToTier", "maxTechnique"]
    - path: lib/puzzles/slitherlink/techniques.ts
      provides: "Easy–Medium one-hit detectors + applyNextTechnique dispatcher"
      exports: ["applyNextTechnique"]
    - path: lib/puzzles/slitherlink/loops.ts
      provides: "Hard vertex_degree + local_loop detectors"
      exports: ["tryVertexDegree", "tryLocalLoop"]
    - path: lib/puzzles/slitherlink/bifurcation.ts
      provides: "Bounded Expert bifurcation"
      exports: ["tryBifurcation", "SL_BIFURCATION_MAX_DEPTH", "SL_BIFURCATION_MAX_NODES"]
    - path: lib/puzzles/slitherlink/rater.ts
      provides: "rateSlitherlink three-state SE loop"
      exports: ["rateSlitherlink", "RateSlitherlinkResult", "SL_MAX_TECHNIQUE_STEPS"]
    - path: lib/puzzles/slitherlink/generateForTier.ts
      provides: "generateSlitherlinkPuzzleForTier + guideForTier (no builtin)"
      exports: ["generateSlitherlinkPuzzleForTier", "guideForTier", "SL_TIER_MAX_ATTEMPTS", "SL_TIER_MAX_SOFTENS"]
    - path: lib/puzzles/slitherlink/generator.ts
      provides: "Demoted guide JSDoc + generateOnceForGuides for forTier; legacy API + builtin kept"
    - path: __tests__/lib/puzzles/slitherlink/fixtures/techniqueBoards.ts
      provides: "Four frozen tier fixtures"
    - path: __tests__/lib/puzzles/slitherlink/rater.test.ts
      provides: "Fixture peak→tier asserts"
    - path: __tests__/lib/puzzles/slitherlink/bifurcation.test.ts
      provides: "CR-01 soundness for bifurcation"
    - path: __tests__/lib/puzzles/slitherlink/generateForTier.test.ts
      provides: "Determinism, no-builtin, Expert no-hang"
  key_links:
    - from: "peakToTier"
      to: "DifficultyTier"
      via: "D-24 collapse 1–10 → 4; never conflate with SlitherlinkDifficulty"
    - from: "rateSlitherlink"
      to: "applyNextTechnique + loops + tryBifurcation"
      via: "one-hit SE scan; never rate bulk runPropagation as one technique"
    - from: "generateSlitherlinkPuzzleForTier"
      to: "guideForTier + rateSlitherlink"
      via: "accept peak only; throw — never getSlitherlinkBuiltinPuzzle"
    - from: "generateSlitherlinkPuzzle"
      to: "DIFFICULTY_* + slitherlinkParamsForDate + builtin fallback"
      via: "legacy weekday path unchanged (D-10/D-11)"
---

<objective>
Deliver Slitherlink (7×7) full-depth technique rater + generateSlitherlinkPuzzleForTier with four-tier guides under peak gate, demoting legacy 3-way difficulty to guides only (D-01..D-16, D-18..D-24, D-27, TIER-05).

Purpose: Complete second type of Adaptive technique depth; prove no-builtin forTier so mastery wire cannot silently substitute boards.
Output: SL techniqueIds/techniques/loops/bifurcation/rater/generateForTier, demoted generator guides, four fixtures + tests; band.test.ts still green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md
@.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md
@.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-PATTERNS.md
@.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-01-SUMMARY.md
@lib/puzzles/difficulty/tiers.ts
@lib/puzzles/sudoku/rater.ts
@lib/puzzles/sudoku/generateForTier.ts
@lib/puzzles/binary/rater.ts
@lib/puzzles/binary/generateForTier.ts
@lib/puzzles/slitherlink/generator.ts
@lib/puzzles/slitherlink/solver.ts
@lib/puzzles/slitherlink/validate.ts
@lib/puzzles/slitherlink/edges.ts
@lib/puzzles/slitherlink/builtinPuzzle.ts
@lib/puzzles/difficulty/slitherlinkBand.ts
@lib/puzzles/rng.ts
@__tests__/lib/puzzles/slitherlink/band.test.ts
</context>

## Discretion locks (Claude's Discretion → executor MUST follow)

| Topic | Lock |
|-------|------|
| File split | `techniqueIds.ts` + `techniques.ts` (Easy–Medium) + `loops.ts` (Hard) + `bifurcation.ts` + `rater.ts` + `generateForTier.ts` |
| SlitherlinkTechnique IDs | Exact: `zero_elim`, `corner_three`, `adjacent_three_three`, `adjacent_three_zero`, `diagonal_three_three`, `edge_count`, `vertex_degree`, `local_loop`, `bifurcation` (D-06, D-24) |
| TECHNIQUE_ORDER | Ascending list of those nine ids (RESEARCH order) |
| peakToTier | zero_elim/corner_three→easy; adjacent_three_three/adjacent_three_zero/diagonal_three_three/edge_count→medium; vertex_degree/local_loop→hard; bifurcation→expert (D-24) |
| Hard loop | **local_loop only** — no full Jordan/region coloring unless fixtures prove unreachable (CONTEXT Discretion) |
| Decomposition | Decompose propagateForcedEdges / clue-0 / edge-count into named one-hit detectors — **do not** rate `runPropagation` as a single technique |
| Expert budgets | `SL_MAX_TECHNIQUE_STEPS=500`, `SL_BIFURCATION_MAX_DEPTH=2`, `SL_BIFURCATION_MAX_NODES=600` (D-18, D-20) |
| Rate result | solved \| incomplete \| budget_exhausted (D-15); solved requires partial consistency + single loop (WR-01 parity) |
| Four-tier guides | easy minClues **26** inside **32–44**; medium **20** / **22–36**; hard **14** / **12–26**; expert **10** / **8–18** via `guideForTier` only (D-09) |
| Legacy 3-way | Keep `SlitherlinkDifficulty` + `DIFFICULTY_MIN_CLUES` / `DIFFICULTY_INSIDE_RANGE` values; JSDoc rename to **generation guides** (D-08); do not delete |
| Weekday | `slitherlinkParamsForDate` / bandLerp unchanged; still only legacy `generateSlitherlinkPuzzle(seed, dateKey?)` (D-10) |
| forTier carve | Export `generateOnceForGuides(seed, { minClues, inside })` (or equivalent) — no dateKey band, no builtin |
| Attempts/softens | `SL_TIER_MAX_ATTEMPTS=40`, `SL_TIER_MAX_SOFTENS=3` (D-17) |
| Builtin | `getSlitherlinkBuiltinPuzzle` **only** on legacy exhaustion — **never** inside generateForTier (D-13) |
| Soften | Duplicate tiny helper; never return harder than request (D-02) |
| Out of scope | No Nonogram (D-26 deferred); no Binary edits beyond reading pattern; no selector/hydrate; no UI; no APP_SALT; no npm solvers |

## Artifacts this plan produces

| Symbol / file | Role |
|---------------|------|
| `SlitherlinkTechnique`, `TECHNIQUE_ORDER`, `peakToTier` | Ladder contracts (D-24) |
| `applyNextTechnique` + Easy–Medium detectors | One-hit SE scan |
| `tryVertexDegree` / `tryLocalLoop` in loops.ts | Hard band |
| `tryBifurcation` + bifurcation caps | Expert band |
| `rateSlitherlink`, `RateSlitherlinkResult`, `SL_MAX_TECHNIQUE_STEPS` | SE rater |
| `guideForTier`, `generateOnceForGuides`, `generateSlitherlinkPuzzleForTier` | Peak-gated gen (no builtin) |
| Demoted DIFFICULTY_* JSDoc | Legacy guides under peak gate (D-08) |
| Four fixtures + rater / bifurcation / generateForTier tests | SC-1..SC-3 proof |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SL technique contracts + Easy–Hard detectors + fixture scaffold</name>
  <files>lib/puzzles/slitherlink/techniqueIds.ts, lib/puzzles/slitherlink/techniques.ts, lib/puzzles/slitherlink/loops.ts, __tests__/lib/puzzles/slitherlink/fixtures/techniqueBoards.ts, __tests__/lib/puzzles/slitherlink/rater.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md` (D-01, D-06, D-24)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md` (SL technique contract, Pitfall 3 bulk propagation)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-PATTERNS.md` (techniques / loops analogs)
    - `lib/puzzles/sudoku/techniqueIds.ts`
    - `lib/puzzles/binary/techniqueIds.ts` (isomorpic pattern from plan 01)
    - `lib/puzzles/slitherlink/validate.ts` (propagateForcedEdges / runPropagation as reference only)
    - `lib/puzzles/slitherlink/edges.ts` (clonePlayState)
    - `lib/puzzles/difficulty/tiers.ts`
  </read_first>
  <behavior>
    - peakToTier maps per Discretion locks D-24 table
    - TECHNIQUE_ORDER lists all nine techniques once ascending
    - applyNextTechnique applies first productive Easy–Medium hit; Hard detectors live in loops.ts and are included in the detector list
    - Fixtures export ≥1 board each for easy/medium/hard with expectedTier + expectedPeak (Expert stub OK until Task 2)
    - rater.test.ts RED: peakToTier + Easy/Medium/Hard fixture expects
  </behavior>
  <action>
    Per D-01/D-06/D-24: create `techniqueIds.ts` with exact nine SlitherlinkTechnique ids and peakToTier bands from Discretion locks; import shared DifficultyTier only — never alias SlitherlinkDifficulty as product tier (D-22).
    Create `techniques.ts` one-hit detectors: zero_elim, corner_three, adjacent_three_three, adjacent_three_zero, diagonal_three_three, edge_count — operate on cloned play state + clues; return TechniqueResult. Create `loops.ts` with tryVertexDegree and tryLocalLoop (local only — no Jordan coloring). Export applyNextTechnique that scans Easy→Hard detectors in TECHNIQUE_ORDER excluding bifurcation.
    Do not call runPropagation as a rated step. Add fixtures/techniqueBoards.ts (hand-craft Easy; freeze Medium/Hard from generate+rate-once). Start rater.test.ts TDD RED. No Binary production edits; no selector wire.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/slitherlink/rater.test.ts __tests__/lib/puzzles/difficulty/tiers.test.ts --selectProjects unit --no-coverage 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `techniqueIds.ts` exports `peakToTier` and contains ids zero_elim, corner_three, bifurcation
    - `techniques.ts` exports `applyNextTechnique`
    - `loops.ts` references technique ids vertex_degree and local_loop
    - `techniqueBoards.ts` exports fixtures covering expectedTier easy, medium, and hard
    - `rater.test.ts` asserts expectedTier and expectedPeak
  </acceptance_criteria>
  <done>SL ladder + Easy–Hard detectors exist; fixtures scaffolded; rater tests RED until rateSlitherlink.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded bifurcation + rateSlitherlink + four-tier fixtures green</name>
  <files>lib/puzzles/slitherlink/bifurcation.ts, lib/puzzles/slitherlink/rater.ts, __tests__/lib/puzzles/slitherlink/fixtures/techniqueBoards.ts, __tests__/lib/puzzles/slitherlink/rater.test.ts, __tests__/lib/puzzles/slitherlink/bifurcation.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md` (D-04, D-07, D-15, D-18, D-24, D-27)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md` (Expert SL budgets, Pitfall 1/2, Pattern 1)
    - `lib/puzzles/sudoku/rater.ts`
    - `lib/puzzles/sudoku/chains.ts`
    - `lib/puzzles/binary/lookAhead.ts` (soundness pattern from plan 01)
    - `lib/puzzles/slitherlink/techniqueIds.ts`
    - `lib/puzzles/slitherlink/techniques.ts`
    - `lib/puzzles/slitherlink/loops.ts`
    - `lib/puzzles/slitherlink/edges.ts`
    - `lib/puzzles/slitherlink/validate.ts`
    - `lib/puzzles/slitherlink/solver.ts` (do NOT place via solveRecursive during rating)
  </read_first>
  <behavior>
    - rateSlitherlink rates each of four fixtures to expectedTier + expectedPeak with status solved
    - Hard fixtures peak at vertex_degree or local_loop — never bifurcation
    - Budget trip → budget_exhausted; never hang; same clues → identical result
    - Illegal bifurcation must not force an edge (CR-01)
    - Rater does not call countSolutionsUpTo / solveRecursive in the placement loop
  </behavior>
  <action>
    Per D-07/D-15/D-18/D-24/D-27: implement `bifurcation.ts` with `tryBifurcation`, caps `SL_BIFURCATION_MAX_DEPTH=2` and `SL_BIFURCATION_MAX_NODES=600`, clone via clonePlayState per branch; force only when one branch contradicts after Easy–Hard logical prop; signal budgetExceeded on cap — no wall-clock timers.
    Implement `rateSlitherlink(clues): RateSlitherlinkResult` with `SL_MAX_TECHNIQUE_STEPS=500`: validate clue shape; SE loop applyNextTechnique then tryBifurcation; peak via maxTechnique; solved only when consistent single loop (isPartialConsistent + isSingleLoopComplete parity); incomplete / budget_exhausted otherwise.
    Freeze Expert fixture with expectedPeak bifurcation. Green four fixtures. Add bifurcation.test.ts soundness regression. Uniqueness stays at generation only.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/slitherlink/rater.test.ts __tests__/lib/puzzles/slitherlink/bifurcation.test.ts --selectProjects unit --no-coverage 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `rater.ts` exports `rateSlitherlink` and `RateSlitherlinkResult`
    - Named consts SL_MAX_TECHNIQUE_STEPS=500, SL_BIFURCATION_MAX_DEPTH=2, SL_BIFURCATION_MAX_NODES=600 exist
    - `rater.test.ts` asserts all four tiers easy/medium/hard/expert
    - Hard fixture expectedPeak is vertex_degree or local_loop (not bifurcation)
    - `bifurcation.test.ts` asserts illegal force does not apply
    - Region rater/bifurcation: no wall-clock timer APIs — `grep -E 'Date\\.now|performance\\.now' lib/puzzles/slitherlink/rater.ts lib/puzzles/slitherlink/bifurcation.ts | wc -l | grep -q '^0$'`
    <!-- planner-discipline-allow: Date.now -->
    <!-- planner-discipline-allow: performance.now -->
  </acceptance_criteria>
  <done>Four SL fixtures rate correctly; Expert bifurcation budgeted and sound; Hard stays non-bifurcation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Demote legacy guides + generateSlitherlinkPuzzleForTier (no builtin)</name>
  <files>lib/puzzles/slitherlink/generateForTier.ts, lib/puzzles/slitherlink/generator.ts, __tests__/lib/puzzles/slitherlink/generateForTier.test.ts, __tests__/lib/puzzles/slitherlink/band.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md` (D-02, D-08..D-14, D-17, D-19)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md` (Pattern 2/3, SL four-tier guides, Pitfall 5)
    - `lib/puzzles/sudoku/generateForTier.ts`
    - `lib/puzzles/binary/generateForTier.ts`
    - `lib/puzzles/slitherlink/generator.ts`
    - `lib/puzzles/slitherlink/rater.ts`
    - `lib/puzzles/slitherlink/builtinPuzzle.ts`
    - `lib/puzzles/slitherlink/solver.ts`
    - `lib/puzzles/difficulty/slitherlinkBand.ts`
    - `__tests__/lib/puzzles/slitherlink/band.test.ts`
    - `__tests__/lib/puzzles/sudoku/generateForTier.test.ts`
  </read_first>
  <behavior>
    - guideForTier returns exact RESEARCH numbers: easy 26/{32,44}, medium 20/{22,36}, hard 14/{12,26}, expert 10/{8,18}
    - Same seed+target → identical puzzleHash twice
    - Unique solution countSolutionsUpTo===1; accept only solved+exact tier
    - Soften toward easier only; never harder than request
    - generateSlitherlinkPuzzleForTier never imports or calls getSlitherlinkBuiltinPuzzle; throws after softens
    - Expert-target fixed seed completes under Jest timeout
    - band.test.ts and legacy generateSlitherlinkPuzzle(seed, dateKey?) still pass
  </behavior>
  <action>
    Per D-08/D-09/D-10/D-11/D-13: surgically update `generator.ts` JSDoc on DIFFICULTY_MIN_CLUES / DIFFICULTY_INSIDE_RANGE / SlitherlinkDifficulty to state they are **generation guides**, not public difficulty claims — keep numeric values and weekday/no-dateKey legacy path including builtin fallback on exhaustion.
    Export `generateOnceForGuides(seed, guides)` (name may be generateOnceForGuides) that runs polyomino + carve using the four-tier guide params without dateKey band and without builtin.
    Add `generateForTier.ts` with `guideForTier`, `SL_TIER_MAX_ATTEMPTS=40`, `SL_TIER_MAX_SOFTENS=3`, softenTowardEasier, and `generateSlitherlinkPuzzleForTier` returning `{ puzzle, ratedTier, peakTechnique, softened }` (D-14). Accept only rateSlitherlink solved + exact tier; discard incomplete/budget; soften easier; throw after softens — **must not** call getSlitherlinkBuiltinPuzzle (D-13).
    Write generateForTier.test.ts covering guides, determinism, uniqueness, never-harder, Expert completion (D-05/D-21), and explicit no-builtin assert (source or runtime: forTier module must not reference builtinPuzzle). Keep band.test.ts green. No selector/hydrate wire.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/slitherlink/generateForTier.test.ts __tests__/lib/puzzles/slitherlink/band.test.ts __tests__/lib/puzzles/slitherlink/rater.test.ts --selectProjects unit --no-coverage 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - `generateForTier.ts` exports `generateSlitherlinkPuzzleForTier` and `guideForTier`
    - `guideForTier` / generateForTier contains Expert guide values minClues 10 and inside range 8–18
    - `generateForTier.ts` defines SL_TIER_MAX_ATTEMPTS=40 and SL_TIER_MAX_SOFTENS=3
    - `grep -c builtinPuzzle lib/puzzles/slitherlink/generateForTier.ts` exits with count 0
    - `generator.ts` still exports `generateSlitherlinkPuzzle` and still references getSlitherlinkBuiltinPuzzle on the legacy path only
    - `generateForTier.test.ts` includes Expert completion and never-harder / softened asserts
    - `__tests__/lib/puzzles/slitherlink/band.test.ts` still passes
    - `git diff --name-only` for this plan work does not list dailySelector or mastery hydrate files
  </acceptance_criteria>
  <done>TIER-05 Slitherlink gen+rate shipped; legacy 3-way demoted to guides; forTier never builtin; ROADMAP SC-1..SC-3 satisfied for SL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → rateSlitherlink(clues) | Malformed 7×7 clue grids |
| generateForTier → bifurcation | Expert trial clones play state; CPU DoS risk |
| forTier vs legacy builtin | Builtin must not spoof tier-targeted boards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2.5-03-02-01 | Denial of Service | bifurcation.ts / rater.ts | high | mitigate | Caps depth=2, nodes=600, steps=500; discard on budget_exhausted (D-18) |
| T-v2.5-03-02-02 | Tampering | Unsound bifurcation | high | mitigate | Force only one-branch contradiction; bifurcation.test.ts (D-27, CR-01) |
| T-v2.5-03-02-03 | Spoofing | builtin in forTier | high | mitigate | Never call getSlitherlinkBuiltinPuzzle in generateForTier; throw after softens (D-13); test grep gate |
| T-v2.5-03-02-04 | Tampering | Peak collapse via runPropagation | medium | mitigate | One-hit detectors only; never rate bulk propagation (D-24) |
| T-v2.5-03-02-05 | Tampering | Determinism | medium | mitigate | Seeded RNG; no wall-clock in rate/gen (D-18) |
| T-v2.5-03-02-06 | Tampering | Legacy weekday breakage | medium | mitigate | Surgical JSDoc demotion; band.test.ts must stay green (D-10) |
| T-v2.5-03-02-07 | Information Disclosure | N/A | low | accept | Offline local puzzle lib |
| T-v2.5-03-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Focused SL jest suites (rater, bifurcation, generateForTier, band) pass
- [ ] `npm run typecheck` clean
- [ ] generateForTier.ts has zero builtinPuzzle references
- [ ] No dailySelector / mastery hydrate / UI / Nonogram edits
- [ ] No wall-clock in SL rater / bifurcation / generateForTier
</verification>

<success_criteria>
- TIER-05 delivered: Slitherlink technique ladder → four tiers + generateForTier without builtin
- ROADMAP SC-1: four fixtures classify correctly
- ROADMAP SC-2: target tier hit within attempt budget (or soften)
- ROADMAP SC-3: Expert path budgeted, no hang
- D-08/D-10/D-11: legacy guides + weekday path preserved
</success_criteria>

<output>
Create `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-02-SUMMARY.md` when done
</output>
