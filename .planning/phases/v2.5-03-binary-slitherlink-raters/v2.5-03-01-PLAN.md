---
phase: v2.5-03-binary-slitherlink-raters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/puzzles/binary/techniqueIds.ts
  - lib/puzzles/binary/techniques.ts
  - lib/puzzles/binary/lookAhead.ts
  - lib/puzzles/binary/rater.ts
  - lib/puzzles/binary/generateForTier.ts
  - lib/puzzles/binary/generator.ts
  - __tests__/lib/puzzles/binary/fixtures/techniqueBoards.ts
  - __tests__/lib/puzzles/binary/rater.test.ts
  - __tests__/lib/puzzles/binary/lookAhead.test.ts
  - __tests__/lib/puzzles/binary/generateForTier.test.ts
autonomous: true
requirements:
  - TIER-03
user_setup: []
must_haves:
  truths:
    - "rateBinary maps peak BinaryTechnique to shared DifficultyTier per D-23 bands (adjacent_pair/gap_fill→easy; balance→medium; uniqueness→hard; look_ahead→expert) (D-01, D-06, D-22, D-23, TIER-03)"
    - "≥1 labeled Binary fixture per tier rates solved with expectedTier and expectedPeak (D-04, ROADMAP SC-1)"
    - "generateBinaryPuzzleForTier accepts only solved+exact tier; softens toward easier only; never returns harder than requested; never uses builtin (D-02, D-03, D-12, D-13, D-14, ROADMAP SC-2)"
    - "Expert look_ahead uses step/node/depth caps only; budget_exhausted discards; Expert-target seed completes without hang (D-05, D-18, D-19, D-21, ROADMAP SC-3)"
    - "Rater places cells only via logical techniques — no backtracking solve placement; legacy generateBinaryPuzzle(seed, dateKey?) unchanged (D-07, D-11, D-27)"
  artifacts:
    - path: lib/puzzles/binary/techniqueIds.ts
      provides: "BinaryTechnique union + TECHNIQUE_ORDER + peakToTier"
      exports: ["BinaryTechnique", "TECHNIQUE_ORDER", "peakToTier", "maxTechnique", "techniqueRank"]
    - path: lib/puzzles/binary/techniques.ts
      provides: "Easy–Hard one-hit detectors + applyNextTechnique"
      exports: ["applyNextTechnique", "TechniqueResult"]
    - path: lib/puzzles/binary/lookAhead.ts
      provides: "Bounded Expert look_ahead"
      exports: ["tryLookAhead", "BINARY_LOOKAHEAD_MAX_DEPTH", "BINARY_LOOKAHEAD_MAX_NODES"]
    - path: lib/puzzles/binary/rater.ts
      provides: "rateBinary three-state SE loop"
      exports: ["rateBinary", "RateBinaryResult", "BINARY_MAX_TECHNIQUE_STEPS"]
    - path: lib/puzzles/binary/generateForTier.ts
      provides: "generateBinaryPuzzleForTier + given guides + soften"
      exports: ["generateBinaryPuzzleForTier", "givenGuideForTier", "softenTowardEasier", "BINARY_TIER_MAX_ATTEMPTS", "BINARY_TIER_MAX_SOFTENS"]
    - path: lib/puzzles/binary/generator.ts
      provides: "Exported generateOnce for forTier reuse; legacy API intact"
      exports: ["generateOnce", "generateBinaryPuzzle"]
    - path: __tests__/lib/puzzles/binary/fixtures/techniqueBoards.ts
      provides: "Four frozen tier fixtures"
    - path: __tests__/lib/puzzles/binary/rater.test.ts
      provides: "Fixture peak→tier + peakToTier table"
    - path: __tests__/lib/puzzles/binary/lookAhead.test.ts
      provides: "CR-01 soundness regression for look_ahead"
    - path: __tests__/lib/puzzles/binary/generateForTier.test.ts
      provides: "Determinism, exact accept, soften, Expert no-hang"
  key_links:
    - from: "peakToTier"
      to: "DifficultyTier from lib/puzzles/difficulty/tiers.ts"
      via: "D-23 band map; never fork a second tier enum"
      pattern: "from ['\\\"].*difficulty/tiers"
    - from: "rateBinary"
      to: "applyNextTechnique + tryLookAhead"
      via: "SE restart; peak = maxTechnique; step budget BINARY_MAX_TECHNIQUE_STEPS"
    - from: "generateBinaryPuzzleForTier"
      to: "rateBinary + generateOnce + countSolutionsUpTo"
      via: "accept only status===solved && rated.tier===currentTier"
---

<objective>
Deliver Binary (8×8 Takuzu) full-depth technique rater + generateBinaryPuzzleForTier with exact-tier accept and soften-toward-easier (D-01..D-07, D-11..D-23, D-27, TIER-03).

Purpose: Prove Binary peak-technique gate so v2.5-05 can wire mastery without inventing accept policy.
Output: Binary techniqueIds/techniques/lookAhead/rater/generateForTier, exported generateOnce, four fixtures + unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md
@.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md
@.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-PATTERNS.md
@lib/puzzles/difficulty/tiers.ts
@lib/puzzles/sudoku/techniqueIds.ts
@lib/puzzles/sudoku/rater.ts
@lib/puzzles/sudoku/generateForTier.ts
@lib/puzzles/sudoku/chains.ts
@lib/puzzles/binary/generator.ts
@lib/puzzles/binary/solver.ts
@lib/puzzles/binary/validate.ts
@lib/puzzles/binary/spec.ts
@lib/puzzles/rng.ts
</context>

## Discretion locks (Claude's Discretion → executor MUST follow)

| Topic | Lock |
|-------|------|
| File split | `techniqueIds.ts` + `techniques.ts` (Easy–Hard) + `lookAhead.ts` + `rater.ts` + `generateForTier.ts` — no `techniques/` directory; no shared cross-type rater framework |
| BinaryTechnique IDs | Exact: `adjacent_pair`, `gap_fill`, `balance`, `uniqueness`, `look_ahead` (D-06, D-23) |
| TECHNIQUE_ORDER | Ascending: adjacent_pair → gap_fill → balance → uniqueness → look_ahead |
| peakToTier | adjacent_pair/gap_fill→easy; balance→medium; uniqueness→hard; look_ahead→expert (D-23) |
| Hard bitmask | Ship `uniqueness` only first (RESEARCH A2). Add Hard-only `line_mask` only if fixtures/gen cannot hit Hard without look_ahead — must still not call full-board look_ahead for Hard peak |
| Board size | `BINARY_SIZE = 8` — ignore outdated 6×6 prose |
| Expert budgets | `BINARY_MAX_TECHNIQUE_STEPS=500`, `BINARY_LOOKAHEAD_MAX_DEPTH=2`, `BINARY_LOOKAHEAD_MAX_NODES=800` (D-18, D-20) |
| Rate result | `{status:'solved', peak, tier}` \| `{status:'incomplete', peak}` \| `{status:'budget_exhausted', peak}` (D-15) |
| Given guides | easy **30**, medium **26**, hard **22**, expert **18** via `givenGuideForTier` (guides only; D-03) |
| Attempts/softens | `BINARY_TIER_MAX_ATTEMPTS=40`, `BINARY_TIER_MAX_SOFTENS=3`; sub-seed `tier-${tier}-${soft}-${attempt}` (D-17) |
| Return shape | `{ puzzle, ratedTier, peakTechnique, softened }` (D-14) |
| Soften helper | Duplicate tiny `softenTowardEasier` in this module (use `tierIndex`/`tierFromIndex`); do not create shared gen framework |
| Soundness | Force only when one hypothesis branch contradicts after Easy–Hard prop (Sudoku CR-01 class; D-27) |
| Out of scope | No Slitherlink; no Nonogram; no dailySelector/hydrate; no UI; no APP_SALT; no npm solvers; do not edit Sudoku peakToTier (D-25) |

## Artifacts this plan produces

| Symbol / file | Role |
|---------------|------|
| `BinaryTechnique`, `TECHNIQUE_ORDER`, `peakToTier`, `maxTechnique` | Ladder contracts |
| `applyNextTechnique` | One-hit Easy–Hard detectors |
| `tryLookAhead` + lookahead caps | Expert technique |
| `rateBinary`, `RateBinaryResult`, `BINARY_MAX_TECHNIQUE_STEPS` | SE rater |
| `generateOnce` (exported) | Carve helper for forTier |
| `generateBinaryPuzzleForTier`, `givenGuideForTier`, attempt/soften consts | Tier-targeted gen |
| Four fixtures + `rater` / `lookAhead` / `generateForTier` tests | SC-1..SC-3 proof |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Binary technique contracts + Easy–Hard detectors + fixture scaffold</name>
  <files>lib/puzzles/binary/techniqueIds.ts, lib/puzzles/binary/techniques.ts, __tests__/lib/puzzles/binary/fixtures/techniqueBoards.ts, __tests__/lib/puzzles/binary/rater.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md` (D-01, D-06, D-23)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md` (Binary technique contract, pitfalls)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-PATTERNS.md` (techniqueIds / techniques analogs)
    - `lib/puzzles/sudoku/techniqueIds.ts`
    - `lib/puzzles/sudoku/techniques.ts` (applyNextTechnique DETECTORS pattern)
    - `lib/puzzles/difficulty/tiers.ts`
    - `lib/puzzles/binary/spec.ts`
    - `lib/puzzles/binary/validate.ts`
    - `lib/puzzles/binary/grid.ts`
  </read_first>
  <behavior>
    - peakToTier maps adjacent_pair and gap_fill to easy; balance to medium; uniqueness to hard; look_ahead to expert
    - TECHNIQUE_ORDER lists each BinaryTechnique once ascending
    - applyNextTechnique returns first productive Easy–Hard hit only (never look_ahead in this module)
    - Fixture module exports ≥1 board each for easy/medium/hard with expectedTier + expectedPeak (Expert may stub until Task 2)
    - rater.test.ts RED: peakToTier table + Easy/Medium/Hard fixture expects (fail until rateBinary exists)
  </behavior>
  <action>
    Per D-01/D-06/D-23: create `techniqueIds.ts` exporting `BinaryTechnique` union with exact ids adjacent_pair, gap_fill, balance, uniqueness, look_ahead; ordered `TECHNIQUE_ORDER`; `techniqueRank`, `maxTechnique`, and `peakToTier` importing `DifficultyTier` from `../difficulty/tiers` — never fork a parallel tier union (D-22).
    Create `techniques.ts` with one-hit detectors on an 8×8 play grid (0/1/empty): adjacent_pair (XX? / ?XX flank opposite), gap_fill (X_X middle opposite), balance (row/col hit N/2 → fill remaining), uniqueness (near-duplicate row/col forces digit). Export `applyNextTechnique` scanning DETECTORS in TECHNIQUE_ORDER excluding look_ahead. Prefer uniqueness-only for Hard band; do not add line_mask unless later tasks prove Hard unreachable.
    Add `__tests__/lib/puzzles/binary/fixtures/techniqueBoards.ts` with hand-crafted Easy and generate+rate-once frozen Medium/Hard boards (freeze constants — never regenerate in CI). Start `rater.test.ts` with peakToTier table + fixture cases (TDD RED). Avoid UI, selector, Slitherlink, and npm packages.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/binary/rater.test.ts __tests__/lib/puzzles/difficulty/tiers.test.ts --selectProjects unit --no-coverage 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `lib/puzzles/binary/techniqueIds.ts` exports `peakToTier` and imports from `difficulty/tiers`
    - `techniqueIds.ts` contains the five id strings adjacent_pair, gap_fill, balance, uniqueness, look_ahead
    - `lib/puzzles/binary/techniques.ts` exports `applyNextTechnique`
    - `techniqueBoards.ts` exports fixtures covering expectedTier values easy, medium, and hard
    - `rater.test.ts` references `expectedTier` and `expectedPeak`
  </acceptance_criteria>
  <done>Binary ladder contracts + Easy–Hard detectors exist; fixtures scaffolded; rater tests written (may fail until rateBinary in Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded look_ahead + rateBinary + four-tier fixtures green</name>
  <files>lib/puzzles/binary/lookAhead.ts, lib/puzzles/binary/rater.ts, __tests__/lib/puzzles/binary/fixtures/techniqueBoards.ts, __tests__/lib/puzzles/binary/rater.test.ts, __tests__/lib/puzzles/binary/lookAhead.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md` (D-04, D-07, D-15, D-18, D-27)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md` (Expert budgets Binary table, Pitfall 1 CR-01, Pattern 1)
    - `lib/puzzles/sudoku/rater.ts`
    - `lib/puzzles/sudoku/chains.ts`
    - `__tests__/lib/puzzles/sudoku/chains.test.ts`
    - `lib/puzzles/binary/techniqueIds.ts`
    - `lib/puzzles/binary/techniques.ts`
    - `lib/puzzles/binary/solver.ts` (for what NOT to call during placement)
    - `lib/puzzles/binary/validate.ts`
  </read_first>
  <behavior>
    - rateBinary(easyFixture).status === solved and .tier === easy; same for medium/hard/expert fixtures with expectedPeak
    - When look_ahead node/depth budget trips → status budget_exhausted (or incomplete), never infinite loop
    - Same givens → identical RateBinaryResult across two calls
    - rateBinary must not import solve for placement; uniqueness precheck via countSolutionsUpTo is OK only outside rate placement
    - Illegal look_ahead premise must not force a victim cell (CR-01 soundness)
  </behavior>
  <action>
    Per D-07/D-15/D-18/D-27: implement `lookAhead.ts` exporting `tryLookAhead` with caps `BINARY_LOOKAHEAD_MAX_DEPTH=2` and `BINARY_LOOKAHEAD_MAX_NODES=800`. Sound trial only: force a cell only when exactly one branch contradicts after Easy–Hard logical prop; return budgetExceeded when node/depth caps trip — step/node counters only (never wall-clock elapsed timers for accept/reject).
    Implement `rateBinary(givens): RateBinaryResult` in `rater.ts`: validate 8×8 shape; loop ≤ `BINARY_MAX_TECHNIQUE_STEPS=500`; each step try `applyNextTechnique` then `tryLookAhead`; update peak via `maxTechnique`; full consistent board → solved + peakToTier; no hit → incomplete; budget → budget_exhausted. Conflict-check before solved (validate violations / WR-01 parity). If peak null on already-complete valid board, treat as easy (adjacent_pair or gap_fill peak default — document choice in SUMMARY).
    Freeze ≥1 Expert fixture with expectedPeak look_ahead. Green all four fixtures in `rater.test.ts`. Add `lookAhead.test.ts` soundness regression (illegal force must leave victim empty). Do not edit generator or wire selector.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/binary/rater.test.ts __tests__/lib/puzzles/binary/lookAhead.test.ts --selectProjects unit --no-coverage 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `rater.ts` exports `rateBinary` and `RateBinaryResult`
    - `rater.ts` or `lookAhead.ts` defines named const `BINARY_MAX_TECHNIQUE_STEPS` equal to 500 and lookahead depth/nodes 2 / 800
    - `rater.test.ts` asserts all four expectedTier easy medium hard expert from fixtures
    - `lookAhead.test.ts` exists and asserts soundness (illegal force does not apply)
    - Region `lib/puzzles/binary/rater.ts` and `lookAhead.ts`: no wall-clock timer APIs for rating — `awk '/^export function rateBinary/,/^export /' lib/puzzles/binary/rater.ts | grep -cE 'Date\\.now|performance\\.now' | grep -q '^0$'`
    <!-- planner-discipline-allow: Date.now -->
    <!-- planner-discipline-allow: performance.now -->
  </acceptance_criteria>
  <done>All four Binary tier fixtures rate correctly; Expert look_ahead budgeted and sound; RateBinaryResult stable for generate-for-tier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: generateBinaryPuzzleForTier accept/soften + Expert seed</name>
  <files>lib/puzzles/binary/generateForTier.ts, lib/puzzles/binary/generator.ts, __tests__/lib/puzzles/binary/generateForTier.test.ts, __tests__/lib/puzzles/binary/generator.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-CONTEXT.md` (D-02, D-03, D-05, D-11..D-14, D-17, D-19)
    - `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-RESEARCH.md` (Pattern 2, Binary given guides, attempt caps)
    - `lib/puzzles/sudoku/generateForTier.ts`
    - `__tests__/lib/puzzles/sudoku/generateForTier.test.ts`
    - `lib/puzzles/binary/generator.ts`
    - `lib/puzzles/binary/rater.ts`
    - `lib/puzzles/binary/solver.ts`
    - `lib/puzzles/difficulty/tiers.ts`
    - `lib/puzzles/rng.ts`
    - `__tests__/lib/puzzles/binary/generator.test.ts`
  </read_first>
  <behavior>
    - givenGuideForTier easy|medium|hard|expert → 30|26|22|18
    - Same seed+target → identical puzzleHash twice
    - Returned puzzle always countSolutionsUpTo(...,2)===1
    - When softened===false, ratedTier === requested targetTier
    - When softened===true, tierIndex(ratedTier) strictly below original target; never harder than request
    - Expert-target fixed seed completes under Jest default timeout
    - Legacy generateBinaryPuzzle(seed, dateKey?) tests still pass
  </behavior>
  <action>
    Per D-02/D-03/D-11/D-14/D-17: surgically `export function generateOnce` from `generator.ts` without changing `generateBinaryPuzzle(seed, dateKey?)` weekday/default givens semantics.
    Add `generateForTier.ts` with `givenGuideForTier` (30/26/22/18), `BINARY_TIER_MAX_ATTEMPTS=40`, `BINARY_TIER_MAX_SOFTENS=3`, duplicated `softenTowardEasier`, and `generateBinaryPuzzleForTier(seed, targetTier)` mirroring Sudoku: deriveSubSeed label `tier-${tier}-${soft}-${attempt}`; carve via generateOnce; require unique; rateBinary; accept only solved+exact tier; on budget/incomplete discard; after attempts soften easier only; throw Error after softens exhausted — never builtin (D-13 parity; Binary has no builtin but must not invent silent fallback boards).
    Write `generateForTier.test.ts` (TDD): guide table, soften ladder, determinism, uniqueness, never-harder, Expert seed completion (D-05/D-21), softened metadata path. Re-run legacy `generator.test.ts`. Do not wire dailySelector / mastery / hydrate.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/binary/generateForTier.test.ts __tests__/lib/puzzles/binary/generator.test.ts __tests__/lib/puzzles/binary/rater.test.ts --selectProjects unit --no-coverage 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - `generateForTier.ts` exports `generateBinaryPuzzleForTier` and `givenGuideForTier`
    - `generateForTier.ts` contains guide numeric literals 30, 26, 22, and 18
    - `generateForTier.ts` defines `BINARY_TIER_MAX_ATTEMPTS` as 40 and `BINARY_TIER_MAX_SOFTENS` as 3
    - `generator.ts` exports `generateOnce` and still exports `generateBinaryPuzzle`
    - `generateForTier.test.ts` includes Expert-target completion and uniqueness via countSolutionsUpTo
    - `git diff --name-only` for this plan work does not list `lib/puzzles/dailySelector.ts` or mastery Context files
  </acceptance_criteria>
  <done>TIER-03 Binary gen+rate loop shipped; ROADMAP SC-1..SC-3 satisfied for Binary; legacy weekday path intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → rateBinary(givens) | Malformed 8×8 grids from fixtures/generators |
| generateBinaryPuzzleForTier → rateBinary / lookAhead | Expert search can burn main-thread / Jest CPU |
| Puzzle lib → network/storage | Must remain offline; no storage shape change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2.5-03-01-01 | Denial of Service | lookAhead.ts / rater.ts | high | mitigate | Caps depth=2, nodes=800, steps=500; budget_exhausted discard (D-18) |
| T-v2.5-03-01-02 | Tampering | lookAhead unsound force | high | mitigate | CR-01: force only when one branch contradicts; lookAhead.test.ts regression (D-27) |
| T-v2.5-03-01-03 | Tampering | rateBinary input | medium | mitigate | Validate 8×8 shape + digit range at entry → incomplete |
| T-v2.5-03-01-04 | Spoofing | generateForTier difficulty | medium | mitigate | Accept only peak tier exact match; guides never gate accept (D-02/D-12) |
| T-v2.5-03-01-05 | Tampering | Determinism | medium | mitigate | Seeded RNG only; no wall-clock in rate/gen path (D-18) |
| T-v2.5-03-01-06 | Information Disclosure | N/A | low | accept | Offline local puzzle lib; no network/PII |
| T-v2.5-03-SC | Tampering | npm installs | high | mitigate | No new packages; legitimacy N/A |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx jest __tests__/lib/puzzles/binary/rater.test.ts __tests__/lib/puzzles/binary/lookAhead.test.ts __tests__/lib/puzzles/binary/generateForTier.test.ts __tests__/lib/puzzles/binary/generator.test.ts --selectProjects unit --no-coverage` passes
- [ ] `npm run typecheck` clean for new Binary modules
- [ ] Rater imports DifficultyTier from `lib/puzzles/difficulty/tiers.ts` only
- [ ] No dailySelector / mastery hydrate / UI / Slitherlink edits in this plan
- [ ] No wall-clock in binary rater / lookAhead / generateForTier
</verification>

<success_criteria>
- TIER-03 delivered: Binary technique ladder → four tiers + generateForTier
- ROADMAP SC-1: four fixtures classify correctly
- ROADMAP SC-2: target tier hit within attempt budget (or documented soften)
- ROADMAP SC-3: Expert path budgeted, no hang
- D-11 honored: legacy generateBinaryPuzzle intact; selector unwired
</success_criteria>

<output>
Create `.planning/phases/v2.5-03-binary-slitherlink-raters/v2.5-03-01-SUMMARY.md` when done
</output>
