---
phase: v2.5-05-dedupe-diversity-wire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - constants/config.ts
  - lib/storage/playedHashStorage.ts
  - docs/CONFIGURATION.md
  - __tests__/lib/storage/playedHashStorage.test.ts
autonomous: true
requirements:
  - DIV-01
user_setup: []
must_haves:
  truths:
    - "Each gameType has an independent FIFO played-hash ring capped at 200 entries (D-01, D-03, DIV-01)"
    - "Missing, corrupt, or newer-than-app ring payloads load as empty per-type lists without touching mastery or daily STORAGE_VERSION (D-12, D-14)"
    - "appendPlayedHash pushes newest and drops oldest when over capacity; storage key is separate from mastery (D-01, D-03, D-12)"
  artifacts:
    - path: constants/config.ts
      provides: "PLAYED_HASH_STORAGE_KEY/VERSION, PLAYED_HASH_RING_CAPACITY=200, AVOID_HASH_MAX_ATTEMPTS=40"
    - path: lib/storage/playedHashStorage.ts
      provides: "loadPlayedHashes / savePlayedHashes / appendPlayedHash / normalize + empty defaults"
      exports: ["loadPlayedHashes", "savePlayedHashes", "appendPlayedHash", "clearPlayedHashes", "PlayedHashesState"]
    - path: docs/CONFIGURATION.md
      provides: "Key inventory + bump-table row for played-hash ring"
    - path: __tests__/lib/storage/playedHashStorage.test.ts
      provides: "FIFO / missing / corrupt / version-gate coverage for DIV-01"
  key_links:
    - from: "appendPlayedHash"
      to: "AsyncStorage PLAYED_HASH_STORAGE_KEY"
      via: "load → push → slice(-CAPACITY) → save with PLAYED_HASH_STORAGE_VERSION"
    - from: "normalizePersistedPlayedHashes"
      to: "empty byType rings"
      via: "version > app or invalid JSON → warn + empty (never wipe mastery)"
---

<objective>
Add a versioned, per-`gameType` played-hash ring storage module (capacity 200, FIFO) mirroring `masteryStorage`, plus config constants and CONFIGURATION docs — foundation for DIV-01 before selector/hydrate wiring.

Purpose: Persist recent completed puzzle hashes safely offline without bumping daily or mastery storage versions (D-01, D-03, D-12, D-14, DIV-01).
Output: `playedHashStorage.ts`, config keys, docs row, unit tests green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-CONTEXT.md
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-RESEARCH.md
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-PATTERNS.md
@lib/storage/masteryStorage.ts
@lib/storage/completionHistoryStorage.ts
@constants/config.ts
@docs/CONFIGURATION.md
@__tests__/lib/storage/masteryStorage.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config constants + playedHashStorage module</name>
  <files>constants/config.ts, lib/storage/playedHashStorage.ts, __tests__/lib/storage/playedHashStorage.test.ts</files>
  <read_first>
    - lib/storage/masteryStorage.ts (normalize / version gate / load-save-clear shell)
    - lib/storage/completionHistoryStorage.ts (FIFO slice(-MAX) on save)
    - constants/config.ts (MASTERY_STORAGE_* block to mirror)
    - __tests__/lib/storage/masteryStorage.test.ts (test structure)
    - .planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-PATTERNS.md (Pattern Assignments for playedHashStorage)
  </read_first>
  <behavior>
    - Missing AsyncStorage key → empty rings for sudoku/binary/nonogram/slitherlink
    - Round-trip save/load preserves byType arrays and writes PLAYED_HASH_STORAGE_VERSION
    - version newer than app → console.warn + empty rings
    - Invalid JSON / non-object → empty rings; never mutates mastery key
    - appendPlayedHash beyond 200 → length 200 and oldest dropped (FIFO, D-01, D-03)
    - Non-string hash entries coerced away on normalize
    - STORAGE_VERSION and MASTERY_STORAGE_VERSION numeric values unchanged by this feature
  </behavior>
  <action>
    Add to constants/config.ts (do not change APP_SALT, STORAGE_VERSION, or MASTERY_STORAGE_VERSION): PLAYED_HASH_STORAGE_KEY = '@foolish-you/played-hash-v1'; PLAYED_HASH_STORAGE_VERSION = 1; PLAYED_HASH_RING_CAPACITY = 200 (D-01); AVOID_HASH_MAX_ATTEMPTS = 40 (D-05 — exported now for plan 02 consumers).

    Create lib/storage/playedHashStorage.ts as a separate module (D-12): type PlayedHashesState = { byType: Record&lt;GameType, string[]&gt; } (FIFO oldest→newest). Export loadPlayedHashes, savePlayedHashes, appendPlayedHash(gameType, puzzleHash), clearPlayedHashes, and normalize helper for tests. Mirror masteryStorage: try/catch warn on failure; missing → emptyPlayedHashes(); version &gt; PLAYED_HASH_STORAGE_VERSION → warn + empty; per-type coerce string-only arrays then slice(-PLAYED_HASH_RING_CAPACITY). appendPlayedHash: load → optional no-op if last entry equals new hash → push → slice(-CAPACITY) → save with version field. Persist payload shape: { version, byType }. Never read/write MASTERY_STORAGE_KEY or daily STORAGE_KEY (D-14).

    Write __tests__/lib/storage/playedHashStorage.test.ts first (RED→GREEN): cases from behavior block; AsyncStorage.clear in beforeEach.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=playedHashStorage --coverage=false</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "PLAYED_HASH_STORAGE_KEY|PLAYED_HASH_RING_CAPACITY|AVOID_HASH_MAX_ATTEMPTS" constants/config.ts` matches all three constants
    - `test -f lib/storage/playedHashStorage.ts`
    - `rg -n "export async function (loadPlayedHashes|savePlayedHashes|appendPlayedHash|clearPlayedHashes)" lib/storage/playedHashStorage.ts` shows four exports
    - `npm test -- --testPathPattern=playedHashStorage --coverage=false` exits 0
    - `rg -n "export const STORAGE_VERSION = 2" constants/config.ts` still matches
    - `rg -n "export const MASTERY_STORAGE_VERSION = 1" constants/config.ts` still matches
  </acceptance_criteria>
  <done>Played-hash ring CRUD + FIFO 200 + safe empty migrate exist and are unit-tested (DIV-01, D-01, D-03, D-12, D-14).</done>
</task>

<task type="auto">
  <name>Task 2: Document played-hash key in CONFIGURATION.md</name>
  <files>docs/CONFIGURATION.md</files>
  <read_first>
    - docs/CONFIGURATION.md (Mastery key inventory ~208–209, AsyncStorage key table ~270, Storage version bumps table ~280–294)
    - constants/config.ts (new PLAYED_HASH_* values from Task 1)
  </read_first>
  <action>
    Document the new store only: add PLAYED_HASH_STORAGE_KEY / VERSION / RING_CAPACITY (and AVOID_HASH_MAX_ATTEMPTS if the constants table lists retry caps) to the constants inventory; add `@foolish-you/played-hash-v1` to the AsyncStorage key table pointing at playedHashStorage.ts; add a Storage version bumps row for Played-hash ring → PLAYED_HASH_STORAGE_VERSION → playedHashStorage.ts + playedHashStorage.test.ts. Explicit note: introducing this ring must not bump STORAGE_VERSION or MASTERY_STORAGE_VERSION (D-12). No SHIP-01 board-drift release notes (deferred to v2.5-06).
  </action>
  <verify>
    <automated>rg -n "played-hash-v1|PLAYED_HASH_STORAGE_VERSION|playedHashStorage" docs/CONFIGURATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "played-hash-v1" docs/CONFIGURATION.md` is ≥ 1
    - `rg -c "PLAYED_HASH_STORAGE_VERSION" docs/CONFIGURATION.md` is ≥ 1
    - `rg -n "Played-hash|playedHashStorage" docs/CONFIGURATION.md` shows a bumps-table row
  </acceptance_criteria>
  <done>CONFIGURATION.md inventories the new key and bump discipline without daily/mastery version bumps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → playedHashStorage normalize | Untrusted persisted JSON may be corrupt, oversized, or newer-version |
| playedHashStorage → callers | Callers receive validated in-memory rings only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v25-05-01 | Tampering | playedHashStorage normalize | medium | mitigate | Explicit field copy; drop non-string hashes; version &gt; app → empty rings (D-14) |
| T-v25-05-02 | Denial of service | ring arrays | medium | mitigate | Cap each type at PLAYED_HASH_RING_CAPACITY (200) on normalize and append |
| T-v25-05-03 | Information disclosure | puzzleHash in AsyncStorage | low | accept | Content fingerprint only; offline local device; not a secret |
| T-v25-05-SC | Tampering | npm installs | high | accept | No new packages this plan — N/A |
</threat_model>

<verification>
- `npm test -- --testPathPattern=playedHashStorage --coverage=false`
- Confirm STORAGE_VERSION=2 and MASTERY_STORAGE_VERSION=1 unchanged
</verification>

<success_criteria>
- DIV-01 storage foundation landed: 200 FIFO/type, safe empty migrate, separate key (D-01, D-03, D-12, D-14)
- AVOID_HASH_MAX_ATTEMPTS exported for wave-2 selector loop
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Produced by |
|----------|------|-------------|
| Config constants | `constants/config.ts` | Plan 01 |
| Played-hash storage module | `lib/storage/playedHashStorage.ts` | Plan 01 |
| Storage unit tests | `__tests__/lib/storage/playedHashStorage.test.ts` | Plan 01 |
| Config docs row | `docs/CONFIGURATION.md` | Plan 01 |

<output>
Create `.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-01-SUMMARY.md` when done
</output>
