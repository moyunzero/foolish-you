---
phase: v2.5-05-dedupe-diversity-wire
plan: 03
type: execute
wave: 3
depends_on:
  - v2.5-05-01
  - v2.5-05-02
files_modified:
  - contexts/DailyGameContext.tsx
  - __tests__/lib/mastery/freezeMastery.test.ts
  - __tests__/lib/storage/playedHashCompletePath.test.ts
autonomous: true
requirements:
  - DIV-01
  - DIV-02
user_setup: []
must_haves:
  truths:
    - "On completed persistStatus, after existing mastery save, appendPlayedHash(gameType, puzzleHash) runs in the same flow with independent try/catch warn (D-02, D-13, DIV-01, SC-2)"
    - "On abandoned persistStatus, mastery still updates but appendPlayedHash is not called (D-02)"
    - "Same dateKey rehydrate keeps puzzleHash after mastery mutate and after appendPlayedHash of an unrelated hash (D-10, MAST-04 freeze regression)"
    - "Mastery write failure does not roll back completion UX; hash append failure does not roll back mastery (D-13)"
  artifacts:
    - path: contexts/DailyGameContext.tsx
      provides: "complete → mastery then appendPlayedHash; abandon → mastery only"
    - path: __tests__/lib/mastery/freezeMastery.test.ts
      provides: "MAST-04 + ring mutate freeze regression"
    - path: __tests__/lib/storage/playedHashCompletePath.test.ts
      provides: "Sequential mastery→append contract + abandon must not append"
  key_links:
    - from: "persistStatus completed branch"
      to: "appendPlayedHash"
      via: "after saveMasteryState try/catch (D-13)"
    - from: "persistStatus abandoned branch"
      to: "saveMasteryState only"
      via: "no appendPlayedHash call (D-02)"
    - from: "hydrateDailyGame same dateKey"
      to: "unchanged puzzleHash"
      via: "freeze short path despite ring growth (MAST-04)"
---

<objective>
On complete, sequentially persist mastery then append today’s puzzleHash to the played-hash ring; never append on abandon; extend freeze regression so same-day rehydrate stays stable after ring writes (D-02, D-10, D-13, MAST-04, SC-2).

Purpose: Close DIV complete-path persistence and lock MAST-04 after full hydrate+selector wire.
Output: Context dual-write + freeze/complete-path tests green; phase verification gate ready.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-CONTEXT.md
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-RESEARCH.md
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-PATTERNS.md
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-01-SUMMARY.md
@.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-02-SUMMARY.md
@contexts/DailyGameContext.tsx
@lib/storage/playedHashStorage.ts
@__tests__/lib/mastery/freezeMastery.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete dual-write appendPlayedHash in Context</name>
  <files>contexts/DailyGameContext.tsx</files>
  <read_first>
    - contexts/DailyGameContext.tsx (persistStatus completed/abandoned mastery try/catch ~399–434)
    - lib/storage/playedHashStorage.ts (appendPlayedHash)
    - .planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-PATTERNS.md (Pattern 5 complete dual-write)
  </read_first>
  <action>
    Import appendPlayedHash from lib/storage/playedHashStorage. In persistStatus when nextStatus === 'completed', after the existing mastery saveMasteryState try/catch, add a separate try/catch that awaits appendPlayedHash(updated.gameType, updated.puzzleHash) and console.warn on failure with a distinct message prefix for played-hash (D-13, SC-2). Do not roll back snapshot completion, mastery, or UX on hash failure. In the abandoned branch, leave mastery update as-is and do not call appendPlayedHash (D-02). No other Context state fields for the ring.
  </action>
  <verify>
    <automated>rg -n "appendPlayedHash" contexts/DailyGameContext.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "appendPlayedHash" contexts/DailyGameContext.tsx` shows import + call
    - Call site sits only under the completed branch (after mastery try/catch); abandoned branch has no appendPlayedHash invocation
    - `npm run typecheck` exits 0 after the edit
  </acceptance_criteria>
  <done>Complete path appends hash after mastery; abandon does not (D-02, D-13, DIV-01, SC-2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze + complete-path regression tests</name>
  <files>__tests__/lib/mastery/freezeMastery.test.ts, __tests__/lib/storage/playedHashCompletePath.test.ts</files>
  <read_first>
    - __tests__/lib/mastery/freezeMastery.test.ts (existing MAST-04 case)
    - lib/daily/dailyHydrate.ts (freeze short path)
    - lib/storage/playedHashStorage.ts
    - lib/storage/masteryStorage.ts
    - contexts/DailyGameContext.tsx (completed vs abandoned branches after Task 1)
  </read_first>
  <behavior>
    - After hydrate create, appendPlayedHash(gameType, 'fake-other-hash') then same-day hydrateDailyGame → puzzleHash unchanged (D-10, MAST-04)
    - Existing mastery-mutate freeze case remains green
    - Sequential contract: saveMasteryState then appendPlayedHash leaves both stores updated; simulating abandon-only mastery write leaves ring unchanged for that puzzleHash (D-02, D-13)
  </behavior>
  <action>
    Extend freezeMastery.test.ts: after create, call appendPlayedHash(created.gameType, 'fake-other-hash') in addition to (or after) mastery mutate; assert rehydrate puzzleHash equals original (D-10, MAST-04). Keep fixed DATE_KEY.

    Add __tests__/lib/storage/playedHashCompletePath.test.ts: (1) clear storage; saveMasteryState(applyMasteryOutcome(...)); appendPlayedHash(type, hash); reload both — mastery updated and ring contains hash at end (SC-2 sequential contract). (2) Start from empty ring; apply only abandon-style mastery save without append — loadPlayedHashes ring for that type does not contain the puzzle hash (D-02). Do not mount full React Context unless already patterned elsewhere; storage-level contract is sufficient when paired with Context source acceptance in Task 1.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='freezeMastery|playedHashCompletePath|playedHashStorage|dailySelector' --coverage=false</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "appendPlayedHash|fake-other-hash" __tests__/lib/mastery/freezeMastery.test.ts` matches ring mutate freeze case
    - `test -f __tests__/lib/storage/playedHashCompletePath.test.ts`
    - Verify automated command exits 0
    - Full gate: `npm run typecheck && npm test && npm run test:migration && npm run lint` exits 0 before claiming phase plan done
  </acceptance_criteria>
  <done>MAST-04 freeze holds after ring writes; complete vs abandon hash behavior covered (D-02, D-10, D-13, MAST-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Complete UX → dual AsyncStorage writes | Two independent keys; partial failure allowed |
| Ring append ← puzzleHash from snapshot | Trusted in-app snapshot field, still validated as string on normalize |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v25-05-07 | Tampering | partial dual-write | medium | mitigate | Independent try/catch; no cross-key rollback that could wipe mastery (D-13, D-14) |
| T-v25-05-08 | Repudiation | missing append on complete | low | mitigate | Tests assert append after mastery; Context call only on completed |
| T-v25-05-09 | Denial of service | append on every abandon | low | mitigate | D-02: abandon skips append — preserves ring budget for real completions |
| T-v25-05-SC | Tampering | npm installs | high | accept | No new packages — N/A |
</threat_model>

<verification>
- `npm test -- --testPathPattern='freezeMastery|playedHashCompletePath|playedHashStorage|dailySelector' --coverage=false`
- Full AGENTS gate: `npm run typecheck && npm test && npm run test:migration && npm run lint`
- Frontend paths touched (`contexts/`): run project frontend-code-review skill on changed Context before claiming push-ready
</verification>

<success_criteria>
- SC-2: complete persists mastery and hash ring sequentially
- MAST-04: same-day freeze regression green with ring mutation
- D-02: abandon does not consume ring slots
- Phase goal observable: avoid + mastery wire + complete dual persist
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Produced by |
|----------|------|-------------|
| Context complete dual-write | `contexts/DailyGameContext.tsx` | Plan 03 |
| Freeze regression (ring) | `__tests__/lib/mastery/freezeMastery.test.ts` | Plan 03 |
| Complete-path storage tests | `__tests__/lib/storage/playedHashCompletePath.test.ts` | Plan 03 |

### Phase-wide artifacts (all plans)

| Artifact | Path | Plan |
|----------|------|------|
| Config + playedHashStorage | `constants/config.ts`, `lib/storage/playedHashStorage.ts` | 01 |
| Selector + hydrate wire | `lib/puzzles/dailySelector.ts`, `lib/daily/dailyHydrate.ts` | 02 |
| Complete + freeze tests | Context + freeze/complete tests | 03 |

<output>
Create `.planning/phases/v2.5-05-dedupe-diversity-wire/v2.5-05-03-SUMMARY.md` when done
</output>
