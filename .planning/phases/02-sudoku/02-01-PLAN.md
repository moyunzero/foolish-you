---
phase: 02-sudoku
plan: "01"
type: execute
wave: 1
depends_on: ["01-02", "01-03"]
files_modified:
  - constants/config.ts
  - lib/puzzles/types.ts
  - lib/puzzles/dailySelector.ts
  - lib/puzzles/sudoku/generator.ts
  - lib/puzzles/sudoku/solver.ts
  - lib/puzzles/sudoku/validate.ts
  - lib/puzzles/sudoku/hash.ts
  - contexts/DailyGameContext.tsx
  - __tests__/lib/puzzles/sudoku/generator.test.ts
  - __tests__/lib/puzzles/sudoku/validate.test.ts
  - __tests__/lib/puzzles/dailySelector.test.ts
autonomous: true
requirements:
  - SUDO-04
  - DAILY-05
  - STOR-01
  - STOR-02
user_setup: []
must_haves:
  truths:
    - "相同 seed 生成相同 givens 与 puzzleHash"
    - "生成的谜题 countSolutions(givens) === 1"
    - "约 30 个已知数（可配置常量）"
    - "dailySelector 在 sudoku 日返回真实 puzzle，非 stub-v1"
    - "同日 hydrate 不重跑生成器（有 puzzle + playState 则复用）"
    - "lib/puzzles/sudoku 不 import react"
  artifacts:
    - "lib/puzzles/sudoku/generator.ts"
    - "lib/puzzles/sudoku/solver.ts"
    - "lib/puzzles/sudoku/validate.ts"
    - "lib/puzzles/types.ts"
  key_links:
    - from: "lib/puzzles/dailySelector.ts"
      to: "lib/puzzles/sudoku/generator.ts"
      via: "generateSudokuPuzzle"
      pattern: "generateSudokuPuzzle"
    - from: "contexts/DailyGameContext.tsx"
      to: "lib/storage/dailyStorage.ts"
      via: "saveDailySnapshot"
      pattern: "playState"
---

<objective>
实现数独纯逻辑层：确定性生成、唯一解验证、冲突/完成判定，并接入 dailySelector 与 DailyGameContext（题面 + playState 持久化）。

Purpose: UI 计划（02-02）只消费类型与纯函数，不在屏内写规则。
Output: 可 `npm test` 验证的 `lib/puzzles/sudoku/*` + 扩展 snapshot 类型。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-sudoku/02-CONTEXT.md
@.planning/phases/02-sudoku/02-RESEARCH.md
@.planning/research/PITFALLS.md
@lib/puzzles/rng.ts
@lib/puzzles/dailySelector.ts
@contexts/DailyGameContext.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 类型 + 数独核心算法</name>
  <files>lib/puzzles/types.ts, constants/config.ts, lib/puzzles/sudoku/solver.ts, lib/puzzles/sudoku/validate.ts, lib/puzzles/sudoku/hash.ts, lib/puzzles/sudoku/generator.ts, __tests__/lib/puzzles/sudoku/generator.test.ts, __tests__/lib/puzzles/sudoku/validate.test.ts</files>
  <read_first>.planning/phases/02-sudoku/02-RESEARCH.md, .planning/research/PITFALLS.md</read_first>
  <behavior>
    - `generateSudokuPuzzle(12345)` 两次调用 `givens` 深度相等
    - 已知数数量 === `SUDOKU_GIVEN_COUNT`（默认 30）
    - `countSolutions(puzzle, 2) === 1`
    - `getConflictCells` 对同行重复返回两格坐标
    - `isCompleteAndValid` 在全对填满时为 true，缺一为 false
  </behavior>
  <action>
    **constants/config.ts** 新增：
    - `SUDOKU_GIVEN_COUNT = 30`
    - `SUDOKU_MAX_GEN_ATTEMPTS = 50`

    **types.ts** 扩展：
    ```ts
    export type SudokuGivens = number[][]; // 9x9, 0=空 1-9=给定
    export type SudokuPlayState = number[][]; // 9x9, 0=空 1-9=用户填
    export type SudokuPuzzle = {
      kind: 'sudoku';
      givens: SudokuGivens;
      puzzleHash: string;
    };
    export type PuzzlePayload = SudokuPuzzle | { kind: 'binary'; placeholder: true };
  ```
    更新 `DailySnapshot`：`puzzle: PuzzlePayload`；`playState?: SudokuPlayState`（仅 sudoku playing 时）。移除或弃用 `puzzleStub.placeholder` 路径（迁移见 Task 3）。

    **solver.ts：** `solve(grid): boolean`；`countSolutions(grid, limit): number`。

    **validate.ts：**
    - `getConflictCells(play: SudokuPlayState, givens: SudokuGivens): Array<{row,col}>`
    - `isCompleteAndValid(play, givens): boolean`（满格 + 无冲突 + 与 givens 已知格一致）

    **hash.ts：** `computePuzzleHash(givens): string`（稳定字符串，如 givens flat join + simple hash）。

    **generator.ts：** `generateSudokuPuzzle(seed: number): SudokuPuzzle` — 按 RESEARCH 管线；失败递增 seed 重试。

    先写测试再实现。测试在 Node 跑，无 RN。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npm test -- --testPathPattern="sudoku" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - generator.test.ts 含确定性 + 已知数数量 + 唯一解
    - validate.test.ts 含冲突与完成判定
  </acceptance_criteria>
  <done>npm test sudoku 相关全绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dailySelector 接入真实数独</name>
  <files>lib/puzzles/dailySelector.ts, __tests__/lib/puzzles/dailySelector.test.ts</files>
  <read_first>lib/puzzles/sudoku/generator.ts, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - 固定 dateKey + seed，两次 `selectDailyGame` 若 gameType 为 sudoku，则 `puzzleHash` 相同且非 stub-v1
    - 连续两日 `puzzleHash` 不同（在 gameType 同为 sudoku 的 forced 测试用例中）
  </behavior>
  <action>
    更新 `SelectDailyGameResult`：
    - `puzzle: PuzzlePayload`
    - `puzzleHash: string`

    当 `gameType === 'sudoku'`：
    - `const sudokuSeed = deriveSubSeed(seed, 'sudoku')`
    - `puzzle = generateSudokuPuzzle(sudokuSeed)`

    当 `gameType === 'binary'`：
    - 保持 `{ kind: 'binary', placeholder: true }`，`puzzleHash: 'binary-stub-v1'`

    DAILY-05：若 `previous?.puzzleHash === puzzle.puzzleHash`，递增子种子重选类型或重生成（与 Phase 1 逻辑一致，扩展到真实 hash）。

    更新 dailySelector 测试；移除对 `STUB_PUZZLE_HASH` 的 sudoku 断言。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npm test -- --testPathPattern="dailySelector" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - sudoku 日 puzzleHash 非 stub-v1
    - binary 日仍为 stub
  </acceptance_criteria>
  <done>dailySelector 测试通过</done>
</task>

<task type="auto">
  <name>Task 3: DailyGameContext — 题面、playState、迁移</name>
  <files>contexts/DailyGameContext.tsx</files>
  <read_first>.planning/phases/02-sudoku/02-CONTEXT.md, lib/puzzles/types.ts</read_first>
  <action>
    **buildNewDaily：**
    - 使用更新后的 `selectDailyGame` 写入 `puzzle` + `puzzleHash`
    - 若 `gameType === 'sudoku'`，初始化 `playState` 为全 0 的 9×9（或从 givens 拷贝仅用户格为空）

    **hydrate 迁移：**
    - 若今日 record 含 `puzzleStub?.placeholder` 且 `gameType === 'sudoku'`：用 `seed` 生成真实 `puzzle`，保留 `status`/`playState`（无则初始化），`saveDailySnapshot`

    **暴露给 UI（02-02 使用）：**
    ```ts
    updatePlayState: (next: SudokuPlayState) => Promise<void>  // 内部防抖 ~300ms 写 storage
    puzzle: PuzzlePayload | null  // 从 snapshot
    playState: SudokuPlayState | null
    ```
    实现 `useRef` + `setTimeout` 防抖；`markCompleted`/`markAbandoned` 前 flush 待写入。

    **不**在本 task 实现 SudokuGrid（属 02-02）。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npm test 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - DailyGameContext 导出 updatePlayState、puzzle、playState
    - TypeScript 编译无 error（npx tsc --noEmit 若项目已配置）
  </acceptance_criteria>
  <done>Context API 可供 game 屏消费；lib 测试仍全绿</done>
</task>

</tasks>

<verification>
- `npm test` 全部通过
- `grep -r "from 'react'" lib/puzzles/sudoku` 无匹配
- `grep -r "Math.random" lib/puzzles/sudoku` 无匹配
- `grep "stub-v1" lib/puzzles/dailySelector.ts` 不对 sudoku 路径返回常量（binary 可保留）
</verification>

<success_criteria>
- 数独生成与校验可离线单测
- dailySelector 在数独日产出真实题面与 hash
- Context 支持 playState 持久化与 placeholder 迁移
</success_criteria>

<output>
完成后创建 `.planning/phases/02-sudoku/02-01-SUMMARY.md`
</output>
