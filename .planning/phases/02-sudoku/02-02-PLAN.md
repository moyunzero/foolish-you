---
phase: 02-sudoku
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/grid/SudokuGrid.tsx
  - components/grid/SudokuNumpad.tsx
  - app/game.tsx
  - constants/design.ts
autonomous: false
requirements:
  - SUDO-01
  - SUDO-02
  - SUDO-03
user_setup: []
must_haves:
  truths:
    - "数独日可点格选数字、清除用户填入"
    - "已知格不可编辑"
    - "冲突格实时可见（accent-sunset）"
    - "合法填满后「完成今日」可点并进入 /result"
    - "「今天不玩了」仍 markAbandoned → /result"
    - "杀进程重开可恢复 playState"
  artifacts:
    - "components/grid/SudokuGrid.tsx"
    - "components/grid/SudokuNumpad.tsx"
    - "app/game.tsx"
  key_links:
    - from: "app/game.tsx"
      to: "components/grid/SudokuGrid.tsx"
      via: "import"
      pattern: "SudokuGrid"
    - from: "app/game.tsx"
      to: "contexts/DailyGameContext.tsx"
      via: "updatePlayState|markCompleted"
      pattern: "useDailyGame"
---

<objective>
实现可玩的数独 UI：9×9 网格、数字条、完成/放弃，接入 DailyGameContext。

Purpose: 满足 ROADMAP Phase 2 成功标准 1–4 与手测闭环。
Output: Expo Go 可玩的数独分支；binary 日保留占位。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/02-sudoku/02-CONTEXT.md
@.planning/phases/02-sudoku/02-UI-SPEC.md
@.planning/phases/02-sudoku/02-RESEARCH.md
@DESIGN.md
@app/game.tsx
@contexts/DailyGameContext.tsx
@lib/puzzles/sudoku/validate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SudokuGrid + SudokuNumpad</name>
  <files>components/grid/SudokuGrid.tsx, components/grid/SudokuNumpad.tsx, constants/design.ts</files>
  <read_first>.planning/phases/02-sudoku/02-UI-SPEC.md, DESIGN.md, components/ui/OutlinePillButton.tsx</read_first>
  <action>
    **SudokuGrid props：**
    ```ts
    givens: SudokuGivens;
    playState: SudokuPlayState;
    selected: { row: number; col: number } | null;
    conflictCells: Array<{ row: number; col: number }>;
    onSelectCell: (row: number, col: number) => void;
    ```

    - 9×9 `Pressable` 格；3×3 宫格粗线（Tailwind border）
    - 已知格：`font-semibold`，忽略 onSelect 改值（父组件控制）
    - 冲突格：`border-accent-sunset` 或浅底（`constants/design.ts` 确认 token 名与 tailwind 一致）
    - `accessibilityLabel`  per UI-SPEC

    **SudokuNumpad：**
    - 1–9 横排 outline 按钮（可复用 `OutlinePillButton` 小号或内联样式）
    - 「清除」按钮
    - `disabled` prop：无选中可编辑格时禁用

    父组件负责：选中态、`getConflictCells` 调用、数字写入 `playState`。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npx tsc --noEmit 2>&1 || true</automated>
  </verify>
  <acceptance_criteria>
    - SudokuGrid 渲染 81 格且已知数来自 givens
    - Numpad 暴露 onDigit / onClear
  </acceptance_criteria>
  <done>组件可在 Story/game 屏引用（无业务线）</done>
</task>

<task type="auto">
  <name>Task 2: game.tsx 数独分支 + 完成流</name>
  <files>app/game.tsx</files>
  <read_first>contexts/DailyGameContext.tsx, lib/puzzles/sudoku/validate.ts</read_first>
  <action>
    当 `gameType === 'sudoku'` 且 `puzzle.kind === 'sudoku'`：

    1. 本地 state：`selectedCell`；`playState` 来自 context（或本地 mirror + `updatePlayState`）
    2. 每次 `playState` 变更 → `getConflictCells(playState, givens)`
    3. 点格：若非常数格 → 更新 selected
    4. 点数字：写入 selected 格（1–9）；点清除 → 置 0
    5. `canComplete = isCompleteAndValid(playState, givens)`
    6. **完成今日**（`OutlinePillButton` variant primary 或 UI-SPEC 白底胶囊）：`disabled={!canComplete}` → `markCompleted()` → `router.replace('/result')`
    7. **今天不玩了**：保持现有 `markAbandoned` 流
    8. 移除 disabled 的「先看看题目」

    当 `gameType === 'binary'`：保留 Phase 1 虚线占位，文案可改为「二进制谜题 · Phase 3 开放」。

    若 `status !== 'playing'` 不应停留 game（由 index Redirect 保证）；game 内可加 guard。

    布局遵循 02-UI-SPEC 与 01-UI-SPEC 边距。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/foolish-you && npx expo export --platform ios 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - game.tsx 含 SudokuGrid 条件渲染
    - 完成按钮绑定 isCompleteAndValid
  </acceptance_criteria>
  <done>模拟器/Expo Go 可交互数独（待 checkpoint）</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 手测 — 数独完整一局</name>
  <what-built>可玩数独 + 完成/放弃 + 续玩</what-built>
  <how-to-verify>
    1. `npx expo start`，Expo Go 打开项目
    2. 若今日非数独：改系统日期或清 AsyncStorage 直到 `gameType === 'sudoku'`（或临时在 dev 强制类型，测完还原）
    3. 填若干格 → 观察冲突高亮（故意填重复）
    4. 杀进程重开 → 进度仍在
    5. 合法填满 →「完成今日」→ `/result`
    6. 新日或清存储再测「今天不玩了」→ `/result` 且当日不可再玩
    7. 连续两天数独日：盘面不同（DAILY-05 肉眼或 log puzzleHash）
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx expo export --platform ios` 通过
- 手测 checkpoint approved
</verification>

<success_criteria>
- ROADMAP Phase 2 成功标准 1–5 经手测确认
- SUDO-01～03 UI 行为符合 CONTEXT
</success_criteria>

<output>
完成后创建 `.planning/phases/02-sudoku/02-02-SUMMARY.md`
</output>
