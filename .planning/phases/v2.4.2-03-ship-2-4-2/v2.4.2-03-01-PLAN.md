---
phase: v2.4.2-03-ship-2-4-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - package.json
  - package-lock.json
  - README.md
  - README.zh-CN.md
  - docs/CONFIGURATION.md
  - docs/GETTING-STARTED.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SHIP-01
  - SHIP-03
user_setup: []
must_haves:
  truths:
    - "Marketing version string is 2.4.2 on app.json expo.version, package.json version, and package-lock top/packages empty-key version (SHIP-03, D-01)"
    - "ios.buildNumber stays 30 and android.versionCode stays 3 in app.json (D-02)"
    - "Store narrative everywhere says live 2.4.1 and shipping/pending 2.4.2 — never pending 2.4.1 or live-only 2.4.0 (D-03)"
    - "CONFIGURATION version table and App Store status row match 2.4.2 / Live 2.4.1 · shipping 2.4.2; buildNumber prose reflects current 30 without editing build fields (D-04)"
    - "New 2.4.2 What's New zh+en blocks have exactly three feature bullets then one separate drift disclosure stating unplayed dates may change and in-progress progress is kept (SHIP-01, D-05..D-08)"
    - "Prior 2.4.1 What's New heading is marked 已上架; new 2.4.2 block is 待提交 inserted above it (D-08)"
    - "REQUIREMENTS NONO-01 wording is exactly 120 with tier hist 17/17/17/17/17/17/18 (D-16)"
    - "No app/, components/, locales/, APP_SALT, dailySelector, or generator edits (UI-SPEC + phase boundary)"
  artifacts:
    - path: app.json
      provides: "expo.version 2.4.2; native build fields unchanged"
    - path: package.json
      provides: "npm package version 2.4.2"
    - path: package-lock.json
      provides: "lockfile root version 2.4.2"
    - path: docs/CONFIGURATION.md
      provides: "Version table + 2.4.2 What's New zh/en paste blocks + 2.4.1 promoted"
    - path: README.md
      provides: "Badge + live/shipping narrative for 2.4.2"
    - path: README.zh-CN.md
      provides: "Badge + 线上/待提交 narrative for 2.4.2"
    - path: .planning/REQUIREMENTS.md
      provides: "NONO-01 exact 120 + hist wording"
  key_links:
    - from: "app.json expo.version"
      to: "package.json version + package-lock root"
      via: "same marketing string 2.4.2"
    - from: "docs/CONFIGURATION.md What's New 2.4.2"
      to: "App Store Connect paste (human, post-phase)"
      via: "fenced zh/en · bullets; D-05 disclosure last"
    - from: "REQUIREMENTS NONO-01"
      to: "__tests__/lib/puzzles/nonogram/patterns.test.ts"
      via: "docs catch-up to exact 120 + hist already enforced in tests"
---

<objective>
Bump marketing version to `2.4.2` across store/metadata surfaces and write bilingual App Store What's New that discloses unplayed-date board drift while keeping in-progress progress (SHIP-01, SHIP-03, D-01..D-08, D-16).

Purpose: Make the repo's store-facing version and disclosure text match locked product facts before QA sign-off.
Output: Version-aligned metadata + CONFIGURATION What's New paste blocks + NONO-01 exact wording.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-CONTEXT.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-RESEARCH.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-PATTERNS.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-UI-SPEC.md
@docs/CONFIGURATION.md
@app.json
@eas.json
</context>

## Artifacts this phase produces

| Symbol / artifact | File | Notes |
|-------------------|------|-------|
| Marketing `2.4.2` | `app.json` / `package.json` / `package-lock.json` | `expo.version` + npm version only |
| Store narrative | `README.md` / `README.zh-CN.md` / `docs/GETTING-STARTED.md` / `CLAUDE.md` | Live `2.4.1` · shipping `2.4.2` |
| What's New paste | `docs/CONFIGURATION.md` | zh/en · bullets; features then disclosure |
| NONO-01 wording | `.planning/REQUIREMENTS.md` | exact 120 + hist 17×6+18 |

<tasks>

<task type="auto">
  <name>Task 1: Marketing version 2.4.2 + store narrative (D-01..D-03)</name>
  <files>app.json, package.json, package-lock.json, README.md, README.zh-CN.md, docs/GETTING-STARTED.md, CLAUDE.md</files>
  <read_first>
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-CONTEXT.md` (D-01, D-02, D-03)
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-PATTERNS.md` (marketing three-pack + README rewrite targets)
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-UI-SPEC.md` (Phase UI Scope Lock — no app/components/locales)
    - `app.json` (confirm current `expo.version`, `ios.buildNumber`, `android.versionCode`)
    - `eas.json` (confirm `appVersionSource: local` + production `autoIncrement` — do not edit)
    - `README.md` / `README.zh-CN.md` badge + App Store lines + KPI/milestone tables mentioning live/shipping versions
    - `docs/GETTING-STARTED.md` end-user live/repo line
    - `CLAUDE.md` Scope marketing version clause
  </read_first>
  <action>
    1. Per D-01 / SHIP-03: set marketing version string to `2.4.2` in `app.json` `expo.version`, `package.json` `version`, and `package-lock.json` top-level `version` plus `packages[""].version` only. Do not rewrite transitive dependency versions.
    2. Per D-02: leave `app.json` `expo.ios.buildNumber` and `expo.android.versionCode` exactly as found (currently `"30"` and `3`). Do not edit `eas.json`.
    3. Per D-03: rewrite store narrative so live/online is `2.4.1` and shipping/pending is `2.4.2` in `README.md`, `README.zh-CN.md`, `docs/GETTING-STARTED.md`, and `CLAUDE.md` Scope line. Update version badges to `version-2.4.2`. Update body/KPI/milestone rows that still claim live `2.4.0` or shipping/pending `2.4.1`.
    4. Do not touch `app/`, `components/`, `hooks/`, `contexts/`, `locales/`, `constants/config.ts` (`APP_SALT`), `lib/puzzles/`, or any puzzle/UI product code (UI-SPEC + phase boundary).
  </action>
  <acceptance_criteria>
    - `rg -n '"version": "2\.4\.2"' app.json package.json package-lock.json` matches marketing fields
    - `node -e "const a=require('./app.json'); if(a.expo.version!=='2.4.2') process.exit(1); if(a.expo.ios.buildNumber!=='30') process.exit(2); if(a.expo.android.versionCode!==3) process.exit(3)"` exits 0
    - `rg -n 'version-2\.4\.2' README.md README.zh-CN.md` matches both badges
    - `rg -n 'Live.*2\.4\.1|shipping.*2\.4\.2|线上.*2\.4\.1|待提交.*2\.4\.2' README.md README.zh-CN.md docs/GETTING-STARTED.md CLAUDE.md` matches each file at least once for the new narrative
    - Stale pending-2.4.1 / live-only-2.4.0 store claims removed from those four narrative files (verify with `rg -n 'shipping \*\*2\.4\.1\*\*|待提交 \*\*2\.4\.1\*\*|live \`2\.4\.0\`|线上 \`2\.4\.0\`' README.md README.zh-CN.md docs/GETTING-STARTED.md CLAUDE.md` returning no matches)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const a=require('./app.json'); const p=require('./package.json'); if(a.expo.version!=='2.4.2'||p.version!=='2.4.2') process.exit(1); if(a.expo.ios.buildNumber!=='30'||a.expo.android.versionCode!==3) process.exit(2)" && rg -n 'version-2\.4\.2' README.md README.zh-CN.md</automated>
  </verify>
  <done>
    Marketing surfaces say 2.4.2; native build fields untouched; live 2.4.1 / shipping 2.4.2 narrative consistent across README×2, GETTING-STARTED, CLAUDE (D-01..D-03, SHIP-03).
  </done>
</task>

<task type="auto">
  <name>Task 2: CONFIGURATION What's New + NONO-01 exact wording (D-04..D-08, D-16, SHIP-01)</name>
  <files>docs/CONFIGURATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-CONTEXT.md` (D-04..D-08, D-16)
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-UI-SPEC.md` Copywriting Contract (locked bullet order + D-05 semantics)
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-RESEARCH.md` Recommended What's New skeleton
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-PATTERNS.md` CONFIGURATION What's New lifecycle + NONO-01 replace text
    - `docs/CONFIGURATION.md` version table, store status row, existing `2.4.1` What's New block
    - `.planning/REQUIREMENTS.md` NONO-01 line
  </read_first>
  <action>
    1. Per D-04: in `docs/CONFIGURATION.md` set the Version table marketing cell to `2.4.2` and the iOS App Store status row to Live `2.4.1` · shipping `2.4.2`. Update buildNumber prose to state current value is `30` and next EAS production autoIncrement is `31` — edit prose only; never change `app.json` build fields (D-02 / Pitfall 2).
    2. Per D-08: rename the existing `2.4.1` What's New heading from 待提交 to 已上架; keep its Composition feature bullets intact.
    3. Per D-05..D-07 / SHIP-01 / UI-SPEC: insert a new `2.4.2` · 待提交 What's New section above the promoted `2.4.1` block, with bilingual fenced paste blocks (中文 · 傻了么 / English · Brainfool). Exactly three feature `·` bullets in locked topic order (sudoku/binary week rhythm → slitherlink wider stretch → nonogram library to 120), then exactly one separate disclosure bullet last that states unplayed dates may change after update and that in-progress progress is kept. Claude may polish wording to match historical CONFIGURATION tone; D-05 semantics are non-negotiable. Use RESEARCH/UI-SPEC skeleton as the default draft.
    4. Per D-16: rewrite `.planning/REQUIREMENTS.md` NONO-01 from soft ≈120 / roughly balanced to **exactly 120** and tier hist **17/17/17/17/17/17/18**, append-only prefix locked. Docs-only — do not edit pattern catalog code.
    5. Do not add in-app release-notes UI or edit locales.
  </action>
  <acceptance_criteria>
    - `rg -n 'Version \| \`2\.4\.2\`|Live \`2\.4\.1\`|shipping \`2\.4\.2\`' docs/CONFIGURATION.md` matches
    - `rg -n '2\.4\.2\` What.s New · 待提交|2\.4\.1\` What.s New · 已上架' docs/CONFIGURATION.md` matches both status headings
    - `rg -n '尚未游玩的日期盘面可能变化|进行中的进度会保留' docs/CONFIGURATION.md` matches Chinese disclosure
    - `rg -n 'dates you have not opened yet may change|in-progress progress is kept' docs/CONFIGURATION.md` matches English disclosure
    - `rg -n 'exactly 120|17/17/17/17/17/17/18' .planning/REQUIREMENTS.md` matches NONO-01
    - Soft NONO wording gone: `rg -n '~120|roughly balanced' .planning/REQUIREMENTS.md` returns no matches on the NONO-01 requirement line
  </acceptance_criteria>
  <verify>
    <automated>rg -n '2\.4\.2\` What.s New · 待提交' docs/CONFIGURATION.md && rg -n '尚未游玩的日期盘面可能变化' docs/CONFIGURATION.md && rg -n 'exactly 120' .planning/REQUIREMENTS.md && rg -n '17/17/17/17/17/17/18' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    CONFIGURATION carries paste-ready 2.4.2 What's New with D-05 disclosure last; 2.4.1 marked shipped; NONO-01 docs match exact catalog contract (SHIP-01, D-04..D-08, D-16).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo docs → App Store Connect | Operator pastes What's New; misleading copy becomes public store text |
| Marketing version metadata → EAS/store | Wrong `expo.version` ships wrong user-facing version; build numbers stay EAS-owned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v242-03-01 | Tampering | `APP_SALT` / `dailySelector` / generators | high | mitigate | Explicit out-of-scope; Task 1/2 forbid product/puzzle edits; later plan greps salt |
| T-v242-03-02 | Tampering | `ios.buildNumber` / `android.versionCode` | medium | mitigate | D-02 acceptance: leave `"30"` / `3`; prose-only CONFIGURATION fix |
| T-v242-03-03 | Repudiation | What's New disclosure | medium | mitigate | SHIP-01 / D-05 mandatory zh+en disclosure + in-progress keep; features-first order |
| T-v242-03-04 | Information Disclosure | Store/docs narrative | low | mitigate | D-03 rewrite live `2.4.1` / shipping `2.4.2` so docs match reality |
| T-v242-03-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |
</threat_model>

<verification>
node version checks + rg gates in task acceptance_criteria; full AGENTS CI deferred to plan 02.
</verification>

<success_criteria>
- SHIP-03 surfaces show marketing `2.4.2` with native build fields unchanged
- SHIP-01 What's New zh+en disclose unplayed drift + keep progress (features first)
- D-03 / D-16 narrative and NONO wording aligned
</success_criteria>

<output>
Create `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-01-SUMMARY.md` when done
</output>
