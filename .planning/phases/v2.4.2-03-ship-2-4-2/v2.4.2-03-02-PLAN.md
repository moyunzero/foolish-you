---
phase: v2.4.2-03-ship-2-4-2
plan: 02
type: execute
wave: 2
depends_on:
  - v2.4.2-03-01
files_modified:
  - docs/TESTING.md
  - docs/qa/v2.4.2-ship/README.md
  - docs/qa/v2.4.2-ship/evidence/
  - .maestro/flows/v242/v242-smoke-sudoku.yaml
  - .maestro/flows/v242/v242-smoke-binary.yaml
  - .maestro/flows/v242/v242-smoke-nonogram.yaml
  - .maestro/flows/v242/v242-smoke-slitherlink.yaml
  - scripts/maestro-v242-ship.sh
  - .planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md
autonomous: false
requirements:
  - SHIP-01
  - SHIP-03
user_setup: []
must_haves:
  truths:
    - "docs/qa/v2.4.2-ship/ exists with README preconditions, Maestro commands, and evidence layout (D-12)"
    - "Four Maestro smoke flows enter game for sudoku/binary/nonogram/slitherlink via Dev force buttons (D-09, D-10)"
    - "Evidence directory contains maestro log with MAESTRO_EXIT=0 plus at least one screenshot per type (D-09)"
    - "docs/TESTING.md has a v2.4.2 hand-QA section covering four types, Mon loose, Sun tight, in-progress reopen keep (D-11)"
    - "AGENTS four-command CI green and recorded in VERIFICATION (D-14, ROADMAP SC-3)"
    - "VERIFICATION.md checklist covers SHIP-01/SHIP-03 and D-01..D-16 with human signature after Maestro + hand QA (D-13, D-15)"
    - "Phase complete means repo ready — EAS production / ASC submit explicitly out of scope (D-13)"
    - "No product UI / APP_SALT / selector changes in this plan"
  artifacts:
    - path: docs/qa/v2.4.2-ship/README.md
      provides: "Hand QA + Maestro preconditions and evidence instructions"
    - path: scripts/maestro-v242-ship.sh
      provides: "One-process-per-flow Maestro runner for four smokes"
    - path: .maestro/flows/v242/v242-smoke-sudoku.yaml
      provides: "Sudoku enter-game smoke"
    - path: .maestro/flows/v242/v242-smoke-binary.yaml
      provides: "Binary enter-game smoke"
    - path: .maestro/flows/v242/v242-smoke-nonogram.yaml
      provides: "Nonogram enter-game smoke"
    - path: .maestro/flows/v242/v242-smoke-slitherlink.yaml
      provides: "Slitherlink enter-game smoke"
    - path: docs/TESTING.md
      provides: "v2.4.2 manual QA section linking qa folder + VERIFICATION"
    - path: .planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md
      provides: "Sign-off matrix + CI results + human signature"
  key_links:
    - from: "scripts/maestro-v242-ship.sh"
      to: ".maestro/flows/v242/*-smoke-*.yaml"
      via: "FLOWS array + maestro test --test-output-dir evidence"
    - from: "docs/TESTING.md § v2.4.2"
      to: "docs/qa/v2.4.2-ship/README.md + VERIFICATION.md"
      via: "cross-links for hand QA vs Maestro"
    - from: "Dev force type buttons"
      to: "Maestro smokes"
      via: "reuse v22 subflows; clears archive — keep-reopen is hand-only"
---

<objective>
Prove ship readiness: Maestro four-type smoke evidence, hand-QA checklist (Mon/Sun/four types/keep), AGENTS CI green, and VERIFICATION human sign-off (D-09..D-15) — without EAS production or ASC submit (D-13).

Purpose: Close ROADMAP success criteria 3–4 and lock repo-ready confidence after Plan 01 version/disclosure.
Output: QA folder + four smokes + script + TESTING section + signed VERIFICATION.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-CONTEXT.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-RESEARCH.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-PATTERNS.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-UI-SPEC.md
@.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-01-SUMMARY.md
@.planning/phases/v2.4-sunday-special/v24-03-PLAN.md
@.planning/phases/v2.4-sunday-special/v2.4-VERIFICATION.md
@docs/qa/v2.4-sunday-special/README.md
@docs/TESTING.md
@scripts/maestro-v24-acceptance.sh
@.maestro/flows/v22/subflows/dev-force-sudoku.yaml
@AGENTS.md
</context>

## Artifacts this phase produces

| Symbol / artifact | File | Notes |
|-------------------|------|-------|
| QA README + evidence | `docs/qa/v2.4.2-ship/` | Required Maestro evidence under `evidence/YYYYMMDD/` |
| Four smoke flows | `.maestro/flows/v242/v242-smoke-*.yaml` | One enter-game smoke per type |
| Ship Maestro script | `scripts/maestro-v242-ship.sh` | Mirror v24 one-process-per-flow |
| Hand QA section | `docs/TESTING.md` | § v2.4.2 |
| Sign-off matrix | `v2.4.2-03-VERIFICATION.md` | CI + Maestro + hand QA + signature |

<tasks>

<task type="auto">
  <name>Task 1: QA docs + four Maestro smokes + VERIFICATION scaffold (D-09..D-12, D-15)</name>
  <files>docs/TESTING.md, docs/qa/v2.4.2-ship/README.md, .maestro/flows/v242/v242-smoke-sudoku.yaml, .maestro/flows/v242/v242-smoke-binary.yaml, .maestro/flows/v242/v242-smoke-nonogram.yaml, .maestro/flows/v242/v242-smoke-slitherlink.yaml, scripts/maestro-v242-ship.sh, .planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-CONTEXT.md` (D-09..D-15)
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-PATTERNS.md` (Maestro smoke shape, acceptance script, VERIFICATION sections, TESTING § pattern)
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-RESEARCH.md` (Pitfall 3–5: UDID/Metro, smoke≠Mon/Sun, force clears archive)
    - `docs/qa/v2.4-sunday-special/README.md` (folder structure analog)
    - `docs/TESTING.md` (§ v2.4 and § v2.5-01 insertion points)
    - `scripts/maestro-v24-acceptance.sh` (Metro gate, one flow per process, MAESTRO_EXIT footer)
    - `.maestro/flows/v22/subflows/dev-open-panel.yaml`, `dev-force-sudoku.yaml`, `dev-hide-bar.yaml`
    - `locales/zh/ui.ts` `gameTypes` labels (`数独` / `二进制` / `数绘` / `数回`)
    - `.planning/phases/v2.4-sunday-special/v2.4-VERIFICATION.md` (matrix + CI + signature template)
    - Plan 01 SUMMARY if present (confirm version/What's New landed)
  </read_first>
  <action>
    1. Create `docs/qa/v2.4.2-ship/README.md` mirroring the v2.4 Sunday Special QA README: links to CONTEXT, TESTING § v2.4.2, and `v2.4.2-03-VERIFICATION.md`; environment table (`__DEV__` build, Metro on port 8081, boot Simulator, optional `MAESTRO_UDID`); hand-QA shortest path with separate rows for four types, Monday loose (Simulator Monday dateKey), Sunday tight (`假日期·周日`), and in-progress reopen keep (play cells → kill/relaunch → same board; do not tap Dev force-type mid-check because force regenerates and clears today's archive); Maestro section marking four smokes as required (D-09), not optional; evidence path `docs/qa/v2.4.2-ship/evidence/YYYYMMDD/` requiring acceptance log with `MAESTRO_EXIT=0` plus ≥1 screenshot per type.
    2. Add four YAML flows under `.maestro/flows/v242/`: `v242-smoke-sudoku.yaml`, `v242-smoke-binary.yaml`, `v242-smoke-nonogram.yaml`, `v242-smoke-slitherlink.yaml`. Each: `appId: com.moyunzero.foolish-you`, launchApp clearState, wait for `完成今日`, runFlow `../v22/subflows/dev-open-panel.yaml`, tapOn the matching Dev force label, wait for `完成今日`, optionally runFlow hide-bar, assertVisible the matching `gameTypes` title, takeScreenshot named per type. Do not require Sunday/Monday force inside smokes (D-10 vs D-11).
    3. Add executable `scripts/maestro-v242-ship.sh` adapted from `maestro-v24-acceptance.sh`: default EVIDENCE under `docs/qa/v2.4.2-ship/evidence/$(date +%Y%m%d)`, FLOWS = the four v242 smoke basenames, `maestro test` path `.maestro/flows/v242/${flow}.yaml`, Metro :8081 preflight, terminate app between flows, `MAESTRO_UDID` env override, footer `MAESTRO_EXIT=0` or `1`.
    4. Append a **v2.4.2** section to `docs/TESTING.md` (keep existing v2.4 / v2.5-01 sections) with the ROADMAP hand-QA table (four types ≥1 day, Mon loose / Sun tight, in-progress reopen) and links to `docs/qa/v2.4.2-ship/README.md`, `./scripts/maestro-v242-ship.sh`, and phase VERIFICATION.
    5. Create `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md` with: success/decision checklist for SHIP-01, SHIP-03, and D-01..D-16; CI gate table for the four AGENTS commands (empty until Task 2); invariant greps including `APP_SALT` value `foolish-you-v1` and confirmation this phase did not edit product UI/selector; separate Maestro matrix rows (required) vs hand-QA matrix rows (four types, Mon, Sun, keep); note EAS production / ASC submit are out of phase (D-13); human signature block (D-15) left unsigned until checkpoint.
    6. Do not modify `app/`, `components/`, or `locales/` product UI (UI-SPEC). Frontend-code-review skill does not apply unless those paths are touched (they must not be).
  </action>
  <acceptance_criteria>
    - `test -f docs/qa/v2.4.2-ship/README.md`
    - `test -f .maestro/flows/v242/v242-smoke-sudoku.yaml && test -f .maestro/flows/v242/v242-smoke-binary.yaml && test -f .maestro/flows/v242/v242-smoke-nonogram.yaml && test -f .maestro/flows/v242/v242-smoke-slitherlink.yaml`
    - `test -x scripts/maestro-v242-ship.sh || test -f scripts/maestro-v242-ship.sh`
    - `rg -n 'v2\.4\.2|docs/qa/v2\.4\.2-ship|maestro-v242-ship' docs/TESTING.md` matches
    - `rg -n 'SHIP-01|SHIP-03|D-09|D-11|D-14|APP_SALT' .planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md` matches
    - Each smoke YAML contains `takeScreenshot` and a Dev force tap for its type (`rg -n '数独|二进制|数绘|数回' .maestro/flows/v242/` covers all four labels)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/qa/v2.4.2-ship/README.md && test -f scripts/maestro-v242-ship.sh && test -f .maestro/flows/v242/v242-smoke-sudoku.yaml && test -f .maestro/flows/v242/v242-smoke-slitherlink.yaml && rg -n 'v2\.4\.2' docs/TESTING.md && rg -n 'SHIP-01' .planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md</automated>
  </verify>
  <done>
    QA docs, four smokes, runner script, and VERIFICATION scaffold exist; Maestro required; hand QA distinct from smoke (D-09..D-12, D-15 scaffold).
  </done>
</task>

<task type="auto">
  <name>Task 2: AGENTS CI four-command + invariant greps into VERIFICATION (D-14)</name>
  <files>.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md</files>
  <read_first>
    - `AGENTS.md` Verify before done section
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md` (scaffold from Task 1)
    - `constants/config.ts` (`APP_SALT` baseline)
    - Plan 01 file list / git diff to confirm no product UI paths changed in this phase
  </read_first>
  <action>
    1. Per D-14 / ROADMAP SC-3: run `npm run typecheck && npm test && npm run test:migration && npm run lint` to completion; record each command pass/fail and timestamp in the VERIFICATION CI table.
    2. Record invariant greps in VERIFICATION: `APP_SALT` still equals `foolish-you-v1`; `app.json` marketing version is `2.4.2` with buildNumber `30` and versionCode `3`; CONFIGURATION What's New disclosure lines present (cross-check SHIP-01); this phase's diff does not modify `app/`, `components/`, `locales/`, `lib/puzzles/dailySelector.ts`, or salt.
    3. Do not install new npm packages. Do not run EAS production or ASC submit.
    4. Leave Maestro evidence rows and human signature unchecked — those belong to the blocking checkpoint.
  </action>
  <acceptance_criteria>
    - `npm run typecheck && npm test && npm run test:migration && npm run lint` exits 0
    - VERIFICATION CI table marks all four commands passed
    - `rg -n "APP_SALT.*=.*['\"]foolish-you-v1['\"]" constants/config.ts` matches
    - `rg -n 'typecheck|test:migration|npm run lint' .planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md` shows recorded CI results
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test && npm run test:migration && npm run lint</automated>
  </verify>
  <done>
    Full AGENTS CI green and recorded; salt/version invariants documented in VERIFICATION (D-14).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maestro evidence + hand QA sign-off (D-09..D-11, D-13, D-15)</name>
  <files>.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md, docs/qa/v2.4.2-ship/evidence/</files>
  <read_first>
    - `docs/qa/v2.4.2-ship/README.md`
    - `docs/TESTING.md` § v2.4.2
    - `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md` Maestro + hand-QA matrices
    - `docs/CONFIGURATION.md` 2.4.2 What's New (confirm disclosure before store paste later)
  </read_first>
  <action>
    Pause for human local sign-off. Do not change product code in this task unless defects are reported. After approval, check off Maestro and hand-QA rows and add the human signature in `v2.4.2-03-VERIFICATION.md`. Phase complete = repo ready only (D-13) — do not require EAS production or Submit for Review here.
  </action>
  <what-built>
    Marketing version `2.4.2`, bilingual What's New with board-drift disclosure, four Maestro smoke flows + `maestro-v242-ship.sh`, TESTING § v2.4.2, and VERIFICATION scaffold with CI already recorded green.
  </what-built>
  <how-to-verify>
    1. Start Metro (`npx expo start` or run:ios Dev Client) so port 8081 listens; boot the target Simulator (set `MAESTRO_UDID` if not using the script default).
    2. Run `./scripts/maestro-v242-ship.sh` and confirm evidence under `docs/qa/v2.4.2-ship/evidence/YYYYMMDD/` includes a log ending with `MAESTRO_EXIT=0` and at least one screenshot per game type (D-09, D-10).
    3. Hand QA — four types: using Dev force, open each of 数独 / 二进制 / 数绘 / 数回 for at least one playable day and confirm the board is usable (D-11).
    4. Hand QA — Sunday tight: tap `假日期·周日` (or known Sunday dateKey), force each type as needed, confirm Sunday feels harder / special path still one puzzle per day.
    5. Hand QA — Monday loose: set Simulator Date &amp; Time to a known Monday (e.g. 2026-07-06 or 2026-07-13), relaunch, force types as needed, confirm Monday feels easier than Sunday sampling.
    6. Hand QA — in-progress keep: on a normal day, fill a few cells, background or kill the app, relaunch, confirm same board and progress. Do not use Dev force-type mid-check (it clears today's archive). Unit SHIP-02 already covers stale-hash keep; this is the product reopen check.
    7. Spot-check CONFIGURATION 2.4.2 What's New still has the unplayed-date disclosure + in-progress keep line (SHIP-01).
    8. Check off Maestro + hand-QA rows in `v2.4.2-03-VERIFICATION.md` and sign the human signature block (D-15).
  </how-to-verify>
  <resume-signal>Type "approved" after signing the VERIFICATION matrices, or list defects found</resume-signal>
  <acceptance_criteria>
    - Human reply includes approval or an explicit defect list
    - On approval: Maestro and hand-QA rows checked in `v2.4.2-03-VERIFICATION.md` and signature present
    - Evidence dir exists with `MAESTRO_EXIT=0` log (`rg -n 'MAESTRO_EXIT=0' docs/qa/v2.4.2-ship/evidence/` matches after run)
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Maestro|手测|SHIP-01|D-11|signature|签字' .planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-VERIFICATION.md</automated>
  </verify>
  <done>
    Human confirms Maestro four-type evidence + Mon/Sun/four-type/keep hand QA; VERIFICATION signed; repo ready for post-phase EAS/ASC (D-09..D-11, D-13, D-15).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Dev Client + Maestro | `__DEV__` force tools can clear today's archive; production builds must not expose Dev panel |
| QA evidence directory | Screenshots/logs only — must not archive credentials or EAS tokens |
| VERIFICATION human signature | Phase completion claim without device proof is repudiation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v242-03-05 | Tampering | `APP_SALT` / selector during ship | high | mitigate | Task 2 invariant greps; fail phase if salt/selector/UI paths changed |
| T-v242-03-06 | Elevation of Privilege | DevTools shipping to release | medium | mitigate | Smokes use existing `__DEV__` panel only; do not weaken `DEV_TOOLS_ENABLED` gates |
| T-v242-03-07 | Information Disclosure | `docs/qa/v2.4.2-ship/evidence/` | low | mitigate | Evidence = Maestro logs + screenshots only; no secrets in README |
| T-v242-03-08 | Repudiation | VERIFICATION sign-off | medium | mitigate | Blocking human checkpoint; separate Maestro vs hand-QA rows; signature required (D-15) |
| T-v242-03-09 | Denial of Service | Maestro flake / wrong UDID | low | accept | One-process-per-flow script + Metro preflight + `MAESTRO_UDID` override documented |
| T-v242-03-SC | Tampering | npm installs | high | accept | No new packages; CI uses existing scripts only |
</threat_model>

<verification>
npm run typecheck && npm test && npm run test:migration && npm run lint
Human: ./scripts/maestro-v242-ship.sh + hand QA per docs/qa/v2.4.2-ship/README.md
</verification>

<success_criteria>
- Maestro evidence MAESTRO_EXIT=0 for four types (D-09, D-10)
- Hand QA signed: four types, Mon loose, Sun tight, in-progress reopen (D-11)
- AGENTS CI four-command green in VERIFICATION (D-14)
- Phase complete without requiring EAS/ASC (D-13)
</success_criteria>

<output>
Create `.planning/phases/v2.4.2-03-ship-2-4-2/v2.4.2-03-02-SUMMARY.md` when done
</output>
