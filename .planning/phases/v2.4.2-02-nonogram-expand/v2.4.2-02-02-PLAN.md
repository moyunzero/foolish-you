---
phase: v2.4.2-02-nonogram-expand
plan: 02
type: execute
wave: 2
depends_on:
  - v2.4.2-02-01
files_modified:
  - __tests__/lib/storage/fixtures/stale-playing-nonogram.json
autonomous: true
requirements:
  - NONO-01
user_setup: []
must_haves:
  truths:
    - "stale-playing-nonogram.json uses silly-face catalog board (8×8 solution + matching clues) instead of the prior minimal board (D-12)"
    - "Top-level and puzzle.puzzleHash are a deliberate stale token that differs from selectDailyGame for the fixture dateKey+seed+forceGameType nonogram (D-13)"
    - "Fixture remains status playing, isSnapshotPuzzleConsistent true, and hasNonEmptyPlayProgress true (≥1 FILL cell)"
    - "snapshotPrep.ship02 four-type it.each suite structure unchanged — only nonogram fixture content upgrades; no catalog-rows hard-align assert added (D-14)"
    - "prepareTodaySnapshot keeps identical puzzleHash, playState, and puzzle for the upgraded nonogram fixture (SHIP-02 keep contract)"
  artifacts:
    - path: __tests__/lib/storage/fixtures/stale-playing-nonogram.json
      provides: "silly-face-derived stale playing nonogram fixture"
  key_links:
    - from: "silly-face rows / computeClues"
      to: "fixture puzzle.solution + rowClues + colClues"
      via: "RESEARCH verified clue arrays"
    - from: "isSnapshotPuzzleConsistent"
      to: "prepareTodaySnapshot keep branch"
      via: "structural gate — stale hash must not equal canonical"
---

<objective>
Strengthen the SHIP-02 nonogram stale-playing fixture to a real prefix-catalog silly-face board with a deliberate stale puzzleHash, while leaving the four-type ship02 suite structure unchanged (D-12..D-14).

Purpose: After NONO-01 catalog growth, keep-if-consistent must still protect in-progress nonogram days with a realistic board (not a 1-cell stub).
Output: Upgraded `stale-playing-nonogram.json`; ship02 suite stays green without new catalog-align asserts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-CONTEXT.md
@.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-RESEARCH.md
@.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-PATTERNS.md
@.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-01-SUMMARY.md
@AGENTS.md
@__tests__/lib/storage/fixtures/stale-playing-nonogram.json
@__tests__/lib/storage/snapshotPrep.ship02.test.ts
@lib/storage/snapshotPrep.ts
@lib/storage/snapshotValidate.ts
@lib/puzzles/nonogram/patterns.ts
</context>

## Artifacts this phase produces

| Artifact | Plan | Notes |
|----------|------|-------|
| Exact catalog asserts + +30 patterns + zh/en titles | 01 | NONO-01, NONO-02 |
| SHIP-02 `silly-face` stale-playing nonogram fixture | 02 | D-12..D-14 |
| UI / `APP_SALT` / selector / generator / ship02 suite structure | — | Unchanged |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade stale-playing-nonogram to silly-face + stale hash</name>
  <files>__tests__/lib/storage/fixtures/stale-playing-nonogram.json</files>
  <read_first>
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-CONTEXT.md` (D-12, D-13, D-14)
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-RESEARCH.md` (How to Build silly-face Fixture + verified rowClues/colClues)
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-PATTERNS.md` (fixture target section + ship02 keep contract — read only)
    - `__tests__/lib/storage/fixtures/stale-playing-nonogram.json` (current minimal board envelope)
    - `__tests__/lib/storage/snapshotPrep.ship02.test.ts` (it.each contract — do not restructure)
    - `lib/puzzles/nonogram/patterns.ts` (silly-face rows at index 0)
    - Sibling fixtures `stale-playing-binary.json` / `stale-playing-sudoku.json` for envelope realism only
  </read_first>
  <behavior>
    - After upgrade: isSnapshotPuzzleConsistent(fixture) === true
    - hasNonEmptyPlayProgress(fixture) === true
    - selectDailyGame({ dateKey, seed, forceGameType: 'nonogram' }).puzzleHash !== fixture.puzzleHash
    - prepareTodaySnapshot(fixture) preserves puzzleHash, playState, and puzzle
    - snapshotPrep.ship02.test.ts still has four FIXTURES entries; no new expect comparing fixture.solution to NONOGRAM_PATTERNS rows
  </behavior>
  <action>
    Per D-12/D-13: replace the minimal nonogram puzzle body in stale-playing-nonogram.json with the silly-face catalog picture. Convert silly-face.rows to an 8×8 boolean solution (`1`→true). Set rowClues and colClues to the RESEARCH-verified arrays ([[6],[1,4,1],[1,1],[1,4,1],[1,2,1],[1,1],[6],[0]] and [[5],[1,1],[2,1,1],[2,2,1],[2,2,1],[2,1,1],[1,1],[5]]). Set pictureTitle to silly-face (or any string). Set both top-level puzzleHash and puzzle.puzzleHash to a fixed deliberate stale token such as stale-ship02-nonogram-silly-face that will not match canonical selectDailyGame for the fixture dateKey/seed. Keep status playing, version, dateKey, seed, startedAt unless a hash collision forces a seed tweak (unlikely with a fixed stale token). Fill playState as 8×8 mostly empty (-1) with at least one FILL (1) cell so progress is non-empty — discretion on which silhouette cells are filled. Per D-14: do not edit snapshotPrep.ship02.test.ts structure and do not add a catalog-rows equality assert. Do not edit production snapshotPrep/snapshotValidate unless a proven bug blocks the keep contract.
  </action>
  <acceptance_criteria>
    - `rg -n "silly-face|stale-ship02-nonogram-silly-face" __tests__/lib/storage/fixtures/stale-playing-nonogram.json` matches ≥1 for the stale hash token and/or pictureTitle.
    - Fixture puzzle.rows and puzzle.cols are 8; solution is nested length 8×8.
    - `npm test -- --testPathPattern='snapshotPrep.ship02' -x` exits 0.
    - `git diff --name-only` for this plan is limited to the nonogram fixture (and SUMMARY); snapshotPrep.ship02.test.ts has no structural rewrite.
    - Negative: do not introduce a test that asserts fixture.solution equals NONOGRAM_PATTERNS[0].rows (D-14 declined hard align).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --testPathPattern='snapshotPrep.ship02' -x</automated>
  </verify>
  <done>Nonogram SHIP-02 fixture is silly-face-based with deliberate stale hash; four-type keep suite green; D-12..D-14 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen JSON fixture → prepareTodaySnapshot | Untrusted shape; structural validation only |
| Catalog growth → in-progress keep path | Must not swap consistent playing boards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v24202-05 | Tampering | prepareTodaySnapshot keep path | high | mitigate | ship02 suite must stay green after fixture upgrade; forbid canonical-hash regen |
| T-v24202-06 | Tampering | Fixture puzzleHash accidentally equals canonical | medium | mitigate | Use fixed stale token; assert hash divergence in existing ship02 test |
| T-v24202-07 | Denial of service / UX | Empty playState failing progress gate | low | mitigate | Require ≥1 FILL cell in fixture playState |
| T-v24202-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
- `npm test -- --testPathPattern='snapshotPrep.ship02' -x`
- After both plans: `npm test -- --testPathPattern='nonogram/patterns|snapshotPrep.ship02' -x`
- Confirm no edits to `snapshotPrep.ship02.test.ts` suite shape, `APP_SALT`, `dailySelector`, generator, UI
</verification>

<success_criteria>
- D-12: fixture board derived from silly-face
- D-13: deliberate stale puzzleHash; consistency + keep contract green
- D-14: four-type ship02 suite unchanged; no catalog-rows hard-align assert
</success_criteria>

<output>
Create `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-02-SUMMARY.md` when done
</output>
