# Phase v2.4.2-02: Nonogram Expand（数绘扩库） - Context

**Gathered:** 2026-07-27
**Status:** Ready for planning

<domain>
## Phase Boundary

把数绘静态库 `NONOGRAM_PATTERNS` 从 **90 → 正好 120**（全 8×8），**只追加、不改前 90 前缀**；新图有 zh/en `titleKey`；tier 精确均衡到 **17/17/17/17/17/17/18**。配套加强 SHIP-02 数绘 stale-playing fixture（真实前缀库图）。不改 UI、不改选题权重、不改 `APP_SALT`。

**In scope:** NONO-01, NONO-02；前缀锁测试升级；`stale-playing-nonogram` fixture 加强。

**Out of scope / other phases:**
- 营销版本 `2.4.2` + 变盘披露 + Maestro → **v2.4.2-03**
- Feel / DIFF-03 → 暂停中的 v2.5
- 改 `dailySelector` 数绘权重、UI 难度标签、hint、第五玩法

</domain>

<decisions>
## Implementation Decisions

### Exact count & tier split

- **D-01:** 目标规模 **正好 120**（不是软区间）。
- **D-02:** Tier 直方图目标 **17/17/17/17/17/17/18**（现 13/13/13/13/13/13/12 → 先补齐 tier 6，再各 +4）。
- **D-03:** 单测断言 **精确** 该直方图与 `length === 120`（不允许 ±1 裁量）。
- **D-04:** 某稿不过 8×8 / 唯一性等 QA → **remake 直到凑满 120**，不得降规格。

### Content authorship

- **D-05:** **Claude 全权创作**并直接合入 `patterns.ts` + zh/en locales（同 v24-02 流程）。
- **D-06:** 主题延续 **混搭傻气剪影**（食物 / 动物 / 物件 / 胡闹混搭），不单偏某一类。
- **D-07:** 标题语气保持 **短、损一点、可认出物件**（现库「短路机器人」档）。
- **D-08:** 新条目继续填写废弃字段中文 **`title`**，与现库形状一致；展示仍以 locales 为准。

### Prefix lock depth

- **D-09:** 冻结 **前 90** 条的 **id 顺序 + rows + tier**（字节级不可改）；新图只能 **append**。
- **D-10:** 新 `id` / `titleKey`：**kebab-case 英文 slug，且 `titleKey === id`**。
- **D-11:** 测试落地：扩展 `__tests__/lib/puzzles/nonogram/patterns.test.ts` —— **冻结前 90 expected-id 列表** + length/tier 精确断言（可读，不要求 rows hash）。

### SHIP-02 nonogram fixtures

- **D-12:** **加强**现有 `__tests__/lib/storage/fixtures/stale-playing-nonogram.json`（替换最小盘），底板用前缀库图 **`silly-face`**。
- **D-13:** 「过时」用 **故意 stale 的 `puzzleHash` 字符串**；puzzle 结构仍须通过 `isSnapshotPuzzleConsistent`。
- **D-14:** `snapshotPrep.ship02` **仍为四题型同一 suite**；只升级 nonogram 那条 fixture/断言，**不**额外要求 fixture rows 与 catalog 再做一次硬对齐断言（fixture 内容来自 silly-face 即可）。

### Claude's Discretion

- 具体 30 张新图的题材选题与像素稿（须满足 D-06/D-07、全 8×8、可解/自洽线索）。
- expected-id 冻结列表的维护方式（内联数组 vs 从当前 HEAD 导出一次后锁死）。
- `silly-face` fixture 的 playState 局部进度怎么填（非空即可）。
- 是否顺手清理/更新 patterns 测试里旧的「84–98 / 12–14」阈值文案。

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Milestone / requirements
- `.planning/PROJECT.md` — v2.4.2 Content Depth
- `.planning/REQUIREMENTS.md` — NONO-01, NONO-02
- `.planning/ROADMAP.md` — Phase v2.4.2-02 success criteria
- `.planning/STATE.md` — current milestone position

### Prior phase decisions
- `.planning/phases/v2.4-sunday-special/v2.4-CONTEXT.md` — D-13..D-20 数绘扩库惯例（append-only、8×8、幽默、接受变盘）
- `.planning/phases/v2.4-sunday-special/v24-02-SUMMARY.md` — 90 图交付、tier 13×6+12、prefix/locale 测试模式
- `.planning/phases/v2.4.2-01-band-retune/v2.4.2-01-CONTEXT.md` — D-15 数绘库变更后补强 SHIP-02 fixture

### Code (must touch / must not break)
- `lib/puzzles/nonogram/patterns.ts` — `NONOGRAM_PATTERNS` / `patternsForTier`
- `lib/puzzles/nonogram/generator.ts` — 按 dateKey/tier 选图（不改选题权重）
- `locales/zh/patterns.ts` / `locales/en/patterns.ts` — titleKey 文案
- `__tests__/lib/puzzles/nonogram/patterns.test.ts` — length / tier / prefix / locale parity
- `__tests__/lib/storage/snapshotPrep.ship02.test.ts` — SHIP-02 keep 契约
- `__tests__/lib/storage/fixtures/stale-playing-nonogram.json` — 待加强的数绘 fixture
- `AGENTS.md` — daily determinism；storage safety；UI 层勿动算法

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `NONOGRAM_PATTERNS` + `patternsForTier` — 扩库主战场；append-only 增长
- `locales/{zh,en}/patterns.ts` — titleKey 映射；parity 测试已覆盖「每个 id 双语非空」
- `patterns.test.ts` — 已有 length 带、tier 直方图、前 30 prefix lock、locale parity；本 phase 升到 120 + 前 90 lock + 精确直方图
- `snapshotPrep.ship02.test.ts` + four fixtures — keep-if-consistent 已锁；只换 nonogram fixture 内容

### Established Patterns
- Content expansion: RED catalog asserts → GREEN append data → locale maps → parity green（v24-02）
- `titleKey === id`；废弃 `title` 仍填中文以保持对象形状
- 跨版本桶漂移可接受；披露归 v2.4.2-03

### Integration Points
- Generator / dailySelector 按库长度与 tier 池选图 — 扩库改变未开局日期的盘面（预期）
- 进行中 snapshot 不得因库变更被 `prepareTodaySnapshot` 换盘（SHIP-02）
- UI / reveal / gallery 靠 pattern id + i18n — 本 phase 不改 UI 组件

</code_context>

<specifics>
## Specific Ideas

- 目标直方图写死为 **17/17/17/17/17/17/18**（用户明确拒绝软 ±1）。
- SHIP-02 数绘底板明确用 **`silly-face`**（库内第一张）。
- Q4 最终锁定为「只升级 suite 内 nonogram case」，fixture 内容来自 silly-face，但不强制再加 catalog rows 硬对齐断言。

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

（变盘披露文案 / Maestro / `app.json` 2.4.2 仍属 v2.4.2-03。）

</deferred>

---

*Phase: v2.4.2-02-Nonogram Expand（数绘扩库）*
*Context gathered: 2026-07-27*
