---
phase: v2.4.2-02-nonogram-expand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/lib/puzzles/nonogram/patterns.test.ts
  - lib/puzzles/nonogram/patterns.ts
  - locales/zh/patterns.ts
  - locales/en/patterns.ts
autonomous: true
requirements:
  - NONO-01
  - NONO-02
user_setup: []
must_haves:
  truths:
    - "NONOGRAM_PATTERNS.length === 120 exactly (D-01, D-03) — CONTEXT overrides ROADMAP soft ≈120"
    - "Tier histogram is exactly [17,17,17,17,17,17,18] for tiers 0–6 (D-02, D-03) — no ±1 discretion"
    - "First 90 pattern ids stay prefix-stable: same order as frozen PREFIX_IDS; no deletes/edits to prefix id/rows/tier (D-09, D-11)"
    - "Exactly 30 new patterns appended after compass; all 8×8 rows matching /^[01]{8}$/; unique kebab-case ids; titleKey === id (D-04, D-10)"
    - "Every pattern id has non-empty zh and en titles in locales/*/patterns.ts (NONO-02, D-08 display via locales)"
    - "Humor silhouette mix (food/animal/object/silly) continues; short slightly mean recognizable titles (D-05, D-06, D-07)"
    - "APP_SALT, dailySelector, nonogram generator selection logic, and UI paths untouched"
  artifacts:
    - path: __tests__/lib/puzzles/nonogram/patterns.test.ts
      provides: "Exact length 120, exact tier hist, PREFIX_IDS×90 prefix lock, locale parity"
    - path: lib/puzzles/nonogram/patterns.ts
      provides: "Append-only +30 NonogramPattern entries after compass"
    - path: locales/zh/patterns.ts
      provides: "zh titles for all 30 new ids"
    - path: locales/en/patterns.ts
      provides: "en titles for all 30 new ids"
  key_links:
    - from: "NONOGRAM_PATTERNS append"
      to: "patternsForTier → selectPattern"
      via: "bucket growth changes unplayed dateKey picks — accepted drift; do not edit generator"
    - from: "pattern.id / titleKey"
      to: "locales zh+en patterns maps"
      via: "parity test requires key === id and non-empty strings"
---

<objective>
Expand the nonogram catalog from 90 to exactly 120 with append-only humorous 8×8 patterns, exact tier histogram 17/17/17/17/17/17/18, frozen first-90 id lock, and bilingual titleKey maps (NONO-01, NONO-02, D-01..D-11).

Purpose: Deepen the thinnest content pool with CONTEXT-exact gates (not ROADMAP soft ≈120 / ±1).
Output: Updated `patterns.test.ts` asserts; +30 patterns; zh/en titles.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-CONTEXT.md
@.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-RESEARCH.md
@.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-PATTERNS.md
@.planning/phases/v2.4-sunday-special/v24-02-SUMMARY.md
@AGENTS.md
@lib/puzzles/nonogram/patterns.ts
@locales/zh/patterns.ts
@locales/en/patterns.ts
@__tests__/lib/puzzles/nonogram/patterns.test.ts
</context>

## Artifacts this phase produces

| Artifact | Plan | Notes |
|----------|------|-------|
| Exact catalog asserts (length 120, hist 17×6+18, PREFIX_IDS×90) | 01 | D-01..D-03, D-09, D-11 |
| +30 append-only `NonogramPattern` entries | 01 | D-04..D-08, D-10; after `compass` |
| zh + en `titleKey` maps for new ids | 01 | NONO-02 |
| SHIP-02 `silly-face` stale-playing fixture | 02 | D-12..D-14 |
| UI / `APP_SALT` / `dailySelector` / generator | — | Explicitly unchanged |

**CONTEXT override:** ROADMAP/REQUIREMENTS soft ≈120 and tier ±1 are superseded by D-01/D-02/D-03 exact `120` and `[17,17,17,17,17,17,18]`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — exact catalog asserts + PREFIX_IDS×90</name>
  <files>__tests__/lib/puzzles/nonogram/patterns.test.ts</files>
  <read_first>
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-CONTEXT.md` (D-01..D-03, D-09, D-11)
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-RESEARCH.md` (section How to Freeze First-90 Expected IDs — paste PREFIX_IDS list; Exact catalog asserts)
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-PATTERNS.md` (patterns.test.ts section)
    - `__tests__/lib/puzzles/nonogram/patterns.test.ts` (current soft length/tier bands + 30-id PREFIX_IDS)
    - `lib/puzzles/nonogram/patterns.ts` (confirm current length 90 and last id `compass` — read only)
  </read_first>
  <behavior>
    - expect(NONOGRAM_PATTERNS).toHaveLength(120)
    - unique ids size === 120; unique deprecated titles size === 120
    - for tier 0..5 count === 17; tier 6 count === 18
    - slice(0,90).map(id) === frozen PREFIX_IDS of length 90 (silly-face … compass) from RESEARCH export — do not re-export after later append
    - existing titleKey===id, locale parity, 8×8 / clue self-consistency / transform suites remain
  </behavior>
  <action>
    Per D-01/D-02/D-03/D-09/D-11: replace the soft length band and soft per-tier band tests with exact equals (length 120; expected hist array [17,17,17,17,17,17,18]). Extend PREFIX_IDS from the first-30 list to the full frozen 90-id ordered list pasted from RESEARCH.md (silly-face through compass). Optionally rename test titles that still describe soft bands. Do not edit patterns.ts or locales in this task — RED must fail on length/hist until Task 2. Do not touch generator, APP_SALT, dailySelector, UI, or ship02 fixtures.
  </action>
  <acceptance_criteria>
    - `rg -n "toHaveLength\\(120\\)" __tests__/lib/puzzles/nonogram/patterns.test.ts` matches ≥1.
    - `rg -n "17, 17, 17, 17, 17, 17, 18" __tests__/lib/puzzles/nonogram/patterns.test.ts` matches ≥1 (or equivalent per-tier exact expects).
    - `rg -n "PREFIX_IDS" __tests__/lib/puzzles/nonogram/patterns.test.ts` matches ≥1 and the array lists exactly 90 string literals ending with compass.
    - Soft band helpers gone: after excluding comment-only lines, `rg -n "toBeGreaterThanOrEqual\\(84\\)|toBeLessThanOrEqual\\(98\\)|toBeGreaterThanOrEqual\\(12\\)|toBeLessThanOrEqual\\(14\\)" __tests__/lib/puzzles/nonogram/patterns.test.ts` exits 1 (no matches).
    - `npm test -- --testPathPattern='nonogram/patterns' -x` exits non-zero (RED) until Task 2 lands data.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --testPathPattern='nonogram/patterns' -x ; test $? -ne 0</automated>
  </verify>
  <done>patterns.test.ts encodes exact 120 / exact hist / PREFIX_IDS×90; suite fails RED against current 90-pattern catalog.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — append 30 patterns + zh/en titles</name>
  <files>lib/puzzles/nonogram/patterns.ts, locales/zh/patterns.ts, locales/en/patterns.ts</files>
  <read_first>
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-CONTEXT.md` (D-04..D-10)
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-RESEARCH.md` (Tier fill checklist; Pitfalls 1–4; Don't Hand-Roll uniqueness)
    - `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-PATTERNS.md` (entry shape, append after compass, locale append pattern, humor tone robot example)
    - `lib/puzzles/nonogram/patterns.ts` (append after last entry `compass` only)
    - `locales/zh/patterns.ts` / `locales/en/patterns.ts` (append after compass keys)
    - Task 1 updated `patterns.test.ts` (exact gates that must go green)
  </read_first>
  <behavior>
    - After append: length 120; hist [17,17,17,17,17,17,18]; PREFIX_IDS still matches first 90
    - Append plan: +6 patterns with tier 6, then +4 each for tiers 0–5 (total +30)
    - Every new id: kebab-case unique, titleKey === id, deprecated Chinese title filled (D-08), 8 rows of eight 0/1 chars
    - Every new id has non-empty zh + en locale strings; existing keys unchanged
    - Existing self-consistency / complete-valid / mirror tests pass for all 120; remake any draft that fails (D-04 vs existing QA — no new uniqueness solver)
  </behavior>
  <action>
    Per D-04..D-08/D-10 and NONO-01/NONO-02: Claude invents and appends exactly 30 NonogramPattern objects after `compass` in patterns.ts — never edit/reorder/rename any of the first 90 entries' id, rows, or tier (D-09). Fill tiers to exact hist: six new tier-6 entries first (12→18), then four new entries each for tiers 0–5 (13→17). Themes: mixed silly silhouettes (food/animal/object/nonsense) with short slightly mean recognizable Chinese deprecated titles and matching short en/zh locale strings (D-05/D-06/D-07). New ids must not collide with the frozen 90. In the same GREEN commit (or immediately after with tests green), append the 30 keys to both locales/zh/patterns.ts and locales/en/patterns.ts with key === id. Do not modify patternsForTier, patternSolution, generator.ts, APP_SALT, dailySelector, app/, components/, or ship02 fixtures (fixture is plan 02).
  </action>
  <acceptance_criteria>
    - `npm test -- --testPathPattern='nonogram/patterns' -x` exits 0.
    - `rg -n "id: 'compass'" lib/puzzles/nonogram/patterns.ts` still matches exactly once; new entries appear only after that object in the array.
    - `git diff --name-only` for this plan does not include `lib/puzzles/nonogram/generator.ts`, `lib/puzzles/dailySelector.ts`, `constants/config.ts`, `app.json`, or any `app/` / `components/` path.
    - Locale maps: every NONOGRAM_PATTERNS id resolves via existing parity test (covered by patterns.test green).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --testPathPattern='nonogram/patterns' -x</automated>
  </verify>
  <done>Catalog exactly 120 with exact hist; prefix frozen; bilingual titles for all new ids; NONO-01 and NONO-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static catalog → daily puzzle bytes | Offline curated data; selection hash % bucket.length |
| Locale maps → picture titles | Display strings only; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v24202-01 | Tampering | First-90 prefix id/rows/tier | high | mitigate | PREFIX_IDS×90 assert + append-only discipline; CR rejects prefix diffs |
| T-v24202-02 | Tampering | APP_SALT / dailySelector | high | mitigate | Scope fence: this plan must not edit salt or selector weights |
| T-v24202-03 | Integrity | Locale key miss for new id | medium | mitigate | Existing parity test (titleKey===id + non-empty zh/en) must stay green |
| T-v24202-04 | Spoofing | Unplayed dateKey board drift | low | accept | Product-accepted after catalog growth; disclose in v2.4.2-03 |
| T-v24202-SC | Tampering | npm installs | high | accept | No new packages this phase |
</threat_model>

<verification>
- `npm test -- --testPathPattern='nonogram/patterns' -x`
- Confirm `git diff` empty for `constants/config.ts`, `lib/puzzles/dailySelector.ts`, `lib/puzzles/nonogram/generator.ts`
- Optional: `npm run typecheck && npm run lint` before handoff to plan 02
</verification>

<success_criteria>
- NONO-01: exact length 120, exact hist [17,17,17,17,17,17,18], append-only first-90 lock (D-01..D-03, D-09, D-11)
- NONO-02: every new id has zh+en titles via titleKey / locales (D-08, D-10)
- No UI / APP_SALT / selector / generator logic changes
</success_criteria>

<output>
Create `.planning/phases/v2.4.2-02-nonogram-expand/v2.4.2-02-01-SUMMARY.md` when done
</output>
