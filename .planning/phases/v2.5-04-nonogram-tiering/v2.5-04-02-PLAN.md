---
phase: v2.5-04-nonogram-tiering
plan: 02
type: execute
wave: 2
depends_on:
  - v2.5-04-01
files_modified:
  - lib/puzzles/nonogram/patterns.ts
  - lib/puzzles/nonogram/generateForTier.ts
  - __tests__/lib/puzzles/nonogram/patterns.test.ts
  - __tests__/lib/puzzles/nonogram/generateForTier.test.ts
  - __tests__/lib/puzzles/nonogram/generator.test.ts
  - scripts/freeze-nonogram-difficulty-tiers.ts
autonomous: true
requirements:
  - TIER-04
user_setup: []
must_haves:
  truths:
    - "All 120 patterns carry frozen difficultyTier; weekday tier 0..6 hist remains [17,17,17,17,17,17,18] (D-02, D-10, D-11)"
    - "For every pattern, frozen difficultyTier equals rateNonogram(canonical clues).tier (D-04)"
    - "difficultyTier pool histogram is auditable; sum=120; easy count > 0; uneven OK — no force-rebalance (D-03, ROADMAP SC-1)"
    - "generateNonogramPuzzleForTier filters by difficultyTier, softens toward easier ≤3, sets softened=true when softer tier used; never returns harder than requested (D-05, D-06, D-07, ROADMAP SC-2)"
    - "Empty Easy pool throws loud; Expert-empty softens with injectable synthetic pool (D-05, D-06)"
    - "Legacy generateNonogramPuzzle(seed, dateKey?) and patternsForTier weekday path unchanged (D-08, D-09)"
  artifacts:
    - path: lib/puzzles/nonogram/patterns.ts
      provides: "NonogramPattern.difficultyTier + patternsForDifficultyTier; 120 frozen tags"
      exports: ["NONOGRAM_PATTERNS", "patternsForDifficultyTier", "patternsForTier", "patternSolution", "NonogramPattern"]
    - path: lib/puzzles/nonogram/generateForTier.ts
      provides: "generateNonogramPuzzleForTier filter+soften isomorphic return"
      exports: ["generateNonogramPuzzleForTier", "softenTowardEasier", "NONOGRAM_TIER_MAX_SOFTENS", "GenerateNonogramForTierResult"]
    - path: __tests__/lib/puzzles/nonogram/patterns.test.ts
      provides: "D-04 parity + SC-1 audit hist + weekday hist lock"
    - path: __tests__/lib/puzzles/nonogram/generateForTier.test.ts
      provides: "Soften empty-pool SC-2 + Easy throw + determinism + never-harder"
    - path: scripts/freeze-nonogram-difficulty-tiers.ts
      provides: "Optional one-shot dry-run printer (dev aid only; not imported at runtime)"
  key_links:
    - from: "patternsForDifficultyTier"
      to: "pattern.difficultyTier"
      via: "filter by DifficultyTier — never weekday tier 0..6"
    - from: "generateNonogramPuzzleForTier"
      to: "patternsForDifficultyTier + softenTowardEasier + puzzle build"
      via: "exact pool pick; soften ≤ NONOGRAM_TIER_MAX_SOFTENS=3; isomorphic return"
    - from: "patterns.test.ts D-04"
      to: "rateNonogram + pattern.difficultyTier"
      via: "∀p frozen === rate(canonical).tier"
---

<objective>
Freeze `difficultyTier` on all 120 curated patterns (dual-track with weekday `tier`), ship auditable pool counts, and deliver `generateNonogramPuzzleForTier` as library filter + soften-toward-easier ≤3 (D-02..D-11, TIER-04, ROADMAP SC-1/SC-2).

Purpose: Prove mastery-ready Nonogram pick+fallback without wiring hydrate/selector (deferred v2.5-05).
Output: tagged `patterns.ts`, `generateForTier.ts`, D-04/audit/soften tests; optional freeze script; legacy weekday path green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-CONTEXT.md
@.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-RESEARCH.md
@.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-PATTERNS.md
@.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-01-SUMMARY.md
@lib/puzzles/difficulty/tiers.ts
@lib/puzzles/binary/generateForTier.ts
@lib/puzzles/nonogram/rater.ts
@lib/puzzles/nonogram/patterns.ts
@lib/puzzles/nonogram/generator.ts
@lib/puzzles/nonogram/clues.ts
@lib/puzzles/nonogram/transform.ts
@lib/puzzles/rng.ts
@__tests__/lib/puzzles/nonogram/patterns.test.ts
@__tests__/lib/puzzles/nonogram/generator.test.ts
</context>

## Discretion locks (Claude's Discretion → executor MUST follow)

| Topic | Lock |
|-------|------|
| Field name | `difficultyTier: DifficultyTier` on every pattern (D-11) — keep weekday `tier: 0..6` |
| Freeze method | In-place edit of `patterns.ts`; do not reorder array, change `id`, or change weekday `tier` |
| Freeze source | Rate **canonical** patternSolution → computeClues (untransformed) (RESEARCH Pitfall 6) |
| peakTechnique on pattern | Do **not** freeze peak on pattern objects — only `difficultyTier` (D-02); recompute peak via rateNonogram in tests/forTier return |
| Distribution | Accept real hist; do not retune `NONOGRAM_EASY_MAX_SWEEPS` or rewrite patterns to equalize (D-03). If any pattern status ≠ solved, fix lineSolver before freeze. If Easy pool count is 0 after rate, stop and escalate (product) — do not ship |
| Softens | `NONOGRAM_TIER_MAX_SOFTENS = 3`; soften easier only; Easy empty → throw (D-05, D-06) |
| Return shape | `{ puzzle, ratedTier, peakTechnique, softened }` — include peakTechnique (D-07) |
| Pick model | Filter curated library — **no** carve attempt loop; no history-hash filter this phase |
| Injectable pool | `options?: { patterns?: NonogramPattern[] }` for SC-2 synthetic empty-pool tests |
| Puzzle build | Same transform seed key `nono-xform` + computeClues + computePuzzleHash as `generator.ts` |
| ratedTier return | Prefer `pattern.difficultyTier` (equals rate when D-04 holds); peakTechnique from rateNonogram on canonical clues |
| Dual helpers | Keep `patternsForTier` (weekday); add `patternsForDifficultyTier` — forTier must never call weekday helper |
| Freeze script | Optional `scripts/freeze-nonogram-difficulty-tiers.ts` prints id→tier; runtime must not import it |
| Out of scope | No dailySelector/hydrate wire (D-08); no change to `generateNonogramPuzzle` semantics (D-09); no UI badges; no new patterns to pad tiers; no APP_SALT |

## Artifacts this plan produces

| Symbol / file | Role |
|---------------|------|
| `NonogramPattern.difficultyTier` | Frozen mastery tag on all 120 |
| `patternsForDifficultyTier` | Pool filter by DifficultyTier |
| `generateNonogramPuzzleForTier` + soften const | Exact-tier pick + ≤3 soften |
| D-04 + audit + soften tests | SC-1 / SC-2 proof |
| Optional freeze script | Dev dry-run aid only |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze difficultyTier on 120 + D-04 parity + SC-1 audit</name>
  <files>lib/puzzles/nonogram/patterns.ts, __tests__/lib/puzzles/nonogram/patterns.test.ts, scripts/freeze-nonogram-difficulty-tiers.ts</files>
  <read_first>
    - `.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-CONTEXT.md` (D-02, D-03, D-04, D-10, D-11)
    - `.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-RESEARCH.md` (Pattern 4 freeze workflow, audit hist, Key Constants)
    - `.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-01-SUMMARY.md` (rater API from Wave 1)
    - `lib/puzzles/nonogram/rater.ts`
    - `lib/puzzles/nonogram/patterns.ts` (type + patternsForTier)
    - `lib/puzzles/nonogram/clues.ts`
    - `__tests__/lib/puzzles/nonogram/patterns.test.ts`
    - `lib/puzzles/difficulty/tiers.ts`
  </read_first>
  <behavior>
    - NonogramPattern type requires difficultyTier: DifficultyTier
    - NONOGRAM_PATTERNS.length === 120; weekday hist still [17,17,17,17,17,17,18]
    - For each pattern id, pattern.difficultyTier === rateNonogram(cluesFromCanonicalPattern).tier and status solved
    - Audit hist sums to 120; hist.easy > 0; do not assert equal tier counts
    - patternsForDifficultyTier(tier) returns only patterns with that difficultyTier
  </behavior>
  <action>
    Per D-02/D-03/D-04/D-10/D-11: dry-run rateNonogram over all 120 via optional script or inline Node/Jest helper; document resulting hist in SUMMARY (do not retune thresholds for balance). Extend `NonogramPattern` with required `difficultyTier` importing `DifficultyTier` from `../difficulty/tiers`. Comment weekday `tier` field clarifying it is Mon=0..Sun=6 and must not be confused with difficultyTier.
    In-place add `difficultyTier: '…'` to every pattern object matching rate output — preserve id order and weekday `tier` values. Export `patternsForDifficultyTier(tier, patterns = NONOGRAM_PATTERNS)`.
    Extend `patterns.test.ts`: keep length/prefix/weekday hist green; add it.each D-04 freeze parity for all 120; add SC-1 audit hist (sum 120, easy count must be greater than 0, no equal-count assert). Optional: commit `scripts/freeze-nonogram-difficulty-tiers.ts` that prints id/weekdayTier/difficultyTier/peak/sweeps/probes — must not be imported from app or lib runtime. If any pattern incomplete/budget_exhausted, fix Wave-1 lineSolver/rater before committing freeze (do not invent weekday→DifficultyTier mapping).
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/nonogram/patterns.test.ts --selectProjects unit --no-coverage 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `patterns.ts` NonogramPattern type includes field name difficultyTier
    - Every object in NONOGRAM_PATTERNS has a difficultyTier property with value easy, medium, hard, or expert
    - `patterns.ts` exports `patternsForDifficultyTier`
    - `patterns.test.ts` asserts weekday hist array 17,17,17,17,17,17,18 still
    - `patterns.test.ts` contains D-04-style assert that frozen difficultyTier matches rateNonogram tier for patterns
    - `patterns.test.ts` audits difficultyTier counts summing to 120 with easy greater than 0
    - Pattern array length remains 120 and first id remains silly-face (no reorder)
  </acceptance_criteria>
  <done>120 patterns dual-tagged; D-04 CI lock + SC-1 audit hist green; weekday hist unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generateNonogramPuzzleForTier filter + soften + SC-2 tests</name>
  <files>lib/puzzles/nonogram/generateForTier.ts, __tests__/lib/puzzles/nonogram/generateForTier.test.ts, __tests__/lib/puzzles/nonogram/generator.test.ts</files>
  <read_first>
    - `.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-CONTEXT.md` (D-05, D-06, D-07, D-08, D-09)
    - `.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-RESEARCH.md` (Pattern 5 filter≠carve, soften loop)
    - `.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-PATTERNS.md` (generateForTier analog)
    - `lib/puzzles/binary/generateForTier.ts`
    - `lib/puzzles/nonogram/generator.ts`
    - `lib/puzzles/nonogram/patterns.ts`
    - `lib/puzzles/nonogram/rater.ts`
    - `lib/puzzles/difficulty/tiers.ts`
    - `lib/puzzles/rng.ts`
    - `__tests__/lib/puzzles/binary/generateForTier.test.ts`
    - `__tests__/lib/puzzles/nonogram/generator.test.ts`
  </read_first>
  <behavior>
    - NONOGRAM_TIER_MAX_SOFTENS === 3
    - softenTowardEasier ladder: expert→hard→medium→easy; easy stays easy
    - Same seed+targetTier → identical puzzleHash twice
    - When softened===false, ratedTier === targetTier
    - When softened===true, tierIndex(ratedTier) is strictly below original target; never harder
    - Synthetic library with all expert remapped → Expert target returns softened true and easier ratedTier
    - Synthetic Easy-empty library → throws Error containing Failed to generate nonogram
    - Legacy generateNonogramPuzzle(seed, dateKey?) tests still pass
    - forTier must not call patternsForTier (weekday)
  </behavior>
  <action>
    Per D-05/D-06/D-07/D-08/D-09: add `generateForTier.ts` with `NONOGRAM_TIER_MAX_SOFTENS = 3`, duplicated `softenTowardEasier` using `tierIndex`/`tierFromIndex`, and `generateNonogramPuzzleForTier(seed, targetTier, options?)`. Soften loop: for soft 0..3, pool = patternsForDifficultyTier(tier, options?.patterns ?? NONOGRAM_PATTERNS); if nonempty, deterministic pick via hashStringToSeed(`${seed}:nono-tier-${tier}-${soft}`) % pool.length; build puzzle with same pickTransform/`nono-xform`/applyTransform/computeClues/computePuzzleHash as generator.ts; return { puzzle, ratedTier: pattern.difficultyTier, peakTechnique from rateNonogram(canonical), softened }. Empty pool → soften easier; if already Easy and empty → break then throw Error(`Failed to generate nonogram for tier ${targetTier}`). Never soften harder; no carve attempts; no history hash; do not edit generateNonogramPuzzle body semantics.
    Write `generateForTier.test.ts` (TDD): soften ladder, max softens=3, determinism, never-harder, softened false exact match, SC-2 Expert-empty synthetic injectable pool, Easy-empty throw. Re-run legacy `generator.test.ts`. Do not wire dailySelector / mastery / hydrate / UI.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/puzzles/nonogram/generateForTier.test.ts __tests__/lib/puzzles/nonogram/generator.test.ts __tests__/lib/puzzles/nonogram/patterns.test.ts --selectProjects unit --no-coverage 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - `generateForTier.ts` exports `generateNonogramPuzzleForTier` and `softenTowardEasier`
    - `generateForTier.ts` defines `NONOGRAM_TIER_MAX_SOFTENS` as 3
    - `generateForTier.ts` return type includes peakTechnique and softened
    - `generateForTier.test.ts` covers Expert empty-pool soften with softened true and Easy-empty throw
    - Region `lib/puzzles/nonogram/generateForTier.ts`: does not call patternsForTier — `grep -c 'patternsForTier' lib/puzzles/nonogram/generateForTier.ts | grep -q '^0$'`
    - `git diff --name-only` for this plan work does not list `lib/puzzles/dailySelector.ts` or mastery Context files
    - Legacy `generator.test.ts` still passes
  </acceptance_criteria>
  <done>TIER-04 complete: freeze + audit + forTier soften; ROADMAP SC-1/SC-2 satisfied; legacy weekday path intact; selector unwired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Freeze metadata → forTier pick | Wrong field (weekday tier vs difficultyTier) yields wrong mastery puzzle |
| generateNonogramPuzzleForTier → rateNonogram | Peak recompute on pick; must stay budgeted |
| Empty pool → caller | Silent wrong puzzle vs loud throw |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2.5-04-02-01 | Tampering | Dual-field confusion | high | mitigate | Separate patternsForDifficultyTier vs patternsForTier; forTier grep-gate no weekday helper; weekday hist regression |
| T-v2.5-04-02-02 | Elevation of Privilege | Silent empty Easy pool | high | mitigate | Throw loud when Easy empty after softens (D-05); generateForTier.test.ts Easy-empty case |
| T-v2.5-04-02-03 | Tampering | Soften to harder | medium | mitigate | softenTowardEasier only; assert tierIndex(rated) ≤ tierIndex(target) |
| T-v2.5-04-02-04 | Tampering | Freeze drift | medium | mitigate | D-04 ∀pattern frozen === rateNonogram; CI fails on rater/tag drift |
| T-v2.5-04-02-05 | Denial of Service | rate on forTier pick | medium | mitigate | Reuse Wave-1 probe caps; 8×8 curated only |
| T-v2.5-04-02-06 | Information Disclosure | N/A | low | accept | Offline local puzzle lib; no network/PII |
| T-v2.5-04-SC | Tampering | npm installs | high | mitigate | No new packages; legitimacy N/A |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx jest __tests__/lib/puzzles/nonogram/patterns.test.ts __tests__/lib/puzzles/nonogram/generateForTier.test.ts __tests__/lib/puzzles/nonogram/generator.test.ts --selectProjects unit --no-coverage` passes
- [ ] `npm run typecheck` clean
- [ ] Weekday hist [17×6+18] unchanged; length 120; D-04 green; Easy pool count greater than 0
- [ ] Soften SC-2 + Easy throw covered
- [ ] No dailySelector / mastery hydrate / UI edits; generator legacy API intact
</verification>

<success_criteria>
- TIER-04 delivered: 120 tagged + generateNonogramPuzzleForTier
- ROADMAP SC-1: auditable per-tier counts (Easy non-empty)
- ROADMAP SC-2: empty-target soften measurable via softened flag
- D-08/D-09: selector unwired; weekday generate path unchanged
</success_criteria>

<output>
Create `.planning/phases/v2.5-04-nonogram-tiering/v2.5-04-02-SUMMARY.md` when done
</output>
