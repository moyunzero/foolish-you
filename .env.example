# Optional local overrides. Copy to .env.local — never commit real .env files.
# Use only EXPO_PUBLIC_* for client-safe values. Apple/Google signing secrets
# belong in EAS Secrets or local ~/.eas, not in this repo.
